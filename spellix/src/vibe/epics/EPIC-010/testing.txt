---

Feature-001: Add animated magical night sky background to gameplay layout

Test Status

* passed

Manual Test Steps And Expected Results

1. Start a game and inspect the area behind the board and gameplay sidebar.
   Expected: Gameplay has a dark magical night-sky gradient covering the full layout, while the board, sidebar, text, and controls remain above it and readable.
2. Watch the background for at least 20 seconds.
   Expected: Small stars twinkle at varied positions, sizes, opacity levels, durations, and delays; the pattern does not flash uniformly.
3. Continue watching the background for colourful effects.
   Expected: Subtle glowing orbs, comet-like trails, and swirls appear at varied positions and times, move smoothly, fade away, and do not permanently clutter the screen.
4. Roll dice, select board destinations, open Spells and Settings, use potion UI, and interact with each modal.
   Expected: The decorative layer blocks no clicks, remains behind gameplay and modals, and does not change dice, movement, spell, potion, turn, or modal behavior.
5. Navigate away from Gameplay to Start, Game Setup, Battle, and Reward pages.
   Expected: The magical night sky appears only on Gameplay and creates no extra scroll bars or lingering animation elements on other pages.
6. Enable reduced-motion at operating-system or browser level and reload Gameplay.
   Expected: Stars become static and spell effects are hidden, avoiding continuous motion while gameplay remains fully usable.
7. Resize the browser around the board and inspect the console.
   Expected: The background continues covering the gameplay viewport without changing layout size, and no animation, rendering, or interaction errors appear.

Automated Tests Completed

* Tests were added first and failed because the dedicated component and Gameplay integration did not exist.
* `src/components/gameplay/MagicalNightSky/MagicalNightSky.test.jsx` passes all four tests for 48 varied stars, nine mixed orb/comet/swirl effects, stable descriptors across rerenders, CSS animation contracts, pointer transparency, and reduced-motion handling.
* A failing isolation test showed the first implementation consumed 369 global `Math.random` values; the final component uses a local seeded generator, and the regression now confirms decorative generation does not alter gameplay randomness.
* The new `src/pages/GameplayPage.test.jsx` integration assertion passes and confirms the aria-hidden sky renders with the existing board and gameplay panel.
* Source inspection confirms `MagicalNightSky` is imported only by `GameplayPage`; existing Start, Game Setup, Battle, Reward, and modal components were not changed.
* The broader GameplayPage run passed 8 of 14 tests, including the new integration plus existing language, potion, movement, and spell flows.
* All changed JavaScript and JSX files passed the repository-installed ESLint binary.
* `npm run build` completed successfully, and `git diff --check` reported no whitespace errors.

Automated Tests Still Needed

* Six existing GameplayPage assertions remain stale outside this feature: three expect the former shorter starting-warning sentence, one expects old `Dice result: 4` copy, one expects the former current-player alignment, and one expects the former 100px Spells player image.
* JSDOM and CSS source contracts cannot visually measure animation smoothness, brightness, layering appearance, viewport coverage, real pointer hit-testing, or runtime frame performance.

Manual Testing Still Needed

* A browser pass is still needed for visual intensity, animation variety and smoothness, actual layering, pointer interaction, responsive coverage, reduced-motion behavior, route isolation, performance, and console output.

---

Feature-002: Add styled Potions area to gameplay UI

Test Status

* passed

Manual Test Steps And Expected Results

1. Start Gameplay with an English current player who has no potions.
   Expected: A compact dark-brown Potions area appears directly below the Spells table with yellow text, a visible `Potions` title, `0/3`, intentional empty space, and no scrollbar.
2. Give the current player one, two, and three potions in turn.
   Expected: The area shows `1/3`, `2/3`, and `3/3`; each potion fits in the panel without breaking the layout or creating a scrollbar.
3. Inspect each rendered potion and focus or hover it.
   Expected: The shared flask icon, potion colour, glow, localized name, and localized tooltip remain unchanged and readable.
4. Advance from an English player to a Japanese player with a different potion collection.
   Expected: The title changes to `ポーション`, names and tooltips change to Japanese, and the capacity and items update to the new current player's collection.
5. Gain, discard, replace, or otherwise remove a potion through existing game flows.
   Expected: The displayed slots and `current/3` capacity update with the stored collection, while reward and potion behavior remains unchanged.
6. Inspect the Gameplay sidebar, board, buttons, settings, player information, and modal overlays at the supported viewport size.
   Expected: The Potions area stays near Spells and overlaps none of those elements; no console errors occur.

Automated Tests Completed

* Tests were added first and six new assertions failed because the panel classes, capacity display, div slots, and stylesheet did not yet exist.
* `src/features/potions/PotionList.test.jsx` passes all nine tests for persistent empty rendering, exact `0/3` through `3/3` capacities, gain/removal rerenders, div-only markup, global `PotionIcon` usage, English and Japanese icon content, title font class, fantasy colours, and no-scroll CSS.
* The full `src/pages/GameplayPage.test.jsx` run passes both potion integration tests, confirming the empty `0/3` state and turn-based collection, language, icon, tooltip, and capacity updates.
* Existing potion capacity tests continue to define the shared maximum as three; `PotionList` imports that existing constant rather than changing capacity behavior.
* The repository-installed ESLint binary passes all Feature-002 JavaScript and JSX files.
* `npm run build` completes successfully, and `git diff --check` reports no whitespace errors.

Automated Tests Still Needed

* Six pre-existing GameplayPage assertions remain stale outside this feature: three expect the former shorter starting-warning sentence, one expects old `Dice result: 4` copy, one expects the former current-player alignment, and one expects the former 100px Spells player image.
* JSDOM and source contracts cannot visually confirm real-browser spacing, readability, overlap, or scrollbar behavior at every supported viewport.

Manual Testing Still Needed

* A browser pass is still needed for the fantasy styling, 0-3 potion fit, hover/focus tooltips, sidebar overlap, live reward/discard flows, and console output.

---

Bug-001: Gameplay magical background needs more stars and continuous spell effects

Test Status

* passed

Manual Test Steps And Expected Results

1. Start Gameplay and inspect the full night-sky background behind the board and sidebar.
   Expected: The sky is noticeably fuller than before, with many small stars distributed across the entire layout while gameplay content remains readable.
2. Watch the background continuously for at least 20 seconds.
   Expected: A new spell effect appears every roughly 0.5-1.5 seconds, completed effects disappear, and there is no long period without recent magical activity.
3. Compare effects appearing near the top, bottom, left, right, and centre.
   Expected: Effects use varied random positions across the full background rather than repeatedly clustering in one area.
4. Observe the effect shapes, colours, sizes, movement, and timing.
   Expected: Orbs, comets, and swirls appear in varied blue, purple, green, yellow, pink, and orange treatments with different movement and fade timing.
5. Roll dice, select board destinations, open each modal, and interact with Spells, Potions, and Settings while effects spawn.
   Expected: The sky stays behind all content, blocks no click or drag interaction, creates no scrollbar, and does not alter gameplay randomness or state.
6. Leave Gameplay while effects are active, wait several seconds, and inspect the console and performance tools if available.
   Expected: The sky unmounts cleanly with no timer-driven updates, memory leak warnings, or console errors after navigation.
7. Enable reduced motion and reload Gameplay.
   Expected: Stars remain static and spell effects remain hidden, while Gameplay stays fully usable.

Automated Tests Completed

* The original four sky tests passed during reproduction, confirming the reported implementation still rendered only 48 stars and nine permanently looping mount-time effects.
* Three new regressions then failed for the expected reasons: star count was still 48, the active effect set was nine static nodes, and spell CSS used infinite iterations.
* `src/components/gameplay/MagicalNightSky/MagicalNightSky.test.jsx` now passes all five tests for 96 varied stars, an initial orb/comet/swirl mix, continuous 500-1500 ms spawning, broad edge-to-edge positions, colour/type variety, finite animations, completed-node removal, an eight-node active cap, timer cleanup on unmount, stable star descriptors, local RNG isolation, pointer transparency, and reduced-motion CSS.
* The full `src/pages/GameplayPage.test.jsx` run remains at its established 8 of 14 passing baseline, including the magical-sky Gameplay integration plus existing language, potion, movement, and spell flows.
* The repository-installed ESLint binary passes the changed sky component and tests.
* `npm run build` completes successfully, and `git diff --check` reports no whitespace errors.

Automated Tests Still Needed

* Six pre-existing GameplayPage assertions remain stale outside this bug: three expect the former shorter starting-warning sentence, one expects old `Dice result: 4` copy, one expects the former current-player alignment, and one expects the former 100px Spells player image.
* JSDOM cannot measure real animation frame performance, visual density, perceived randomness, actual pointer hit-testing, or browser memory usage over a long session.

Manual Testing Still Needed

* A browser pass is still needed to confirm visual density, continuous effect cadence, full-area distribution, smooth fading, interaction layering, reduced motion, long-session performance, navigation cleanup, and console output.

---

Bug-002: Potions area should match committed spell slot display styling

Test Status

* passed

Manual Test Steps And Expected Results

1. Start Gameplay with a player who has committed Spells and compare the Spells and Potions panels in the sidebar.
   Expected: Both panels use the same parchment gradient, brown border, rounded corners, inset/drop shadow, dark text, width, padding, and overall component-family treatment.
2. Compare the `Spells` and `Potions` headings.
   Expected: Both headings use matching 18px bold text, letter spacing, dark colour, and aligned internal spacing; the numeric potion capacity remains clearly visible beside its title.
3. Inspect a player with one, two, and three potions.
   Expected: Each potion sits inside a bordered parchment slot matching committed spell columns, all slots remain aligned, and no scrollbar appears.
4. Inspect a player with no potions.
   Expected: The styled Potions panel remains visible with `0/3` and intentional empty space beneath the header.
5. Advance between English and Japanese players with different potion collections.
   Expected: The localized title, names, tooltips, icons, colours, glow, and `current/3` capacity continue updating without any style or layout break.
6. Inspect the board, sidebar controls, player information, Settings, and modal overlays.
   Expected: The matched Potions panel stays directly below Spells, overlaps nothing, creates no extra scrollbar, and produces no console errors.

Automated Tests Completed

* The existing source inspection reproduced the mismatch: Potions used a dark `#3a2013` background, yellow text, a different border, and an 8px radius while committed Spells used its established parchment treatment.
* A new exact-style regression failed first on the former 10px Potions gap and would also reject the independent background, border, radius, text, heading, shadow, and unframed slot styles.
* `src/features/potions/PotionList.test.jsx` now passes all nine tests, including exact committed-display container, heading, and slot-column CSS plus existing 0-3 capacity, gain/removal, div-only markup, no-scroll, English/Japanese, global icon, tooltip, colour, and glow behavior.
* The full `src/pages/GameplayPage.test.jsx` run remains at its established 8 of 14 passing baseline; both potion integration tests pass for empty state and current-player turn updates.
* The repository-installed ESLint binary passes the PotionList component and tests.
* `npm run build` completes successfully, and `git diff --check` reports no whitespace errors.

Automated Tests Still Needed

* Six pre-existing GameplayPage assertions remain stale outside this bug: three expect the former shorter starting-warning sentence, one expects old `Dice result: 4` copy, one expects the former current-player alignment, and one expects the former 100px Spells player image.
* JSDOM and CSS source contracts cannot visually compare rendered panels, measure real spacing or overlap, or confirm scrollbar behavior at every supported viewport.

Manual Testing Still Needed

* A browser pass is still needed for the direct Spells/Potions visual comparison, 0-3 potion fit, localization, tooltips, sidebar overlap, scrollbar behavior, and console output.

---

Bug-003: Potion elements should remove rarity/type text and use fixed matching slot sizes

Test Status

* passed

Manual Test Steps And Expected Results

1. Start Gameplay with a current player who owns potions of different rarities and Board/Battle availability.
   Expected: Each gameplay potion shows only its flask and localized name; no `Common`, `Rare`, `Battle`, `Board`, `Both`, separator, or metadata paragraph appears.
2. Inspect players with one, two, and three potions, including potions with short and long names.
   Expected: Every potion slot is exactly 86px wide, all slots in the row stretch to the same height, long names wrap readably, and the layout remains aligned.
3. Inspect a full three-potion row.
   Expected: All three fixed slots fit inside the Potions panel with consistent 8px gaps and no horizontal or vertical scrollbar.
4. Advance between English and Japanese players with different potion collections.
   Expected: Names and tooltips continue using the current player's language, and differing name lengths do not create uneven slot heights.
5. Hover or focus each potion icon.
   Expected: The localized tooltip, potion colour, and glow remain unchanged even though visible rarity/type metadata is gone.
6. Gain, remove, discard, or replace potions through existing flows.
   Expected: Capacity still updates through `0/3`, `1/3`, `2/3`, and `3/3`; reward, rarity, availability, and potion data behavior remain unchanged.

Automated Tests Completed

* Two new regressions failed before implementation because gameplay still rendered both metadata paragraphs and the component source still contained the metadata `p` tag; the CSS also retained flexible widths and top alignment.
* `src/features/potions/PotionList.test.jsx` passes all nine tests for absent rarity/type metadata, preserved names/icons/tooltips/colours/glow, exact 86px width/min-width/flex basis, stretch-based equal heights, 0-3 capacity, current collection updates, div-only markup, no scrolling, localization, and committed-display styling.
* The full `src/pages/GameplayPage.test.jsx` run remains at its established 8 of 14 passing baseline; both potion integration tests pass and now confirm metadata stays absent across English/Japanese turn changes.
* Source inspection confirms `rarity` and `availability` remain in `src/data/potions.js`; no reward, filtering, or potion data file changed.
* The repository-installed ESLint binary passes the PotionList component and affected tests.
* `npm run build` completes successfully, and `git diff --check` reports no whitespace errors.

Automated Tests Still Needed

* Six pre-existing GameplayPage assertions remain stale outside this bug: three expect the former shorter starting-warning sentence, one expects old `Dice result: 4` copy, one expects the former current-player alignment, and one expects the former 100px Spells player image.
* JSDOM and CSS source contracts cannot measure actual equal rendered heights, long-name wrapping, or scrollbar behavior across every supported browser and viewport.

Manual Testing Still Needed

* A browser pass is still needed for equal slot heights, long English/Japanese name wrapping, full-row fit, tooltip behavior, visual alignment, scrollbar behavior, and console output.

---

Feature-003: Add River Mini Game debug trigger, mini game flow, loot chest page, and mini game lose page

Test Status

* passed

Manual Test Steps And Expected Results

1. Start Gameplay, open Settings, open Debug, and inspect the Mini Games section.
   Expected: `Start River Mini Game` is available for the current player and opens the standalone River Mini Game page without changing the active player.
2. Inspect the River Mini Game at the start of several new attempts.
   Expected: The supplied river image covers the viewport without tiling; each attempt shows three rows of three hidden-status rocks, with only Row 1 clickable and safe positions varying between attempts.
3. Choose safe rocks through the three rows.
   Expected: Row 1 and Row 2 each contain two safe choices, Row 3 contains one, previous/future rows stay disabled, instructions advance, and the third safe choice opens Loot Chest.
4. Click an unsafe rock in any active row.
   Expected: The instruction immediately reports the unsafe choice, the selected rock animates for one second, all rocks become disabled, and only then does the Mini Game Failed page open.
5. Win, click Continue on Loot Chest, and close the board modal.
   Expected: Gameplay returns with the same player active, the modal says `You crossed the river! You may roll again.`, and Roll Dice is available after closing it.
6. Lose, then click Return to Board.
   Expected: Gameplay returns with the next player active and no River win notice.
7. After both return paths, use board controls, Settings, Spells, Potions, and an existing battle trigger.
   Expected: Existing gameplay remains usable, overlays do not conflict, and no console errors occur.

Automated Tests Completed

* The five new focused suites failed before implementation on missing River modules/pages and missing mini-game context methods.
* `src/features/miniGames/riverMiniGame.test.js` verifies three rows of three rocks, exact safe counts of 2/2/1, and random-source-dependent safe positions.
* `src/pages/MiniGames/RiverMiniGame.test.jsx` verifies current-player display, three row groups, active-row-only interaction, instruction progression, win routing, the delayed unsafe route, animation state, River background CSS, and unsafe timer cleanup on unmount.
* `src/features/miniGames/miniGameState.test.jsx` verifies stored type/result/player/return behavior, same-player River wins, the roll-again notice and dismissal, and next-player loss returns.
* `src/pages/MiniGames/MiniGameResultPages.test.jsx` verifies both shared page buttons resolve mini-game state and return to Gameplay.
* `src/AppMiniGames.test.jsx` verifies the Debug Mini Games trigger, River route/current player, Gameplay win modal, dismissal, and enabled Roll Dice control.
* The focused suite passes 5 suites and 13 tests. The expanded run with existing DebugModal, GameSetupContext, and gameSetup regressions passes 8 suites and 36 tests.
* The full `src/pages/GameplayPage.test.jsx` run remains at its established 8 of 14 passing baseline, with exactly the same six unrelated stale assertions.
* The repository-installed ESLint binary passes every Feature-003 implementation and test file.
* `npm run build` compiles successfully, including the River image import and all three routes; `git diff --check` reports no whitespace errors.

Automated Tests Still Needed

* `src/App.test.jsx` remains blocked before test execution by the repository's existing `source-map-support` `generatedLine` crash; the dedicated App mini-game integration suite passes.
* Six pre-existing GameplayPage assertions remain stale outside this feature: three expect the former shorter starting-warning sentence, one expects old `Dice result: 4` copy, one expects the former current-player alignment, and one expects the former 100px Spells player image.
* JSDOM cannot visually confirm the real background crop, rock animation smoothness, overlap, or responsiveness in supported browsers.

Manual Testing Still Needed

* A browser pass is still needed for the river background crop, safe-position variation across fresh attempts, rock animation timing, complete win/loss navigation, current-player continuity, next-player handoff, board/modal interaction, responsive fit, and console output.

---

Feature-004: Rework River Mini Game layout, animations, translations, and rock images

Test Status

* passed

Manual Test Steps And Expected Results

1. Start the River Mini Game as an English player and inspect the initial page.
   Expected: The old title and current-player text are absent; the bold main instruction, English Row 1 instruction, one row of three rock images, and the current player's colour/gender character image appear in that order.
2. Compare the three visible rocks and restart the River Mini Game several times.
   Expected: Each row uses three different images from `src/images/miniGames/rocks/`, images remain fixed during an attempt, different rows may reuse images, bottoms align, and taller rocks extend upward without distortion.
3. Hover each idle rock, then select a safe Row 1 rock.
   Expected: Hover provides a small scale/brightness cue without moving layout; the selected rock receives safe feedback, all rocks disable, Row 1 moves downward and fades out, and Row 2 enters from above before becoming clickable.
4. Select a safe Row 2 rock and then a safe Row 3 rock.
   Expected: Row 2 transitions to Row 3 in the same sequence; the final safe rock animates, the English win message remains visible briefly, and then Loot Chest opens.
5. Select an unsafe rock on any active row.
   Expected: The selected image shakes/sinks, the English loss message is visible for one second, no further rock can be clicked, and then Mini Game Failed opens.
6. Repeat the safe and unsafe flows as a Japanese player.
   Expected: The main instruction, each row instruction, win message, and loss message use the specified Japanese text and Japanese font class.
7. Repeat with a missing or invalid player language.
   Expected: All River instructions and results fall back to English.
8. Continue from Loot Chest and return from Mini Game Failed.
   Expected: Existing shared behavior is preserved: a win keeps the same player and roll-again notice, while a loss advances to the next player.

Automated Tests Completed

* Regressions were added before implementation and exposed the old three-row placeholder layout, missing image data/translation helper, instant safe progression, absent transition states, and immediate win navigation; one unsupported Jest matcher was corrected before implementation.
* `src/features/miniGames/riverMiniGame.test.js` verifies the unchanged 2/2/1 safe counts, random safe positions, three unique images per row, valid image sources, and permitted image reuse across rows.
* `src/i18n/translations.test.js` verifies every exact English/Japanese River main, row, win, and loss string plus invalid-language English fallback.
* `src/pages/MiniGames/RiverMiniGame.test.jsx` verifies one-row-only rendering, image-only rocks, removed title/current-player text, player image placement, correct asset paths, bottom alignment and hover CSS, safe/exiting/entering phases, disabled transition clicks, delayed win/loss routing, Japanese display, fallback, and timer cleanup.
* `src/AppMiniGames.test.jsx` now verifies the debug route through the Feature-004 Row 1 instruction and current-player image contract instead of the intentionally removed title/text.
* The final combined River, translation, App integration, shared result page, mini-game state, DebugModal, and GameSetupContext run passes 8 suites and 41 tests.
* The repository-installed ESLint binary passes all Feature-004 implementation and test files.
* `npm run build` compiles successfully with the actual `miniGames/rocks/rock-1.png` through `rock-10.png` imports, and `git diff --check` reports no whitespace errors.

Automated Tests Still Needed

* The broad `src/App.test.jsx` suite was not rerun because its existing `source-map-support` startup crash is already documented; the dedicated App mini-game integration suite passes.
* JSDOM and source contracts cannot judge real animation smoothness, perceived progression, rock transparency/click-area fit, image alignment across intrinsic dimensions, or responsive overlap.

Manual Testing Still Needed

* A browser pass is still needed for image appearance, bottom alignment, hover behavior, safe/unsafe feedback, sequential exit/entry smoothness, bilingual readability, character placement, responsive fit, complete shared return flows, and console output.

---

Feature-005: Add debug flag to reveal safe rocks in River Mini Game

Test Status

* passed

Manual Test Steps And Expected Results

1. Confirm `SHOW_SAFE_RIVER_ROCKS` is set to `true`, then start the River Mini Game.
   Expected: Row 1 shows a clearly visible `SAFE` marker above each of its two safe rocks, while its unsafe rock has no marker.
2. Click a marked safe rock and progress through all three rows.
   Expected: Markers move and fade with the outgoing row; Row 2 shows two safe markers and Row 3 shows one, with no hidden future rows or markers displayed.
3. Hover and click marked safe rocks during normal play.
   Expected: The marker causes no layout jump, does not intercept the click, and the existing safe feedback and row transitions run unchanged.
4. Click an unmarked unsafe rock.
   Expected: The existing unsafe animation and loss text run, then Mini Game Failed opens; the marker does not change the result.
5. Temporarily set `SHOW_SAFE_RIVER_ROCKS` to `false` and reload the River Mini Game.
   Expected: No `SAFE` markers appear, rock images and layout look otherwise unchanged, and safe/unsafe clicks retain their existing behavior.
6. Restore the flag to `true` after the disabled check.
   Expected: Safe markers return and no console errors occur.

Automated Tests Completed

* Three focused regressions failed before implementation because no enabled markers, debug constant file, or row-specific marker updates existed; the six existing River behavior tests remained green.
* `src/pages/MiniGames/RiverMiniGame.test.jsx` passes all nine tests, including two marked/one unmarked Row 1 rocks, disabled-flag hiding, unchanged safe clicks when hidden, 2/2/1 marker updates through transitions, non-interactive marker CSS, safe/unsafe animations, win/loss routes, translations, and cleanup.
* The final combined River logic, translations, River page, App integration, shared result pages, mini-game state, DebugModal, and GameSetupContext run passes 8 suites and 44 tests.
* Source inspection verifies the temporary comment and enabled `SHOW_SAFE_RIVER_ROCKS` constant are isolated in `src/features/miniGames/riverDebug.js`.
* The repository-installed ESLint binary passes the debug constant, River component, and River tests.
* `npm run build` compiles successfully, and `git diff --check` reports no whitespace errors.

Automated Tests Still Needed

* The broad `src/App.test.jsx` suite was not rerun because its existing `source-map-support` startup crash is already documented; the dedicated App mini-game integration suite passes.
* JSDOM cannot visually judge marker contrast, exact image overlap, movement with CSS animations, or real pointer hit-testing.

Manual Testing Still Needed

* A browser pass is still needed for marker visibility, marker/rock alignment, transition movement, hover and click interaction, the temporary disabled-flag appearance, responsive behavior, and console output.

---

Feature-006: Refine River Mini Game result flow, rock sizing, player spacing, and return modal translation

Test Status

* passed

Manual Test Steps And Expected Results

1. Cross all three River rows and wait on the final result state without clicking anything.
   Expected: The final safe animation completes, the win message remains visible, the rocks are replaced by a localized Continue button, and the page does not navigate automatically.
2. Click Continue after winning.
   Expected: The win result is recorded and the shared Loot Chest page opens.
3. Start another attempt, click an unsafe rock, and wait without clicking anything.
   Expected: The unsafe animation and fail message play first; after the animation, the rocks are replaced by Continue and the page remains on the result state.
4. Click Continue after failing.
   Expected: The loss result is recorded and the shared Mini Game Lose page opens.
5. Inspect the River rocks, their hover/selection effects, and the player image on desktop and a narrow viewport.
   Expected: Each rock remains bottom-aligned at a fixed 130px width with automatic height, animations still work, and the player has 40px top margin without overlapping the row or Continue button.
6. Complete a win as an English player, continue through Loot Chest, and return to Gameplay.
   Expected: The same player remains active, the modal says `You crossed the river! You may roll again.`, and Roll Dice is enabled after the centred bottom OK button closes the modal.
7. Repeat the win flow as a Japanese player, then repeat with a missing or invalid language.
   Expected: Japanese shows `川を渡り切りました！もう一度サイコロを振ることができます。`; missing or invalid language falls back to English.
8. Open each normal modal and the dice result modal.
   Expected: Normal modal content fills the body with centred actions at the bottom, while the isolated dice modal layout remains unchanged.
9. Complete a River loss and return from the lose page.
   Expected: The next player becomes active, no roll-again notice appears, and existing board controls remain usable without console errors.

Automated Tests Completed

* Focused regressions failed before implementation for timer-driven win/loss navigation, visible result-state rocks, missing Continue and return-notice translations, old rock/player CSS values, and absent shared modal body/bottom-action layout.
* `src/pages/MiniGames/RiverMiniGame.test.jsx` verifies safe and unsafe animation timing, persistent win/loss states, row replacement, localized Continue controls, click-driven destinations, unchanged safe-marker behavior, exact 130px automatic-height rocks, bottom alignment, hover effects, 40px player margin, and timer cleanup.
* `src/i18n/translations.test.js` verifies exact English/Japanese Continue, result, instruction, and board-return strings plus English fallback for invalid language.
* `src/AppMiniGames.test.jsx` verifies English and Japanese board-return notices, dismissal, the same active player, and enabled Roll Dice behavior.
* `src/components/Modal.test.jsx` verifies normal modal body/action regions, flex-based bottom actions, horizontal centring, multiple actions, and the unchanged isolated dice variant.
* The expanded River, translation, App integration, shared result page, mini-game state, DebugModal, GameSetupContext, and Modal run passes 9 suites and 50 tests.
* The repository-installed ESLint binary passes every changed JavaScript implementation and test file.
* `npm run build` compiles successfully, and `git diff --check` reports no whitespace errors.

Automated Tests Still Needed

* The broad `src/App.test.jsx` suite was not rerun because its existing `source-map-support` startup crash is already documented; the dedicated App mini-game integration suite passes.
* The full `src/pages/GameplayPage.test.jsx` retains six known unrelated stale assertions documented by earlier Epic-010 work; the focused Gameplay return-modal integration passes.
* JSDOM and source contracts cannot measure real image overlap, animation smoothness, modal action position, or responsive fit in a browser.

Manual Testing Still Needed

* A browser pass is still needed for complete click-paced win/loss flows, animation smoothness, rock/player/Continue spacing, responsive fit, bilingual readability, all normal modal action positions, the unchanged dice modal, return-to-board turn continuity, and console output.

---

Bug-004: River roll-again modal text should use correct language fonts

Test Status

* passed

Manual Test Steps And Expected Results

1. Win the River Mini Game as an English player, continue through Loot Chest, and return to Gameplay.
   Expected: The modal displays `You crossed the river! You may roll again.` using the English Unkempt font.
2. Close the English return modal.
   Expected: The centred OK button remains at the bottom of the modal, the same player remains active, and Roll Dice is enabled.
3. Repeat the River win and return flow as a Japanese player.
   Expected: The Japanese return message uses Noto Serif JP rather than Unkempt.
4. Inspect both return messages against the fantasy modal background.
   Expected: The text remains bold, `#F5FA00`, readable, and unchanged in wording; the background and modal spacing remain unchanged.
5. Open other translated normal modals and the dice result modal.
   Expected: Their existing language fonts and layouts are unchanged, and no console errors occur.

Automated Tests Completed

* The focused App integration reproduction failed before implementation because both the English and Japanese River return message elements had no language class; the existing global Unkempt and Noto Serif JP declarations already passed.
* `src/AppMiniGames.test.jsx` now verifies the exact English message has `language-en`, the exact Japanese message has `language-jp`, and those established classes map to Unkempt and Noto Serif JP respectively.
* The surrounding App integration, shared Modal, translations, and mini-game state run passes 4 suites and 19 tests, including modal dismissal, bottom/centred actions, same-player wins, and enabled roll-again behavior.
* The repository-installed ESLint binary passes `src/pages/GameplayPage.jsx` and `src/AppMiniGames.test.jsx`.
* `npm run build` compiles successfully.

Automated Tests Still Needed

* JSDOM can verify applied class names and CSS declarations but cannot confirm that the remotely imported font files load or visually render in a browser.

Manual Testing Still Needed

* A browser pass is still needed to visually confirm Unkempt and Noto Serif JP rendering, inherited bold/yellow modal text, the bottom OK button position, unchanged translated modals, roll-again interaction, and console output.

---

Feature-007: Add larger-text class to text-only modal paragraphs

Test Status

* passed

Manual Test Steps And Expected Results

1. Open Spells for a player with unsaved changes and select Cancel.
   Expected: The Yes/No cancel confirmation displays its main message with larger text and the current language font.
2. Open Spells with valid changes and select Save.
   Expected: The Yes/No save confirmation retains larger text and the current language font.
3. Win the River Mini Game as an English player, continue through Loot Chest, and return to Gameplay.
   Expected: `You crossed the river! You may roll again.` uses larger text plus the English Unkempt font.
4. Repeat the River return flow as a Japanese player.
   Expected: The Japanese message uses larger text plus Noto Serif JP, without losing its translation or language class.
5. Inspect each text-only modal's message and actions.
   Expected: Text remains bold and yellow without overflow; Yes/No or OK actions remain centred at the bottom of the modal.
6. Open Settings, Debug, Spells Token Assignment, turn-change, battle-turn, battle-loss, and dice result modals.
   Expected: Their complex body content, controls, images, lists, token assignment, and dice layout do not receive `larger-text` or otherwise change.

Automated Tests Completed

* Regressions failed before implementation because Gameplay contained only the already-compliant save confirmation; cancel confirmation and both English/Japanese River return messages lacked `larger-text`.
* `src/components/Modal.test.jsx` passes all six tests, including exactly three approved `larger-text` uses, the existing `2em` class rule, no use in current settings/battle/debug/spell-assignment sources, bottom-centred normal actions, and the isolated dice variant.
* `src/AppMiniGames.test.jsx` passes all four tests and verifies the English and Japanese River return messages each retain their language class while adding `larger-text`, modal dismissal, and enabled Roll Dice behavior.
* The focused App and shared Modal run passes 2 suites and 10 tests.
* The save-confirmation behavior and new class assertion pass within `src/pages/GameplayPage.test.jsx`'s established 8-of-14 baseline.
* The surrounding DebugModal suite passes all tests and the SpellsModal source exclusion passes; no complex modal implementation file was changed.
* The repository-installed ESLint binary passes all Feature-007 implementation and test files with no warnings.
* `npm run build` compiles successfully, and `git diff --check` reports no whitespace errors.

Automated Tests Still Needed

* Six pre-existing `src/pages/GameplayPage.test.jsx` assertions remain stale outside this feature: three expect the former shorter starting warning, one expects old dice-result copy, one expects former current-player alignment, and one expects the former 100px Spells player image.
* Two pre-existing `src/features/spells/SpellsModal.test.jsx` assertions expect the former shorter English/Japanese starting-warning copy; the remaining tests pass and the Feature-007 source exclusion is verified separately.
* The broad `src/App.test.jsx` suite was not rerun because its existing `source-map-support` startup crash is already documented; dedicated App mini-game integration passes.
* JSDOM and source contracts cannot visually measure paragraph size, line wrapping, overflow, actual font rendering, or action position in a browser.

Manual Testing Still Needed

* A browser pass is still needed for English/Japanese text size and wrapping, bold/yellow inheritance, bottom action placement, River return interaction, complex-modal exclusions, responsive overflow, and console output.

---

Feature-008: Remove River Mini Game safe rock debug indicators

Test Status

* passed

Manual Test Steps And Expected Results

1. Start several new River Mini Game attempts and inspect Row 1 before selecting a rock.
   Expected: Three normal rock images appear with no `SAFE` labels, green markers, debug outlines, or other status clues.
2. Select a known safe rock and progress through Row 2 and Row 3.
   Expected: Safe status remains hidden before each choice; the selected safe rock still receives its normal feedback and each row transition still completes.
3. Complete all three rows safely and wait on the result state.
   Expected: The win message and Continue button appear without any debug indicator, and Continue still opens Loot Chest.
4. Start another attempt and select an unsafe rock.
   Expected: No status is revealed before the click; the unsafe animation and fail message still run, and Continue still opens Mini Game Lose.
5. Restart the River Mini Game several times.
   Expected: Safe choices and rock images can vary between attempts but remain fixed during each attempt; Row 1 and Row 2 retain two safe rocks and Row 3 retains one.
6. Inspect the browser console while playing both paths.
   Expected: There are no missing-module, unused-import, render, timer, or navigation errors.

Automated Tests Completed

* Four hidden-choice regressions failed before implementation because the enabled flag module, `SAFE` spans, marker CSS, and visible 2/2/1 row markers still existed; five existing River flow tests remained green.
* `src/pages/MiniGames/RiverMiniGame.test.jsx` passes all nine tests for no pre-selection status classes or text, hidden status across all rows, removed debug import/module/UI/CSS, safe progression, unsafe failure, safe/unsafe animations, localized result controls, win/loss destinations, and timer cleanup.
* `src/features/miniGames/riverMiniGame.test.js` continues to verify unchanged three-row generation, exact 2/2/1 safe counts, randomized safe positions, fixed row data, and rock image selection.
* The expanded River, translation, App integration, shared result page, mini-game state, DebugModal, GameSetupContext, and Modal run passes 9 suites and 52 tests.
* Production-source inspection finds no remaining `SHOW_SAFE_RIVER_ROCKS`, `riverDebug`, `river-rock-safe-debug-flag`, or rendered `SAFE` marker references, and confirms `src/features/miniGames/riverDebug.js` no longer exists.
* The repository-installed ESLint binary passes the River component and tests with no warnings.
* `npm run build` compiles successfully after the debug module deletion, and `git diff --check` reports no whitespace errors.

Automated Tests Still Needed

* The broad `src/App.test.jsx` suite was not rerun because its existing `source-map-support` startup crash is already documented; the dedicated App mini-game integration suite passes.
* JSDOM and source contracts cannot visually compare every rock image for accidental clues or judge real animation smoothness and responsive overlap.

Manual Testing Still Needed

* A browser pass is still needed to confirm safe and unsafe rocks are visually indistinguishable before selection across randomized attempts, both animation/result paths remain smooth, responsive layout is unchanged, and the console stays clean.

---

Bug-005: Next-turn modal must always appear after any player turn change

Test Status

* passed

Manual Test Steps And Expected Results

1. Complete normal board movement in a two-player game.
   Expected: The active player changes and the next-turn modal immediately identifies the new player before Roll Dice, Spells, movement, Settings, or other non-critical actions are available.
2. Dismiss the next-turn modal and continue interacting with Gameplay.
   Expected: The modal closes once, the new player's controls become available, and the same modal does not reopen until another player-to-player turn change occurs.
3. Lose the River Mini Game and select Return to Board on the shared Mini Game Lose page.
   Expected: Gameplay opens with the next player active and their localized next-turn modal shown first; no mini-game result modal is shown.
4. Win the River Mini Game and continue through Loot Chest.
   Expected: The same player remains active, the River roll-again notice appears, and no next-turn modal is queued.
5. Finish a battle through both the Respawn loss flow and the reward Continue flow.
   Expected: Each flow advances to the next player with a pending next-turn modal before board interaction.
6. Advance to a player who has not committed their initial spells.
   Expected: The next-turn modal appears before forced Spells setup; dismissing it opens the required Spells modal.
7. Repeat turn changes for English, Japanese, and invalid-language players.
   Expected: The existing translated colour/turn wording and language font class use the new player's language, with invalid values falling back to English.

Automated Tests Completed

* The provider and River-loss regressions failed before implementation because the active player changed to `player-2` while next-turn modal state remained clear; nine surrounding tests still passed.
* `src/features/gameSetup/GameSetupContext.test.jsx` verifies centralized `advanceTurn` state, the correct new player, pending modal state, and one explicit dismissal.
* `src/features/miniGames/miniGameState.test.jsx` verifies River losses queue the modal while River wins preserve the same player, roll-again notice, and clear next-turn state.
* `src/AppMiniGames.test.jsx` verifies the shared Mini Game Lose return reaches Gameplay with the Japanese new player's localized modal, disabled Roll Dice, Spells, and Settings actions, one dismissal, and re-enabled controls; all five App mini-game integration tests pass.
* `src/pages/GameplayPage.test.jsx` verifies a pending turn modal has priority over forced initial Spells setup; its focused regression passes.
* Focused real-provider regressions in `src/pages/BattlePage.test.jsx` and `src/pages/RewardPage.test.jsx` verify battle Respawn and reward Continue each advance to `player-2` with next-turn modal state pending.
* The final App mini-game, provider, mini-game state, and Reward run passes 4 suites and 33 tests; the two focused battle/reward regressions also pass.
* The repository-installed ESLint binary passes every touched implementation and test file except the Battle test's existing direct-node-access violations; none of those violations are on the Bug-005 snapshot changes.
* `npm run build` compiles successfully, and `git diff --check` reports no whitespace errors.

Automated Tests Still Needed

* The expanded `src/pages/GameplayPage.test.jsx` run retains six pre-existing stale assertions: three old starting-warning copies, one old dice-result copy, one former current-player alignment, and one former 100px Spells player image; 14 tests pass, including Bug-005.
* The full `src/pages/BattlePage.test.jsx` run retains three unrelated stale label/dice-copy assertions; the remaining Battle tests pass, including the focused Respawn pending-modal regression.
* The full Battle test file also retains 30 pre-existing `testing-library/no-node-access` lint errors outside the Bug-005 lines.
* The broad `src/App.test.jsx` suite was not rerun because its existing `source-map-support` startup crash is already documented; dedicated App integration passes.
* Source review found no current debug control that directly advances a board turn; current battle, reward, movement, and mini-game turn-ending paths are covered through the centralized provider transition.

Manual Testing Still Needed

* A browser pass is still needed to confirm modal visual priority and focus, disabled interaction through overlays, English/Japanese rendering, repeated turn transitions without duplicates, battle/reward/mini-game routing, responsive behavior, and console output.

---

Feature-009: Add uncommitted token notification to Spells button

Test Status

* passed

Manual Test Steps And Expected Results

1. Start a committed player's turn with an empty token bag and inspect the gameplay Spells button.
   Expected: The button appears normally with no `!` notification; tokens already committed inside spell slots do not trigger one.
2. Add one token, then multiple tokens, to the current player's token bag.
   Expected: One small `!` badge appears at the top-right of the Spells button and overlaps its edge slightly regardless of the bag count.
3. Click the Spells button while the badge is visible.
   Expected: The existing Spells / Token Assignment modal opens normally and the badge does not intercept the click.
4. Commit the final bag token into a spell slot, save, and return to Gameplay.
   Expected: The badge disappears because the current player's token bag is empty; committed spell-slot tokens are not counted.
5. Gain another bag token, then discard or remove it.
   Expected: The badge appears immediately when the token enters the bag and disappears immediately when the bag becomes empty.
6. End a turn from a player with bag tokens to a player with an empty bag, then cycle back.
   Expected: The badge hides for the empty-bag player and reappears for the player whose bag still contains tokens.
7. Inspect other Spells headings and labels.
   Expected: No notification is added outside the gameplay Spells button, and there are no console errors.

Automated Tests Completed

* All three initial focused regressions failed before implementation because the badge markup, current-player condition, wrapper, and positioning styles did not exist.
* `src/pages/GameplayPage.test.jsx` passes four focused Feature-009 regressions covering empty, one-token, and multiple-token bags; committed-token exclusion; both turn directions; unchanged Spells modal opening; live commit/gain/removal updates; one badge only; and required absolute top-right click-through styles.
* The full Gameplay run retains its established six unrelated stale assertions while 13 tests pass, including all four Feature-009 tests.
* `src/AppMiniGames.test.jsx` passes all five integration tests, confirming the wrapped Spells button does not regress River routing, localized return notices, or pending next-turn interaction locks.
* The repository-installed ESLint binary passes `src/pages/GameplayPage.jsx` and `src/pages/GameplayPage.test.jsx` with no warnings.
* `npm run build` compiles successfully, and `git diff --check` reports no whitespace errors.

Automated Tests Still Needed

* The six pre-existing `src/pages/GameplayPage.test.jsx` failures remain outside this feature: three old starting-warning copies, one old dice-result copy, one former current-player alignment, and one former 100px Spells player image.
* JSDOM and CSS source checks cannot visually judge the badge's readability, overlap, or exact placement across browser zoom and responsive sizes.

Manual Testing Still Needed

* A browser pass is still needed to visually confirm the top-right overlap, readability against the fantasy button, click behavior at the badge edge, live updates after real drag-and-save assignment and reward/debug token changes, responsive placement, and console output.

---

Feature-010: Add Cave Mini Game with debug trigger, reward tracking, retreat flow, and roll-again reward

Test Status

* passed

Manual Test Steps And Expected Results

1. Open Settings, enter Debug, and select Start Cave Mini Game for an English player.
   Expected: The standalone Cave page opens for that player with `cave.png` covering the screen, readable instruction text, and side-by-side Go Deeper and Retreat buttons.
2. Repeat with a Japanese player and with a player whose language value is invalid.
   Expected: Every instruction, outcome, summary, button, and return notice is Japanese for the Japanese player; invalid language falls back to English and the existing language font classes apply.
3. Start fresh Cave runs and repeatedly select Go Deeper.
   Expected: Each click uses the current numbered probability row, displays one translated outcome, advances after non-ogre outcomes, and Step 16 always produces an ogre.
4. Obtain token, loot, potion, and roll-again outcomes in separate or repeated runs.
   Expected: Each reward type can be found only once per run; on later steps its former chance moves into ogre chance while Nothing and other available chances remain unchanged.
5. Retreat before finding any rewards.
   Expected: The success text, `No rewards found.` summary, and Continue button replace the active controls; Continue returns to Gameplay, advances to the next player, and shows the next-turn modal first.
6. Retreat after finding token, loot, and normal potion outcomes but no roll-again reward.
   Expected: The summary lists each found reward once, but no token, loot chest, or potion is added to player inventory; Continue advances the turn and queues the next-turn modal.
7. Retreat after finding the roll-again potion.
   Expected: The summary includes Roll Again Potion; Continue keeps the same player, shows the localized larger-text roll-again modal, and allows Roll Dice after OK is selected.
8. Find one or more rewards and then encounter an ogre.
   Expected: The ogre message replaces the active controls, all Cave rewards are discarded, and Continue opens the shared Mini Game Lose page.
9. Select Return to Board after the Cave ogre loss.
   Expected: The next player becomes active with the next-turn modal pending; no Cave reward or roll-again notice is granted.
10. Play existing River win and loss flows after Cave testing.
    Expected: River routes, animations, shared result pages, same-player win notice, and next-player loss behavior remain unchanged, with no console errors.

Automated Tests Completed

* Initial Cave regressions could not load because the probability engine and page did not yet exist; the page harness also exposed and corrected one Jest mock-hoisting name before implementation.
* `src/features/miniGames/caveMiniGame.test.js` verifies all exact 16 probability rows, 100-percent totals, deterministic cumulative outcomes, Step 16's forced ogre, and movement of acquired loot/roll-again chances into ogre chance using the documented Step 7 and Step 8 examples.
* `src/pages/MiniGames/CaveMiniGame.test.jsx` verifies English/Japanese/fallback text, required cave background and flex actions, all four one-time reward summary entries, no-reward retreat, roll/no-roll completion options, control replacement, ogre reward clearing, readable Continue state, shared loss navigation, and Gameplay return.
* `src/features/miniGames/miniGameState.test.jsx` verifies Cave retreat without roll again advances with pending next-turn state, Cave roll again preserves the player with a return notice, and existing River state behavior remains unchanged.
* `src/AppMiniGames.test.jsx` passes debug Cave routing, retreat-to-next-player modal priority, same-player roll-again return, ogre loss after acquiring roll again, Japanese Cave notice, and all existing River integrations.
* `src/i18n/translations.test.js` verifies every required English and Japanese Cave message and English fallback; `src/features/debug/DebugModal.test.jsx` verifies the new current-player Cave trigger.
* The final Cave, River, App, context, translation, debug, and shared result run passes 9 suites and 57 tests; the final focused Cave engine/page rerun passes 2 suites and 9 tests.
* The repository-installed ESLint binary passes all Feature-010 implementation and test files with no warnings.
* `npm run build` compiles successfully against the final code, and `git diff --check` reports no whitespace errors.

Automated Tests Still Needed

* The broad `src/App.test.jsx` suite was not rerun because its existing `source-map-support` startup crash remains outside this feature; dedicated App mini-game integration passes ten tests.
* The full `src/pages/GameplayPage.test.jsx` suite retains six unrelated stale assertions documented under Feature-009 and Bug-005; Cave return behavior is covered in dedicated App/context tests.
* Randomized tests prove probability boundaries deterministically but cannot statistically certify browser randomness over a large sample.

Manual Testing Still Needed

* A browser pass is still needed for cave background crop, readable text-box contrast, side-by-side button layout, Japanese rendering, repeated real random runs, all summary combinations, modal focus/position, responsive behavior, inventory non-granting, and console output.

---

EPIC-010 Completion Regression

Test Status

* passed

Manual Regression Flow And Expected Results

1. Start Gameplay and inspect the board/sidebar atmosphere.
   Expected: The magical night sky remains decorative and non-interactive, with its established star/effect behavior and no board obstruction.
2. Inspect Potions and Spells for players with empty and populated inventories, then change turns.
   Expected: Potion capacity/display styling remains correct and the Spells `!` badge follows only the active player's uncommitted token bag.
3. Play River through safe win and unsafe loss paths.
   Expected: Safe rocks remain hidden before selection; win preserves the player with the localized roll-again notice and loss advances through the shared loss page with a next-turn modal.
4. Play Cave through no-reward retreat, reward retreat, roll-again retreat, and ogre loss.
   Expected: Probability/reward tracking, summaries, shared loss routing, same-player reward, and next-player transitions match Feature-010 without granting out-of-scope rewards.
5. Exercise normal movement, battle Respawn, reward Continue, and mini-game loss turn changes.
   Expected: Every player-to-player change shows exactly one localized next-turn modal before gameplay actions; same-player continuations use their own result notice.
6. Open all text-only and complex modal examples used in Epic-010.
   Expected: Approved text-only messages retain `larger-text`; Settings, Debug, spell assignment, battle, dice, and other complex modal content remains unchanged.

Relevant Automated Regression Completed

* The final mini-game completion run passes 9 suites and 57 tests across App integration, Cave and River pages/engines, mini-game state, shared result pages, translations, and DebugModal.
* The remaining MagicalNightSky, PotionList, and shared Modal completion-gate run passes 3 suites and 20 tests.
* Combined relevant Epic-010 completion coverage passes 12 suites and 77 tests, plus focused Gameplay Feature-009 and Bug-005 regressions already recorded above.
* All implementation/test files touched by Features 009-010 and Bug-005 pass their scoped ESLint checks; the final production build succeeds and whitespace checks pass.

Remaining Epic Gaps And Next Actions

* Run the six-step manual regression above in a real desktop and mobile browser, including visual/randomness/focus checks and console inspection.
* Resolve the pre-existing broad App startup crash and stale Gameplay/Battle assertions in separately requested bug work; they do not invalidate the dedicated Epic-010 coverage.

---

Feature-011: Refine Cave Mini Game movement, retreat timing, ogre chase animation, and reward display

Test Status

* passed

Manual Test Steps And Expected Results

1. Start Cave from Debug and inspect the first decision screen.
   Expected: The current player's colour/gender character appears at the left above the text box, Go Deeper is available, and Retreat is hidden.
2. Select Go Deeper once and try both controls during the movement.
   Expected: The character moves one of 16 positions to the right over 0.5 seconds, Go Deeper cannot calculate twice, both controls remain disabled during movement, and Retreat becomes usable after movement finishes.
3. Continue selecting Go Deeper through later depths.
   Expected: Each resolved decision advances exactly one position, the player approaches the right edge at depth 16 without a visible depth 17, and the existing probability row for each depth remains in use.
4. Find each available reward during one or more runs.
   Expected: A simple Found rewards area appears below the buttons after the first reward, updates with Token, Loot Chest, Potion, and Roll Again Potion at most once each, and uses the current player's English or Japanese labels with English fallback.
5. Retreat after movement with and without rewards, including a roll-again reward.
   Expected: The live display is replaced by the existing final summary and Continue flow; no-roll retreat advances to the next-player modal while roll-again retreat preserves the current player and shows its localized notice.
6. Encounter an ogre after finding a reward.
   Expected: The reward display clears, the player flips, `AO.png` appears to the player's right, both images chase left and fade for 1.5 seconds, and controls remain disabled.
7. Observe the route during and after the chase, then return to the board.
   Expected: The Cave page remains visible until the full chase finishes, then opens the shared Mini Game Lose page; Return to Board advances to the next player and shows the next-turn modal without granting Cave rewards.
8. Repeat the flow at desktop and mobile sizes and inspect the browser console.
   Expected: The progress images stay above and clear of the text box, the final position remains within its width, labels stay readable, and no console errors appear.

Automated Tests Completed

* Before implementation, the focused Cave page regression had 6 expected failures and 1 existing pass because first-step hiding, player progress, movement locks, live rewards, and ogre chase behavior were not present.
* `src/pages/MiniGames/CaveMiniGame.test.jsx` passes 7 tests covering depth zero, hidden first-step Retreat, current-player image resolution, exact 500ms interaction lock, double-click prevention, all 16 engine steps without a seventeenth position, live unique rewards, English/Japanese/fallback labels, preserved retreat summaries, and the delayed 1500ms ogre chase/loss route.
* The focused Cave and shared mini-game regression run passes 5 suites and 27 tests across the Cave page/engine, App routing, context return state, and shared result pages.
* The final Cave page rerun passes 1 suite and 7 tests after lint cleanup.
* The installed ESLint binary passes the Cave component, Cave tests, and updated App mini-game integration tests with no warnings or errors.
* `npm run build` compiles the final implementation successfully.

Automated Tests Still Needed

* JSDOM verifies classes, timing state, image sources, depth values, and navigation order but cannot visually prove sprite direction, smoothness, exact edge alignment, fade quality, or readability against the real cave image.

Manual Testing Still Needed

* Run the eight-step browser flow above to confirm real animation smoothness, right-facing normal sprite interpretation, chase clarity, responsive alignment, Japanese rendering, and console output.

---

Bug-006: Cave Mini Game player sprite facing direction is reversed

Test Status

* passed

Manual Regression Steps And Expected Results

1. Start the Cave Mini Game and inspect the current-player sprite before selecting anything.
   Expected: The inner player image is horizontally flipped so the character faces deeper into the cave, while the progress wrapper remains at depth zero.
2. Select Go Deeper and watch the entire 500ms movement.
   Expected: The player stays flipped before, during, and after movement; horizontal progress remains smooth because movement belongs to the outer wrapper and facing belongs to the nested image.
3. Trigger an ogre encounter.
   Expected: The player becomes unflipped, `AO.png` remains to the player's right, and both images continue moving left and fading for the existing 1500ms chase.
4. Wait for the chase and return from the shared Mini Game Lose page.
   Expected: Navigation does not happen before the chase finishes, then the existing loss route advances to the next player and shows the next-turn modal without console errors.

Automated Tests Completed

* Before the fix, the focused Cave regression reproduced Bug-006 with 3 failures: the initial and moving sprite lacked the required flipped class, while the chase still used the reversed `is-chased` flip.
* After the fix, `src/pages/MiniGames/CaveMiniGame.test.jsx` passes 7 tests, including flipped initial/moving states, explicit unflipped chase state, separate outer movement and inner facing elements, ogre placement, fade timing, and delayed loss navigation.
* The combined Cave/App regression passes 2 suites and 17 tests, confirming the shared loss page, reward discard, next-player transition, and other Cave flows remain intact.
* The installed ESLint binary passes the Cave component and test with no warnings or errors.
* `npm run build` compiles successfully with the fixed sprite classes.

Automated Tests Still Needed

* DOM and CSS assertions prove the transform states and separation but cannot visually determine whether every character artwork reads as facing the intended direction.

Manual Testing Still Needed

* Run the four-step browser regression with representative male and female character sprites to visually confirm exploration and chase directions and inspect console output.

---

Feature-012: Add Cave Mini Game reward generation, retreat reward handling, loot routing, and roll-again preservation

Test Status

* passed

Manual Test Steps And Expected Results

1. Start Cave and continue until a token is found, then inspect the live reward list before retreating.
   Expected: One exact token object is generated immediately from centralized token data, with a 90% Common/10% Rare rarity choice, and its localized token name remains fixed for the run.
2. Start or continue a Cave run until a normal potion is found.
   Expected: One exact potion object is generated immediately from centralized potion data, with an 80% Common/20% Rare rarity choice, and its localized potion name remains fixed for the run.
3. Find repeated reward outcomes in a long run.
   Expected: Token, potion, loot chest, and roll-again potion each appear at most once; acquired chances still move to ogre chance and generated token/potion objects are not regenerated.
4. Retreat without finding a loot chest, first without and then with roll again.
   Expected: `You got out with your…`, the exact found reward list, and Continue appear; Continue advances to the next-turn modal without roll again, or preserves the same player and shows the roll-again modal when found.
5. Find a loot chest without roll again, retreat, and select Open Loot.
   Expected: The existing shared Loot Chest page opens without calculating loot in Cave; Continue from that page returns to Gameplay, advances the turn, and shows the next-turn modal.
6. Find both a loot chest and roll-again potion, retreat, open loot, and continue.
   Expected: Cave source, player, actual rewards, and same-player return behavior survive through the shared Loot Chest page; Gameplay returns to the same player with the roll-again modal and no next-turn modal.
7. Find a token or potion and then encounter an ogre.
   Expected: All local generated rewards are cleared, the existing chase and Mini Game Lose route play unchanged, and returning advances to the next player without a Cave roll-again notice or granted reward.
8. Repeat the retreat and loot flows for Japanese and an invalid language value.
   Expected: Exit copy, exact reward names, Roll Again Potion, Loot Chest, Continue/Open Loot, and the roll-again modal use Japanese for Japanese players; invalid language falls back to English and language font classes remain correct.
9. Inspect player token bags and potion collections before and after Cave outcomes.
   Expected: This feature preserves actual generated reward data for the Cave transaction but does not directly grant token/potion inventory or calculate loot chest contents, as allowed by scope.
10. Repeat representative flows on desktop and mobile while checking the console.
    Expected: The reward list remains readable within the existing Cave layout and no console errors occur.

Automated Tests Completed

* Before implementation, the focused Feature-012 run had 12 expected failures and 28 existing passes across missing generators/state, exact item display, metadata, copy, and Open Loot routes.
* `src/features/miniGames/caveMiniGame.test.js` verifies the empty actual-reward state and exact 10% Rare token and 20% Rare potion boundaries while selecting complete objects through the centralized reward item helper; all existing 16-step probability tests remain green.
* `src/pages/MiniGames/CaveMiniGame.test.jsx` verifies immediate fixed token/potion generation, duplicate prevention, localized exact item names, no-reward handling, full retreat metadata, Continue versus Open Loot, Japanese Open Loot/font class, and reward clearing on ogre.
* `src/features/miniGames/miniGameState.test.jsx` verifies Cave reward metadata is stored in `miniGameResult`, same-player/next-player return behavior remains centralized, and the new exact roll-again notice is created only for a successful roll-again retreat.
* `src/AppMiniGames.test.jsx` verifies loot-only retreat advances after the shared Loot Chest page, loot plus roll again preserves the same player through that page, direct retreat behavior remains correct, the text-only modal retains `larger-text` and language classes, and ogre loss grants no notice.
* `src/i18n/translations.test.js` verifies all new exact English/Japanese Cave exit, Open Loot, reward label, and roll-again modal text plus English fallback.
* The focused Feature-012 run passes 5 suites and 40 tests; the reused reward generator, battle reward, and shared mini-game result regression run passes 3 suites and 40 tests, for 8 suites and 80 tests total.
* The installed ESLint binary passes all Feature-012 implementation and test files with no warnings or errors.
* `npm run build` compiles successfully, source audit finds no Cave inventory-grant/loot-calculation call, and `git diff --check` reports no whitespace errors.

Automated Tests Still Needed

* Deterministic boundary tests prove rarity selection and eligible centralized objects but do not statistically certify browser randomness over a large sample.
* Inventory granting and loot chest calculation need separate feature coverage when those explicitly out-of-scope flows are implemented.

Manual Testing Still Needed

* Run the ten-step browser flow above to inspect real randomized item variety, Japanese fonts, reward-list readability, modal positioning/focus, responsive behavior, unchanged inventories, and console output.

---

Feature-013: Build Mini Game Failed screen with punishment health loss and respawn handling

Test Status

* passed

Manual Test Steps And Expected Results

1. Lose either River or Cave and wait for the shared failure route.
   Expected: The full page uses the existing animated magical night sky and a centred wooden modal-style panel; it does not repeat that the mini game was lost.
2. Inspect the panel before one second has elapsed.
   Expected: The failing player's character and pre-damage health bar appear at the top centre, the weighted punishment text appears 30px below, and no Continue or Respawn action is usable.
3. Repeat failures enough to exercise the punishment values.
   Expected: Each visit selects exactly one value from 10, 15, 20, 25, 30, 35, 40, 45, or 50 using weights 20, 18, 16, 12, 10, 9, 7, 5, and 3 respectively; rerenders do not change that visit's value.
4. Wait one second when the player has more health than the selected loss.
   Expected: Shared player health is reduced once, the existing health bar animates to the clamped result, and Continue appears only after the update.
5. Select Continue.
   Expected: Gameplay opens on the next player with the existing next-turn modal shown first and board actions locked until it is dismissed.
6. Repeat with current health equal to or below the selected loss and at least one removable committed token.
   Expected: Health stops at zero, one token is removed through the existing mini-game death penalty, the removal result is shown, and Respawn appears instead of Continue.
7. Select Respawn.
   Expected: The failing player returns to the first Start Area position at full health without the removed token; Gameplay opens on the next player with the next-turn modal pending.
8. Repeat survivor and death flows for an English player, a Japanese player, and an invalid language value.
   Expected: Punishment, Continue, Respawn, and death-result text use English or Japanese with English fallback; the panel uses `language-en`/Unkempt or `language-jp`/Noto Serif Japanese.
9. Check the failure panel on desktop and mobile while watching the health transition and browser console.
   Expected: The wooden background remains fitted, bold `#F5FA00` text and centred controls remain readable, the player/health area fits, the sky stays behind the panel, and no console errors appear.

Automated Tests Completed

* Before implementation, the focused run failed because the weighted selector did not exist and the placeholder page lacked the sky, player/health display, timing, translations, actions, and wooden panel contract.
* `src/features/miniGames/miniGamePunishment.test.js` verifies the exact nine-value table, total weighted boundaries, and every transition from random values 0 through 0.99999.
* `src/pages/MiniGames/MiniGameResultPages.test.jsx` verifies one-time generation, pre-damage health, the full one-second lock, clamping, animated HealthBar state change, Continue/Respawn branching, English/Japanese content, player image, reused sky, Start Area respawn call, and requested panel CSS contracts.
* `src/features/miniGames/miniGameState.test.jsx` verifies atomic health mutation, one-token EPIC-007 mini-game death punishment, duplicate-application prevention, persisted removal, full-health Start Area respawn, next-player advancement, and pending next-turn state with the real provider.
* `src/AppMiniGames.test.jsx` verifies Cave and River losses enter the new delayed punishment flow and still return through the existing next-turn modal invariant.
* Translation and death-result tests verify exact English/Japanese punishment and action text, English fallback, localized token-removal results, and unchanged default battle death-result copy.
* The final combined Feature-013 regression passes 8 suites and 63 tests across punishment logic, failure/result pages, provider mini-game state, App routing, translations, death results, HealthBar, and MagicalNightSky.
* The repository-installed ESLint binary passes all Feature-013 implementation and test files with no warnings or errors; `npm run build` compiles successfully and `git diff --check` reports no whitespace errors.

Automated Tests Still Needed

* Deterministic boundary tests prove the specified weighted mapping but cannot statistically certify browser randomness over a large sample.
* JSDOM verifies timer state and HealthBar width values but cannot visually certify the real 300ms bar transition, wooden image crop, star animation, or responsive font rendering.

Manual Testing Still Needed

* Run the nine-step browser flow above for real animation quality, panel readability, Japanese font rendering, representative random visits, responsive layout, and console inspection.

---

Feature-014: Grant Cave Game token and potion rewards, handle full bags/slots, and add Cave reward debug buttons

Test Status

* passed

Manual Test Steps And Expected Results

1. Start Cave and inspect the top-left debug controls before moving.
   Expected: Plain Token, Potion, Loot Chest, and Roll Again Potion debug buttons are absolutely positioned over the page without moving the Cave layout.
2. Select each debug reward once in one Cave run.
   Expected: The matching exact reward appears in Found rewards, each selected button disables, different reward buttons remain available until used, and token/potion use the normal Cave generators.
3. Start another run and find each reward naturally.
   Expected: The matching debug button disables as soon as its reward is found, and neither natural nor debug input can add that reward type twice.
4. Retreat with generated token and potion rewards while the token bag and potion collection have room.
   Expected: The exact token is added uncommitted to the token bag and the exact potion is added to potion slots; capacities remain at no more than five and three.
5. Retreat with a token when the token bag already contains five items.
   Expected: A localized shared wooden modal displays the new Token icon, all existing bag Token icons, replacement actions, and a discard-new-token action; Continue/Open Loot remains disabled until one choice is applied.
6. Exercise both full-token choices in separate runs.
   Expected: Replacing keeps exactly five bag tokens and preserves the generated token data, while discarding leaves the original bag unchanged; the new token is never silently lost.
7. Retreat with a potion when all three potion slots are occupied, then exercise each choice in separate runs.
   Expected: The modal uses PotionIcon for the new and existing potions; replacement keeps exactly three potions with the new potion in the chosen slot, while discard leaves all three originals unchanged.
8. Retreat with token, potion, loot chest, and roll again while both inventories are full.
   Expected: Token choice appears first, potion choice second, Loot Chest cannot open before both resolve, then the existing Loot Chest page opens and Continue returns to the same player with the roll-again notice.
9. Repeat a successful no-loot retreat without roll again and an ogre loss after finding rewards.
   Expected: A resolved retreat returns through the existing next-player modal; ogre loss clears local Cave rewards and grants no token, potion, loot, or roll-again result.
10. Repeat full-capacity choices for English, Japanese, and invalid player languages.
    Expected: Titles, instructions, item names, replace/discard actions, and fonts use English or Japanese with English fallback; icon/list choice modals do not use `larger-text`.
11. Repeat representative flows at desktop and mobile sizes while checking the console.
    Expected: Debug controls stay top-left, modal choices remain usable, icons/tooltips remain readable, and no console errors appear.

Automated Tests Completed

* Before implementation, the focused run failed because the Cave transaction helper, choice modal, and four debug reward controls did not exist.
* `src/features/miniGames/caveRewardGrant.test.js` verifies automatic exact token/potion grants, five-token and three-potion limits, token-first pending order, token/potion replacement, both discard-new choices, status completion, and unchanged inventory on discard.
* `src/pages/MiniGames/CaveRewardChoiceModal.test.jsx` verifies Token and PotionIcon reuse, new/current reward display, English/Japanese labels and font classes, replacement/discard callbacks, and absence of `larger-text` on both choice modals.
* `src/pages/MiniGames/CaveMiniGame.test.jsx` verifies all four debug buttons, one-use disabling from debug and natural rewards, same generator reuse, all-reward summaries/retreat metadata, and the requested absolute top-left CSS contract without changing movement or ogre behavior.
* `src/features/miniGames/miniGameState.test.jsx` verifies automatic provider grants, persisted exact Cave metadata, blocked premature return, ordered full-capacity decisions, replacement state, inventory capacities, and unchanged same-player/next-player return behavior.
* `src/AppMiniGames.test.jsx` verifies the real full-token/full-potion modal sequence blocks Open Loot, resolves token before potion, preserves Loot Chest routing, and keeps roll again through the shared Loot Chest page.
* Translation tests verify the new English/Japanese full-capacity copy and functions with existing English fallback; reused token-bag, potion-capacity, battle reward, Cave engine, and reward context/page suites remain green.
* The final combined Feature-014 run passes 11 suites and 86 tests. The repository-installed ESLint binary passes every Feature-014 implementation/test file, `npm run build` compiles successfully, and `git diff --check` reports no whitespace errors.

Automated Tests Still Needed

* JSDOM verifies position declarations, modal order, disabled states, inventory values, routing, and language classes but cannot visually certify icon tooltip placement or top-left overlap at every viewport.
* Deterministic Cave generator tests prove eligible exact rewards and probability boundaries but cannot statistically certify long-run browser randomness.

Manual Testing Still Needed

* Run the eleven-step browser flow above for real pointer/keyboard interaction, modal readability, Japanese font rendering, responsive debug placement, tooltip behavior, representative random rewards, and console inspection.

---

Bug-007: Replace token and potion modal lists with div-based grid/flex layouts

Test Status

* passed

Manual Regression Steps And Expected Results

1. Fill a player's token bag, earn a Cave token reward, and retreat.
   Expected: The full-token modal shows the current bag in a labeled div-based flex group with no `ul` or `li` elements.
2. Select each existing token replacement option and repeat using Discard new token.
   Expected: Every button remains keyboard-accessible, replacement receives the selected token ID, discard keeps the existing bag, and Token icons/names remain unchanged.
3. Fill all potion slots, earn a Cave potion reward, and retreat.
   Expected: The full-potion modal shows the current slots in the same labeled div-based flex group with no `ul` or `li` elements.
4. Select each existing potion replacement option and repeat using Discard new potion.
   Expected: Replacement receives the selected potion index, discard keeps the existing slots, and PotionIcon content remains unchanged.
5. Repeat the token and potion flows for English and Japanese players while checking the console.
   Expected: Group labels, actions, item names, and language font classes remain localized, and no console errors occur.

Automated Tests Completed

* Before the fix, both focused modal tests failed because the rendered token and potion choices exposed `list`/`listitem` roles and no labeled div-based `group`.
* `src/pages/MiniGames/CaveRewardChoiceModal.test.jsx` passes 2 tests covering div containers, localized accessible group names, no `ul`/`li` JSX or rendered list roles, shared flex CSS, icons, language classes, replacement callbacks, and discard callbacks.
* The Cave reward/provider/App regression passes 5 suites and 34 tests, confirming full-capacity choices, inventory mutation, routing, and turn handling remain unchanged.
* The repository-installed ESLint binary passes the changed component and test with no warnings or errors.
* `npm run build` compiles successfully and `git diff --check` reports no whitespace errors.

Automated Tests Still Needed

* JSDOM and source assertions verify markup, accessibility roles, CSS declarations, and callbacks but cannot visually certify wrapping at every viewport size.

Manual Testing Still Needed

* Run the five-step browser regression above to inspect real keyboard focus, flex wrapping, icon/tooltips, Japanese rendering, and console output.

---

Bug-008: Cave token rewards should use existing token assignment component instead of auto-adding to token bag

Test Status

* passed

Manual Regression Steps And Expected Results

1. Find or debug-add a Cave token, retreat, and inspect inventory before assigning it.
   Expected: The token is not auto-added to the bag; the existing shared reward assignment page opens with the exact generated token.
2. Drag the Cave token to an available spell slot and confirm.
   Expected: The shared assignment UI commits the token to that slot, shows the existing completion copy, and Continue restores the Cave retreat summary.
3. Repeat by assigning the token to a non-full token bag and by dragging it to discard.
   Expected: Bag assignment adds one uncommitted token; discard leaves bag and spell slots unchanged; both clear the pending Cave token transaction only after confirmation.
4. Repeat with a full token bag and select an existing bag token through the shared replacement flow.
   Expected: The existing assignment component handles replacement, the bag remains at five tokens, and no Cave “Token bag is full” replacement modal appears.
5. Retreat with both a token and a full-capacity potion reward.
   Expected: Token assignment opens first; after it completes, the existing Cave potion replacement/discard modal opens and potion behavior remains unchanged.
6. Retreat with token, potion, Loot Chest, and Roll Again Potion rewards.
   Expected: The order is token assignment, any pending potion choice, restored Cave summary, Loot Chest, then same-player Gameplay with the roll-again notice.
7. Retreat with a token but without Loot Chest or roll again.
   Expected: Assignment completes before the Cave Continue action returns to Gameplay, advances to the next player, and shows the locked next-turn modal.
8. Repeat token assignment for English and Japanese players.
   Expected: The existing assignment labels, item names, status, confirmation, completion copy, and language font classes remain localized without Cave-specific wording.
9. Find a token and then encounter an ogre.
   Expected: The token is lost, no reward assignment opens, and the existing failure/next-player flow remains unchanged.
10. Inspect the assignment and Cave flows on desktop/mobile with keyboard and pointer input while checking the console.
    Expected: The existing drag/drop controls remain usable, no new `ul`/`li` markup was introduced, and no console errors occur.

Automated Tests Completed

* Before the fix, focused regressions produced two expected failures: `createCaveRewardGrant` inserted `player-1-red-1` directly into the bag, and the real App opened the separate full-token modal instead of shared assignment.
* `src/features/miniGames/caveRewardGrant.test.js` verifies pending token assignment, exact reward preservation, no direct bag mutation, Cave-tagged shared reward state, token-first ordering, and unchanged automatic/full-capacity potion behavior.
* `src/features/miniGames/miniGameState.test.jsx` verifies shared bag assignment, full-bag replacement, discard, pending-return blocking, Cave grant status, potion ordering, inventory capacity, and final roll-again state.
* `src/AppMiniGames.test.jsx` verifies the real shared spell-slot assignment route, removal of the Cave full-token modal, restored retreat summary, full-potion choice, Loot Chest ordering, same-player roll again, and no-roll next-player modal.
* Cave page tests verify token retreats route to `/reward`; existing RewardPage, SpellTokenAssignment, reward state, reward drop helper, and Cave choice modal suites confirm battle behavior, translations, drag/drop contracts, and potion-only choice behavior remain intact.
* The final Bug-008 regression passes 9 suites and 81 tests. The repository-installed ESLint binary passes every changed implementation/test file, `npm run build` compiles successfully, and `git diff --check` reports no whitespace errors.

Automated Tests Still Needed

* The App integration uses a deterministic test adapter to trigger the shared drop callback; the existing SpellTokenAssignment suite separately proves the real drag/drop zones and states, but JSDOM cannot reproduce full pointer geometry.

Manual Testing Still Needed

* Run the ten-step browser regression above for real drag/drop, full-bag selection, responsive layout, Japanese rendering, tooltip/focus behavior, randomized reward identity, and console inspection.

---

Bug-009: Cave token assignment should use cave background

Test Status

* passed

Manual Regression Steps And Expected Results

1. Find or debug-add a Cave token and retreat.
   Expected: The existing shared token assignment opens over `src/images/miniGames/cave.png`, not a battle, gameplay, or fallback background.
2. Inspect the assignment screen at desktop and mobile sizes.
   Expected: The cave image covers the full screen, stays centered, does not tile, remains behind the existing panel, and assignment content stays readable.
3. Assign the token to a spell slot, token bag, and discard in separate runs.
   Expected: Existing drag/drop, confirmation, completion, and Cave return behavior are unchanged.
4. Complete a normal battle token reward assignment.
   Expected: The selected battle environment remains the background and does not use the cave image.
5. Open the normal Gameplay Spells modal and check the console during both flows.
   Expected: Gameplay modal behavior/background is unchanged and no console errors occur.

Automated Tests Completed

* Before the fix, the new focused test failed because a Cave-sourced assignment rendered the synthetic fields battle background; the other 17 RewardPage tests passed.
* `src/pages/RewardPage.test.jsx` passes 18 tests, including exact Cave image selection, shared assignment rendering, unchanged forest battle background, invalid-environment fields fallback, language behavior, and all token/potion reward actions.
* `src/AppMiniGames.test.jsx` passes 14 tests, confirming Cave assignment still continues through potion, loot, roll-again, loss, and next-player flows.
* The combined regression passes 2 suites and 32 tests. Scoped ESLint passes, `npm run build` compiles successfully, and `git diff --check` reports no whitespace errors.

Automated Tests Still Needed

* JSDOM verifies the selected image and existing CSS declarations but cannot visually certify real browser cropping or readability at every viewport.

Manual Testing Still Needed

* Run the five-step browser regression above for real image cover/crop, assignment readability, drag/drop interaction, responsive behavior, and console inspection.
