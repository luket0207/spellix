---

Bug-001: Gameplay magical background needs more stars and continuous spell effects

Status

* fixed

Related Feature

* Feature-001: Add animated magical night sky background to gameplay layout

Bug Summary

* The gameplay background needs to feel more alive.
* There should be more stars visible in the night sky.
* Spell animations should trigger randomly and continuously across the whole background.
* The effect should create variety across all areas of the gameplay layout rather than only occasional or localised effects.

Repro Steps

* Start a game.
* View the gameplay layout background.
* Watch the animated magical night sky.
* Observe the number of stars.
* Observe how often spell animations appear.
* Observe whether spell animations appear across different areas of the screen.

Expected Behaviour

* The gameplay background should have noticeably more stars.
* Stars should still twinkle randomly.
* Spell animations should be triggered randomly all the time.
* Spell animations should appear in random places across the full gameplay background.
* There should be a variety of spell effects happening in different areas.
* The background should feel active, magical, and alive.
* Effects should remain decorative only.
* Effects should not block clicks or interfere with gameplay.

Actual Behaviour

* There are not enough stars.
* Spell animations do not appear often enough.
* Spell animations do not feel like they are happening continuously across the whole background.

Required Fix

* Increase the number of stars in the magical night sky background.
* Increase the frequency of spell animation spawning.
* Ensure spell animations can spawn across all areas of the gameplay layout.
* Add or preserve variety in spell effects.
* Effects should trigger at random positions and random intervals.
* Avoid clustering all effects in the same section of the screen.
* Ensure effects continue to spawn while the player remains on the gameplay page.
* Ensure timers or animation loops are cleaned up when leaving gameplay.

Star Density Requirement

* Increase the number of stars compared with the current implementation.
* The sky should feel full but not cluttered.
* Stars should still vary by:

  * size.
  * opacity.
  * twinkle timing.
  * animation delay.
* Avoid making the stars so bright that they distract from the board or UI.
* Stars should remain behind all gameplay content.

Suggested Direction

```txt id="5qp81k"
Increase star count by at least 2x from the current implementation.
```

* Exact number can vary depending on performance and visual quality.
* Prioritise a fuller, more magical night sky.

Spell Animation Frequency Requirement

* Spell animations should happen regularly and continuously.
* There should usually be at least one spell effect active or recently active.
* The player should not have to wait a long time before seeing another magical effect.
* Effects should spawn at random intervals.
* Avoid long empty periods where nothing happens.

Suggested Direction

```txt id="eyxm28"
Spawn a new spell effect every 500ms to 1500ms.
```

* Exact timing can vary if it looks better.
* The important requirement is that spell effects feel active and ongoing.

Spell Animation Position Requirement

* Spell animations should spawn in random places across the whole gameplay layout.
* They should not be restricted to one small area.
* Effects should be able to appear:

  * near the top.
  * near the bottom.
  * near the left.
  * near the right.
  * around the centre.
* Effects should not affect layout.
* Effects should not create scroll bars.
* Effects should remain behind board/UI elements.

Effect Variety Requirement

* Keep or add visual variety.
* Possible spell effects include:

  * glowing orbs.
  * comet-like trails.
  * swirls.
  * small magical bursts.
  * particle clusters.
* Effects should use varied colours.
* Suggested colours:

  * blue.
  * purple.
  * green.
  * yellow.
  * pink.
  * orange.
* Effects should vary in:

  * size.
  * travel direction.
  * speed.
  * fade timing.
  * colour.
* Do not make all effects identical.

Layering Requirement

* Stars and spell effects should stay behind gameplay content.
* They should not cover or obscure:

  * the board.
  * player pieces.
  * Roll Dice button.
  * Spells button.
  * Potions area.
  * settings cog.
  * modals.
* The magical background layer should continue to use:

```css id="5o315n"
pointer-events: none;
```

* It must not block gameplay interactions.

Performance Requirement

* More stars and more spell effects should not harm gameplay performance.
* Avoid excessive DOM nodes.
* Remove completed spell effect elements after their animation ends.
* Use CSS animations, canvas, or the single selected third-party library consistently.
* Do not introduce additional third-party animation libraries.
* Avoid memory leaks from repeated timers.

Randomness Requirement

* Spell effects should appear random.
* Star twinkle timings should feel random.
* Effects should not repeat in an obvious fixed pattern.
* Randomness does not need to be saved in game state.
* The background can regenerate when gameplay loads.

Expected Behaviour After Fix

* The gameplay background has more stars than before.
* Stars twinkle throughout the background.
* Spell effects appear frequently.
* Spell effects appear across all areas of the gameplay layout.
* Different types or styles of spell effects appear.
* The background feels more magical and active.
* Gameplay remains fully usable.
* Performance remains acceptable.

Out Of Scope

* No changes to gameplay mechanics.
* No changes to board generation.
* No changes to movement rules.
* No changes to battle logic.
* No changes to token assignment.
* No changes to potion area logic.
* No changes to modal styling.
* No changes to Start screen background.
* No changes to Game Setup background.
* No new gameplay meaning attached to background effects.
* No additional third-party libraries beyond the one allowed by Feature-001.

Suspected Cause

* The current star count is too low.
* The spell effect spawn interval may be too slow.
* Spell effects may be limited to a small number of positions or one part of the layout.
* Completed effects may not be replaced quickly enough.

Attempted Fixes

* Feature-001 added the initial magical night sky background.
* The current implementation needs increased density and more continuous activity.
* Added failing regressions for doubled star density, continuous spawning, finite effect nodes, full-area variety, bounded DOM usage, and timer cleanup before changing the implementation.

Final Fix

* Increased the mount-time star field from 48 to 96 while preserving varied size, opacity, twinkle duration, delay, and the local decorative random generator.
* Replaced nine permanently looping spell elements with three initial mixed effects and recursive random spawning every 500-1500 ms.
* Each effect now uses a finite 2.4-4.2 second animation, is removed when complete, and is selected from the existing orb, comet, swirl, six-colour, size, movement, and rotation ranges.
* Expanded spawn positions to 2-98% on both axes, capped the active spell DOM set at eight, and clear the spawn timeout plus every pending removal timeout when Gameplay unmounts.
* Preserved `pointer-events: none`, background layering, reduced-motion styling, Gameplay-only integration, and isolation from gameplay `Math.random` values.

Testing Required

* Confirm the gameplay background has more stars than before.
* Confirm stars still twinkle.
* Confirm star twinkling remains varied.
* Confirm spell effects spawn frequently.
* Confirm spell effects appear continuously while on gameplay.
* Confirm spell effects appear in random places.
* Confirm spell effects appear near the top of the layout.
* Confirm spell effects appear near the bottom of the layout.
* Confirm spell effects appear near the left side of the layout.
* Confirm spell effects appear near the right side of the layout.
* Confirm spell effects appear around the centre.
* Confirm there is visual variety in spell effects.
* Confirm spell effects use varied colours.
* Confirm effects fade in and out smoothly.
* Confirm effects do not block board clicks.
* Confirm effects do not block gameplay buttons.
* Confirm effects remain behind gameplay content.
* Confirm no extra scroll bars are created.
* Confirm completed effects are cleaned up.
* Confirm leaving gameplay cleans up timers or animation loops.
* Confirm gameplay performance remains acceptable.
* Confirm there are no console errors.
* Update `testing.txt` with regression steps before marking this bug fixed.

---

Bug-002: Potions area should match committed spell slot display styling

Status

* fixed

Related Feature

* Feature-002: Add styled Potions area to gameplay UI

Bug Summary

* The new Potions area does not visually match the existing Spells display.
* It should have similar styling to the `committed-spell-slot-display` element.
* The Potions area should feel like part of the same UI group as the committed spell slots.
* The current styling is too different and looks inconsistent.

Repro Steps

* Start a game.
* Reach the gameplay screen.
* Look at the existing committed spell slot display.
* Look at the new Potions area.
* Compare their styling.
* Observe that the Potions area does not match the visual treatment of `committed-spell-slot-display`.

Expected Behaviour

* The Potions area should visually match the existing committed spell slot display styling.
* It should use similar:

  * background.
  * border.
  * border radius.
  * spacing.
  * padding.
  * text colour.
  * font treatment.
  * layout feel.
* The Potions area should look like it belongs directly underneath or near the Spells display.
* It should not look like a separate unrelated UI component.

Actual Behaviour

* The Potions area styling does not match the spell display.
* It does not visually align with the `committed-spell-slot-display` element.

Required Fix

* Update the Potions area styling to closely match `committed-spell-slot-display`.
* Inspect the existing CSS for `committed-spell-slot-display`.
* Reuse or mirror its styles where appropriate.
* Do not invent a completely separate style for the Potions area.
* The Potions area should look like the same component family as the committed spell slot display.

Styling Requirement

* The Potions area should use styling similar to:

```txt id="o1ks8u"
committed-spell-slot-display
```

* This should include similar:

  * container background.
  * border styling.
  * border radius.
  * internal padding.
  * heading style.
  * item spacing.
  * text colour.
  * overall width and alignment where appropriate.

* If `committed-spell-slot-display` already has reusable CSS values, prefer sharing them rather than duplicating slightly different values.

Suggested CSS Direction

```css id="lmc36q"
.potions-area {
  /* mirror committed-spell-slot-display visual treatment */
}

.potions-area-header {
  /* align with committed spell slot heading/header style */
}

.potions-list {
  /* align with committed spell slot item layout */
}
```

* Exact class names can vary.
* The important requirement is visual consistency with `committed-spell-slot-display`.

Structure Requirement

* Potions should still be rendered using `div` elements.
* Do not change the Potions list back to `ul` or `li`.
* Preserve the requirement from Feature-002 that potion items are not list elements.

Title Requirement

* The title should still translate based on the current player’s selected language.
* English:

```txt id="xrfka9"
Potions
```

* Japanese:

```txt id="uw7b0i"
ポーション
```

* The title styling should feel consistent with the committed spell slot display.

Capacity Requirement

* The capacity display should remain.
* It should still display as:

```txt id="khhu57"
0/3
1/3
2/3
3/3
```

* Do not add extra words around the capacity.
* The capacity display should be styled to match the spell display rather than feeling like unrelated text.

Potion Item Requirement

* Potion items should still use the global `PotionIcon` component.
* Potion names should still display in the current player’s language.
* Potion tooltips should still display in the current player’s language.
* Potion icons should sit neatly inside the Potions area.
* The item layout should match the spacing and alignment style of committed spell slot content.

Out Of Scope

* No changes to potion capacity.
* No changes to potion data.
* No changes to potion translations.
* No changes to potion reward generation.
* No potion effect implementation.
* No changes to the Spells table logic.
* No changes to committed spell slot behaviour.
* No unrelated gameplay UI redesign.

Suspected Cause

* Feature-002 likely introduced a new independent Potions area style instead of matching the existing `committed-spell-slot-display` style.
* The Potions area may be using generic brown panel styling rather than the established spell display styling.

Attempted Fixes

* Feature-002 added the Potions area.
* The current implementation needs visual alignment with the committed spell display.
* Added a failing source regression that compares Potions against the established committed display container, heading, and slot-column declarations before changing styles.

Final Fix

* Replaced the independent dark Potions treatment with the committed display's parchment gradient, `#4a3520` border, 10px radius, inset/drop shadows, dark text, 8px gap, 10px padding, and 300px width.
* Matched the Potions heading to the committed display title's 18px bold text and `0.04em` letter spacing while preserving the capacity beside it.
* Styled each existing div-based potion slot with the committed slot column's padding, 6px gap, `#715231` border, 8px radius, and layered parchment background.
* Preserved the compact Potions height, div-only structure, no-scroll layout, `0/3`-`3/3` capacity, global `PotionIcon`, localization, current-player updates, and all potion/gameplay behavior.

Testing Required

* Confirm the Potions area visually matches `committed-spell-slot-display`.
* Confirm the Potions area background matches or closely mirrors the committed spell display.
* Confirm the Potions area border matches or closely mirrors the committed spell display.
* Confirm the Potions area border radius matches or closely mirrors the committed spell display.
* Confirm the Potions area padding and spacing feel consistent with the committed spell display.
* Confirm the Potions title aligns visually with the spell display styling.
* Confirm the capacity text `0/3`, `1/3`, `2/3`, or `3/3` remains visible.
* Confirm potion items are still rendered with `div` elements.
* Confirm no `ul` or `li` is used for potion items.
* Confirm potion icons still use `PotionIcon`.
* Confirm potion names still translate correctly.
* Confirm potion tooltips still translate correctly.
* Confirm the Potions area updates when the current player changes.
* Confirm gameplay layout is not broken by the styling change.
* Confirm there are no console errors.
* Update `testing.txt` with regression steps before marking this bug fixed.

---

Bug-003: Potion elements should remove rarity/type text and use fixed matching slot sizes

Status

* fixed

Related Feature

* Feature-002: Add styled Potions area to gameplay UI

Related Bug

* Bug-002: Potions area should match committed spell slot display styling

Bug Summary

* Potion elements inside the Potions area need cleaner styling.
* The paragraph that displays rarity and Battle/Board information should be removed.
* Each potion element should have a fixed width of 86px.
* All potion elements inside the potion slots should match each other’s height.
* The Potions area should remain visually consistent with the committed spell slot display.

Repro Steps

* Start a game.
* Reach the gameplay screen.
* View the Potions area.
* Look at potion elements inside the potion slots.
* Observe the text showing rarity and Battle/Board information.
* Compare potion element widths and heights.

Expected Behaviour

* Potion elements should not show rarity text.
* Potion elements should not show Battle/Board type text.
* The `p` tag containing rarity and Battle/Board information should be removed entirely.
* Each potion element should be:

```txt id="xq6yxh"
86px wide
```

* Potion elements inside potion slots should all match each other’s height.
* Potion elements should look consistent even when potion names have different lengths.
* Potion icons, names, and tooltips should still work.
* Potion names should remain translated according to the current player’s language.
* Potion tooltips should remain translated according to the current player’s language.

Actual Behaviour

* Potion elements currently display a `p` tag containing rarity and Battle/Board information.
* Potion elements may not have a consistent fixed width.
* Potion elements may not match each other’s height inside potion slots.

Required Fix

* Remove the `p` tag that displays potion rarity and Battle/Board information.
* Do not replace it with another visible rarity/type label.
* Keep rarity and Battle/Board data in the potion data object for logic and filtering.
* Set each potion element width to 86px.
* Ensure all potion elements within the potion slots have matching height.
* Preserve the PotionIcon display.
* Preserve potion name display.
* Preserve potion tooltip behaviour.
* Preserve potion colour and glow styling.

Potion Metadata Display Requirement

* Remove visible metadata text such as:

  * Common.
  * Rare.
  * Battle.
  * Board.
  * Both.
  * Common / Board.
  * Rare / Battle.
* This information should not appear in the gameplay Potions area.
* Do not remove this data from the underlying potion data if it is still needed elsewhere.
* Only remove the visible rendered `p` tag.

Potion Element Width Requirement

* Each potion element should have a fixed width of:

```txt id="mlx5p9"
86px
```

* Suggested CSS direction:

```css id="o0yn01"
.potion-slot,
.potion-display,
.potion-item {
  width: 86px;
}
```

* Exact class names can vary.
* Use the class that represents the individual potion element inside the Potions area.
* Avoid applying this width globally if it breaks potion display in other areas such as rewards.
* If using a global PotionIcon wrapper, support a context-specific class or prop.

Potion Element Height Matching Requirement

* All potion elements inside the potion slots should match each other’s height.
* Potion names with different lengths should not make one potion slot taller than the others.
* Use a consistent slot height or flex alignment.
* Suggested CSS direction:

```css id="c48f53"
.potions-list {
  display: flex;
  align-items: stretch;
}

.potion-slot {
  width: 86px;
  min-height: 100%;
  display: flex;
  align-items: center;
  justify-content: center;
}
```

* Exact implementation can vary.
* The important requirement is that potion elements visually match height inside the Potions area.

Name Wrapping Requirement

* Potion names should remain readable within the 86px width.
* Long potion names may wrap if needed.
* Name wrapping should not make one potion element taller than the others.
* If needed, use a fixed name area height.
* Do not hide names completely unless a later feature requests it.

Tooltip Requirement

* Potion tooltips should still work after removing the metadata paragraph.
* Tooltips should still use the current player’s language.
* Tooltips should remain readable and stay in view where practical.
* Removing the metadata paragraph should not remove the tooltip description.

Styling Consistency Requirement

* Potion elements should still match the Potions area styling from Bug-002.
* The Potions area should still look similar to `committed-spell-slot-display`.
* Potion elements should not look stretched or uneven.
* Potion elements should align neatly inside the potion slots.

Out Of Scope

* No changes to potion capacity.
* No changes to potion data.
* No changes to potion rarity rules.
* No changes to Battle/Board availability logic.
* No changes to potion reward generation.
* No potion effect implementation.
* No changes to potion translations.
* No changes to the Spells display.
* No unrelated gameplay UI redesign.

Suspected Cause

* The Potion element is likely rendering all potion metadata by default.
* The rarity and Battle/Board paragraph may have been useful during earlier debugging or reward display work, but it is too noisy for the gameplay Potions area.
* Potion heights may differ because text content length is changing the natural height of each element.

Attempted Fixes

* Feature-002 added the Potions area.
* Bug-002 aligned the Potions area with the committed spell display.
* This bug focuses specifically on the individual potion elements.
* Added failing component and Gameplay regressions for absent metadata, removal of the metadata `p` tag, fixed 86px slots, and stretch-based equal heights before changing implementation.

Final Fix

* Removed only the gameplay `PotionList` paragraph that rendered `${potion.rarity} | ${potion.availability}`; the underlying rarity and availability fields remain unchanged for rewards, filtering, and game logic.
* Set each feature-local `.potion-slot` to an exact 86px width, 86px minimum width, and `0 0 86px` flex basis without changing the global `PotionIcon` component or other potion displays.
* Changed `.potions-list` to `align-items: stretch`, so all slots in a row match the tallest wrapped potion name while retaining readable wrapping and the existing icon alignment.
* Preserved the committed-display parchment styling, div-only structure, 0-3 capacity, no-scroll layout, current-player updates, English/Japanese names and tooltips, colours, glow, and all potion behavior.

Testing Required

* Confirm the rarity/type `p` tag is removed from potion elements in the Potions area.
* Confirm `Common` is not displayed on potion elements.
* Confirm `Rare` is not displayed on potion elements.
* Confirm `Battle` is not displayed on potion elements.
* Confirm `Board` is not displayed on potion elements.
* Confirm `Both` is not displayed on potion elements.
* Confirm each potion element is 86px wide.
* Confirm all potion elements inside potion slots match each other’s height.
* Confirm long potion names do not break the layout.
* Confirm potion names remain readable.
* Confirm potion names still translate correctly.
* Confirm potion tooltips still work.
* Confirm potion tooltips still translate correctly.
* Confirm potion colours and glow styling still work.
* Confirm the Potions area still displays `0/3`, `1/3`, `2/3`, or `3/3`.
* Confirm the Potions area still visually matches `committed-spell-slot-display`.
* Confirm there are no console errors.
* Update `testing.txt` with regression steps before marking this bug fixed.

---

Bug-004: River roll-again modal text should use correct language fonts

Status

* fixed

Related Feature

* Feature-006: Refine River Mini Game result flow, rock sizing, player spacing, and return modal translation

Bug Summary

* The modal text that says `You crossed the river! You may roll again.` is not respecting the correct fonts for each language.
* English text should use the English fantasy font.
* Japanese text should use the Japanese font.
* The modal already supports English and Japanese text, but the typography is not being applied correctly.

Repro Steps

* Start a game with an English player.
* Trigger and win the River Mini Game.
* Continue through the Loot Chest page.
* Return to the board.
* Observe the modal text:

```txt id="t64w8v"
You crossed the river! You may roll again.
```

* Repeat with a Japanese player.
* Observe the Japanese modal text:

```txt id="n9u9gk"
川を渡り切りました！もう一度サイコロを振ることができます。
```

* Check whether each language uses the correct font.

Expected Behaviour

* English modal text should use:

```txt id="jrm0kr"
Unkempt
```

* Japanese modal text should use:

```txt id="hcsorr"
Noto Serif Japanese
```

* The font should be selected based on the current player’s chosen language.
* The Japanese modal should not use the English font.
* The English modal should not use the Japanese font unless it is a fallback.
* The modal text should remain bold and yellow as required by the modal styling features.
* The OK button should remain at the bottom of the modal.

Actual Behaviour

* The River roll-again modal text does not respect the correct font for the active language.

Required Fix

* Update the River roll-again modal text styling so it uses the correct font based on the current player’s selected language.
* Use the same language-aware font handling used elsewhere in the app.
* The modal body text should receive a language-specific class or style.
* Do not hardcode only one font for this modal.
* Ensure the font is applied to the actual text element, not only the modal container if it is being overridden elsewhere.

Suggested Implementation Direction

```jsx id="fl06vm"
const modalLanguageClass = currentLanguage === "jp"
  ? "text-japanese"
  : "text-english";
```

```jsx id="pqz0hy"
<p className={modalLanguageClass}>
  {riverRollAgainMessage}
</p>
```

Suggested CSS direction:

```css id="lg0z7k"
.text-english {
  font-family: "Unkempt", cursive;
}

.text-japanese {
  font-family: "Noto Serif JP", "Noto Serif Japanese", serif;
}
```

* Exact class names can vary.
* Use the existing project font class names if they already exist.
* Do not introduce duplicate font systems if one already exists.

Modal Text Requirement

* The English modal message should remain:

```txt id="c8ksv4"
You crossed the river! You may roll again.
```

* The Japanese modal message should remain:

```txt id="iv4rug"
川を渡り切りました！もう一度サイコロを振ることができます。
```

* Text colour should remain:

```txt id="igtlgh"
#F5FA00
```

* Text should remain bold.
* The text should remain readable against the modal background.

Font Inheritance Requirement

* Check whether the shared modal styles are overriding language-specific fonts.
* If the shared modal applies a default font, allow the River roll-again message to override it.
* If modal body text inherits incorrectly from the page, explicitly apply the language font to the message.
* Do not break other modal text styling.
* Do not break Japanese font handling in other translated modals.

Button Layout Requirement

* Keep the OK button at the bottom of the modal.
* Keep the OK button horizontally centred.
* Do not let this font fix undo the modal button layout from Feature-006.

Out Of Scope

* No changes to the River Mini Game flow.
* No changes to the Loot Chest page.
* No changes to the Mini Game Lose page.
* No changes to win/loss logic.
* No changes to the modal message wording.
* No changes to modal background image.
* No changes to modal padding.
* No changes to button styling.
* No changes to dice roll modal.
* No unrelated typography redesign.

Suspected Cause

* The modal may be using a global modal font that overrides language-specific text.
* The River roll-again message may not be wrapped in the same language-aware text element as other translated UI.
* The Japanese message may be displayed correctly but still inheriting the English `Unkempt` font.

Attempted Fixes

* Feature-006 added the English/Japanese text for this modal.
* The text content is translated, but the language-specific font styling still needs fixing.
* Confirmed the shared `.language-en` and `.language-jp` font classes already provide Unkempt and Noto Serif JP, while the River roll-again message element currently receives no language class.
* Added focused App integration regressions for both message classes and the existing global font declarations before changing the message element.

Final Fix

* Applied the existing `languageClassName` directly to the River roll-again modal message element in Gameplay.
* English return text now receives `language-en` and uses the established Unkempt font; Japanese return text receives `language-jp` and uses the established Noto Serif JP font.
* Reused the global language classes without adding or changing font CSS, preserving the exact translated messages, bold yellow modal styling, bottom-centred OK action, shared modal layout, and roll-again behavior.

Testing Required

* Confirm English players see `You crossed the river! You may roll again.`
* Confirm Japanese players see `川を渡り切りました！もう一度サイコロを振ることができます。`
* Confirm the English message uses Unkempt.
* Confirm the Japanese message uses Noto Serif Japanese / Noto Serif JP.
* Confirm the text remains `#F5FA00`.
* Confirm the text remains bold.
* Confirm the modal background still uses the fantasy modal background.
* Confirm the OK button remains at the bottom of the modal.
* Confirm the OK button remains horizontally centred.
* Confirm closing the modal still allows the same player to roll again.
* Confirm other translated modal text is not broken.
* Confirm there are no console errors.
* Update `testing.txt` with regression steps before marking this bug fixed.

---

Bug-005: Next-turn modal must always appear after any player turn change

Status

* fixed

Related Feature

* Feature-003: Add River Mini Game debug trigger, mini game flow, loot chest page, and mini game lose page
* Feature-006: Refine River Mini Game result flow, rock sizing, player spacing, and return modal translation

Bug Summary

* The next-turn modal is not reliably appearing after turn changes.
* This is not only an issue after the River Mini Game.
* The next-turn modal must appear after every player-to-player turn change, no matter what caused the turn to change.
* The modal should appear 100% of the time whenever the active player changes.
* It should always be the first thing shown after the new player becomes active.

Repro Steps

* Start a game with at least 2 players.
* Trigger any action that moves the turn from one player to another.
* Examples include:

  * normal movement ending a turn.
  * losing a River Mini Game.
  * any other mini game loss.
  * any battle flow that advances the turn.
  * any modal/action/debug flow that advances the turn.
* Observe whether the current player changes.
* Check whether the next-turn modal appears immediately.

Expected Behaviour

* Any time the active player changes, the next-turn modal should appear.
* The next-turn modal should appear 100% of the time after a player-to-player turn change.
* The next-turn modal should be the first thing shown after the turn changes.
* The new player should not be able to roll, move, open spells, or take another action before seeing the modal.
* The modal should use the new current player’s selected language.
* The modal should show the correct new current player.

Actual Behaviour

* Some turn changes advance to the next player without showing the next-turn modal.
* This can happen after the River Mini Game, but the issue is broader than that.
* The next-turn modal is not guaranteed across all turn-change paths.

Required Fix

* Centralise turn-change behaviour.
* Ensure every player-to-player turn change goes through the same turn-advance function or modal-triggering pathway.
* Do not rely on individual screens or flows remembering to show the next-turn modal manually.
* Any code path that changes the active player must also trigger the next-turn modal.
* Avoid manually setting the next player directly without setting the next-turn modal state.
* If there are existing direct calls to `setCurrentPlayer`, `setCurrentPlayerIndex`, or similar, review them and route them through the central turn-change handler where appropriate.

Turn Change Invariant

* This rule must always be true:

```txt id="p0hx8p"
If the active player changes, the next-turn modal appears first.
```

* This applies no matter what caused the turn change.
* The next-turn modal should appear:

  * after normal end-of-turn movement.
  * after battle flows that advance to the next player.
  * after mini game losses.
  * after returning from shared Mini Game Lose page.
  * after any future mini game result that advances the turn.
  * after debug actions that intentionally advance the turn.
  * after any potion, spell, event, or future mechanic that advances the turn.
* The modal should not be optional.
* The modal should not be skipped.

Central Turn Advance Requirement

* Create or use one central function for advancing from one player to the next.
* Suggested direction:

```js id="yf5y7y"
function advanceToNextPlayer() {
  const nextPlayer = getNextPlayer();
  setCurrentPlayer(nextPlayer);
  setPendingNextTurnModal(true);
}
```

* Exact names can vary.
* The important requirement is that active-player change and next-turn modal state are linked.
* Any flow that advances the turn should call this central function.
* Do not duplicate separate turn-change logic across gameplay, battle, mini game, or debug screens.

Modal Priority Requirement

* The next-turn modal should have priority immediately after a player-to-player turn change.
* It should appear before:

  * Roll Dice becomes usable.
  * movement selection becomes usable.
  * spells can be opened.
  * potion actions can be taken.
  * other non-critical modals are shown.
* The player should not be able to accidentally take a turn without seeing the modal.

Language Requirement

* The next-turn modal should use the language of the new current player.
* If the new current player uses English, show the English version.
* If the new current player uses Japanese, show the Japanese version.
* Preserve the existing EPIC-008 next-turn modal wording and colour translation behaviour.
* If language is missing or invalid, fall back to English.

Duplicate Modal Prevention Requirement

* The next-turn modal should appear exactly once per player-to-player turn change.
* Do not show duplicate next-turn modals for the same turn transition.
* Closing the modal should clear the pending modal state.
* Returning to the board should not retrigger the modal repeatedly unless another turn change occurs.

Same-Player Turn Continuation Requirement

* Do not show the next-turn modal when the active player does not change.
* Example:

  * River Mini Game win lets the same player roll again.
  * This should show the River roll-again modal, not the next-turn modal.
* The next-turn modal is only for player-to-player turn changes.
* Same-player extra turns should use their own explicit modal/message if required.

Review Existing Turn Change Sources

* Review all current places where the active player can change.
* These may include:

  * normal board movement completion.
  * end turn logic.
  * battle result handling.
  * mini game loss handling.
  * shared Mini Game Lose page return.
  * debug controls.
  * future potion or spell turn effects if already present.
* Any of these that changes the active player must trigger the next-turn modal.

Out Of Scope

* No changes to turn order.
* No changes to player randomisation.
* No changes to River Mini Game win flow.
* No changes to same-player roll-again behaviour.
* No changes to modal styling.
* No changes to next-turn modal text unless required to preserve existing translations.
* No changes to battle mechanics.
* No changes to mini game rules.
* No unrelated gameplay redesign.

Suspected Cause

* Some code paths may be changing the current player directly.
* The next-turn modal may only be triggered by one specific end-turn path.
* Returning from separate pages may restore gameplay with the new player already set, but without setting pending modal state.
* Turn-change state and next-turn modal state are not currently enforced together.

Attempted Fixes

* Added provider and River return regressions that reproduced the issue: both ordinary `advanceTurn` and a River loss changed the active player while next-turn modal state remained clear.
* Moved pending next-turn state into `GameSetupContext` so it persists across battle, reward, mini-game result, and Gameplay routes rather than relying on `GameplayPage` local state.
* Routed ordinary advancement and mini-game return through one `transitionToPlayerTurn` helper that compares player IDs and queues the modal only for a real player-to-player change.
* Replaced Gameplay's local turn-modal flag with the provider state, blocked Roll Dice, movement, Spells, forced spell setup, and the global Settings entry point while pending, and added one provider dismissal action.
* Verified the existing battle Respawn and reward Continue paths already call centralized `advanceTurn`; added real-provider pending-state assertions for both.

Final Fix

* Every current player-to-player turn-ending path now atomically stores the new turn index with `pendingNextTurnModal: true` in the shared provider.
* Gameplay renders and dismisses that shared state, so route-based returns cannot lose the modal and the new player's language, colour wording, and piece continue to drive its content.
* River wins and other same-player/no-op transitions preserve clear next-turn state, while River losses, movement, battle Respawn, and reward Continue queue one dismissible modal.
* Pending state now has priority over board controls, Spells including forced setup, and Settings without changing modal styling, turn order, battle mechanics, or mini-game rules.

Testing Required

* Confirm normal turn ending shows the next-turn modal.
* Confirm movement ending a turn shows the next-turn modal.
* Confirm losing the River Mini Game shows the next-turn modal after returning to the board.
* Confirm any shared Mini Game Lose page return that advances the turn shows the next-turn modal.
* Confirm battle flows that advance the turn show the next-turn modal.
* Confirm debug actions that advance the turn show the next-turn modal.
* Confirm the modal is the first thing shown after each player-to-player turn change.
* Confirm the new player cannot roll before seeing the modal.
* Confirm the new player cannot open spells before seeing the modal.
* Confirm the modal uses the new current player’s language.
* Confirm the modal displays the correct new current player.
* Confirm the modal appears exactly once per turn change.
* Confirm closing the modal clears it.
* Confirm same-player extra turns do not show the next-turn modal.
* Confirm River Mini Game wins still show the roll-again modal instead.
* Confirm there are no duplicate modals.
* Confirm there are no console errors.
* Update `testing.txt` with regression steps before marking this bug fixed.

---

Bug-006: Cave Mini Game player sprite facing direction is reversed

Status

* fixed

Related Feature

* Feature-011: Refine Cave Mini Game movement, retreat timing, ogre chase animation, and reward display

Bug Summary

* The player’s character image is facing the wrong direction in the Cave Mini Game.
* At the start of the Cave Mini Game, the character should be flipped so they appear to face into the cave.
* When an ogre appears and chases the player, the character should be unflipped so they appear to run away from the ogre.
* The current implementation has this facing direction the wrong way around.

Repro Steps

* Start the Cave Mini Game.
* Look at the current player’s character image above the cave text box.
* Observe which direction the character is facing.
* Trigger an ogre outcome.
* Observe the character direction during the chase animation.

Expected Behaviour

* At the start of the Cave Mini Game:

  * the player character should be flipped.
  * the character should visually face toward the direction they are moving deeper into the cave.
* During normal Go Deeper movement:

  * the player character should remain flipped in the same direction.
* When an ogre appears:

  * the player character should become unflipped.
  * the player should appear to turn around and run away.
* During the ogre chase:

  * the ogre should appear to the right of the player.
  * the player should face away from the ogre while being chased left.
  * the ogre and player should move left and fade out as already specified.

Actual Behaviour

* The player character is facing the wrong way.
* The flip behaviour is reversed between normal cave movement and the ogre chase.

Required Fix

* Reverse the current player sprite flip logic in the Cave Mini Game.
* Apply the flipped state during normal cave exploration.
* Remove the flipped state during the ogre chase.
* Ensure the movement transform and flip transform do not overwrite each other.
* If the component uses CSS transforms for both movement and flipping, use nested elements or CSS variables to prevent transform conflicts.

Suggested Implementation Direction

```jsx id="fb0w4c"
<div
  className="cave-player-position"
  style={{ transform: `translateX(${progressX}px)` }}
>
  <img
    className={`cave-player-image ${isOgreChase ? "is-unflipped" : "is-flipped"}`}
    src={playerImage}
    alt=""
  />
</div>
```

Suggested CSS Direction

```css id="m182ie"
.cave-player-image.is-flipped {
  transform: scaleX(-1);
}

.cave-player-image.is-unflipped {
  transform: scaleX(1);
}
```

* Exact class names can vary.
* The important requirement is:

  * flipped during normal cave movement.
  * unflipped during ogre chase.

Transform Safety Requirement

* Do not combine movement translation and image flipping on the same element if it causes one transform to override the other.
* Prefer:

  * outer wrapper handles position/movement.
  * inner image handles facing direction.
* This avoids bugs where `translateX(...)` replaces `scaleX(...)`, or `scaleX(...)` replaces `translateX(...)`.

Ogre Chase Requirement

* When the ogre appears:

  * the player image should unflip.
  * the ogre should still appear to the right of the player.
  * the chase animation should still move both images left.
  * both should still fade out once they reach the left.
* Do not break the existing ogre chase animation timing or routing.

Expected Behaviour After Fix

* The player character appears flipped at the start of the Cave Mini Game.
* The player character stays flipped while going deeper.
* The player character unflips when the ogre appears.
* The player character appears to run away from the ogre during the chase.
* The ogre chase animation still works.
* The player and ogre still fade out before navigating to the Mini Game Lose page.

Out Of Scope

* No changes to Cave Game probability logic.
* No changes to Cave Game rewards.
* No changes to Go Deeper or Retreat button logic.
* No changes to found rewards display.
* No changes to ogre image asset.
* No changes to Mini Game Lose page.
* No changes to turn flow.
* No unrelated Cave Game animation changes.

Suspected Cause

* The flip class is likely being applied only during the ogre chase, when it should be applied during normal cave movement instead.
* The sprite’s default facing direction is opposite of what Feature-011 assumed.

Attempted Fixes

* Feature-011 added the initial player movement and ogre chase animation.
* The facing direction now needs reversing.
* Added a focused regression that reproduced the reversal at initial depth, during movement, and during the ogre chase.
* Replaced the chase-only flip with explicit `is-flipped` exploration and `is-unflipped` chase classes on the nested player image.
* Kept all progress and chase positioning on the existing outer wrapper so the facing transform cannot overwrite movement.

Final Fix

* Normal Cave exploration now applies `scaleX(-1)` to the inner player image, including depth zero and every Go Deeper movement.
* Ogre chase switches the same inner image to `scaleX(1)` while the unchanged outer wrapper moves the player and ogre left and fades them out.
* Probability, rewards, buttons, chase timing, shared loss navigation, and turn flow were not changed.

Testing Required

* Confirm the player character is flipped at the start of the Cave Mini Game.
* Confirm the player character remains flipped while moving deeper.
* Confirm clicking Go Deeper does not reset the facing direction incorrectly.
* Confirm the player character unflips when an ogre appears.
* Confirm the player appears to face away from the ogre during the chase.
* Confirm the ogre appears to the right of the player.
* Confirm the chase animation still moves both images left.
* Confirm both images still fade out after reaching the left.
* Confirm the page still routes to the Mini Game Lose page after the chase.
* Confirm there are no transform conflicts between movement and flipping.
* Confirm there are no console errors.
* Update `testing.txt` with regression steps before marking this bug fixed.

---

Bug-007: Replace token and potion modal lists with div-based grid/flex layouts

Status

* fixed

Related Feature

* Feature-014: Grant Cave Game token and potion rewards, handle full bags/slots, and add Cave reward debug buttons

Bug Summary

* Token and potion replacement modals are currently using `ul` / `li` list markup in some places.
* Example area:

```txt id="pn44bb"
Current token bag
```

* The game should not use `ul` or `li` elements for these UI lists.
* Throughout the game, prefer `div` elements with CSS grid or flex layouts.
* This applies to this modal issue and should also be treated as a general UI rule going forward.

Repro Steps

* Fill the current player’s token bag.
* Trigger a Cave Game token reward.
* Retreat successfully.
* Open the modal that asks which token to replace or whether to discard the new token.
* Inspect the element around the current token bag list.
* Repeat the same flow for full potion slots.
* Confirm whether token/potion choices are rendered using `ul` or `li`.

Expected Behaviour

* Token replacement options should be rendered using `div` elements.
* Potion replacement options should be rendered using `div` elements.
* No `ul` elements should be used for token/potion replacement choices.
* No `li` elements should be used for token/potion replacement choices.
* Layout should use CSS grid or flex.
* The UI should remain visually neat and easy to use.
* Existing token and potion replacement behaviour should remain unchanged.

Actual Behaviour

* Some token/potion replacement modal content is rendered using `ul` / `li`.

Required Fix

* Replace `ul` and `li` markup in token and potion replacement modals with `div` elements.
* Update related CSS selectors so styling still applies.
* Use grid or flex layout for item arrangement.
* Ensure each token or potion remains selectable.
* Ensure replacement and discard actions still work correctly.
* Ensure accessibility labels are preserved or improved.
* Do not break `aria` attributes such as:

```txt id="pk4v6x"
Current token bag
```

* Preserve screen-reader meaning through accessible labels on the new `div` containers.

Suggested Structure

```jsx id="o36svg"
<div className="replacement-options" aria-label="Current token bag">
  <div className="replacement-option">
    <TokenIcon token={token} language={language} />
  </div>

  <div className="replacement-option">
    <TokenIcon token={token} language={language} />
  </div>
</div>
```

Potion equivalent:

```jsx id="m27vy2"
<div className="replacement-options" aria-label="Current potion slots">
  <div className="replacement-option">
    <PotionIcon potion={potion} language={language} />
  </div>

  <div className="replacement-option">
    <PotionIcon potion={potion} language={language} />
  </div>
</div>
```

Suggested CSS Direction

```css id="w846ee"
.replacement-options {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(86px, 1fr));
  gap: 12px;
  align-items: stretch;
}
```

Or:

```css id="zz1ued"
.replacement-options {
  display: flex;
  flex-wrap: wrap;
  gap: 12px;
  align-items: stretch;
}
```

* Exact class names can vary.
* Use whichever layout best fits the modal.
* The important requirement is no `ul` or `li`.

Global UI Markup Rule

* Going forward, avoid `ul` and `li` elements throughout the game UI.
* Prefer:

  * `div` containers.
  * CSS grid.
  * CSS flex.
* This applies to:

  * token lists.
  * potion lists.
  * reward lists.
  * replacement options.
  * found rewards displays.
  * modal item choices.
  * debug sections where practical.
  * future game UI lists.
* Do not introduce new `ul` / `li` markup unless there is a very specific reason and it is explicitly approved.

Accessibility Requirement

* Removing `ul` / `li` should not remove useful accessibility.
* Keep meaningful labels on grouped content.
* Use `aria-label` or visible headings where helpful.
* Interactive item choices should still be keyboard-accessible if they were before.
* Do not remove button semantics from clickable options.

Modal Coverage Requirement

* Apply this fix anywhere the full token bag / full potion slots replacement flow uses lists.
* This includes:

  * current token bag display.
  * current potion slots display.
  * choose token to remove modal.
  * choose potion to remove modal.
  * discard newly gained reward option if it is grouped with replacement options.
* Also update any closely related reward choice displays if they were copied from the same list pattern.

Out Of Scope

* No changes to reward granting rules.
* No changes to token bag capacity.
* No changes to potion capacity.
* No changes to token or potion data.
* No changes to TokenIcon.
* No changes to PotionIcon.
* No changes to Cave Game probability logic.
* No changes to roll-again reward behaviour.
* No unrelated modal redesign.

Suspected Cause

* The replacement modals may have been implemented using semantic list markup by default.
* This conflicts with the project’s preferred UI structure of div-based grid/flex layouts.

Attempted Fixes

* Reproduced the issue in `CaveRewardChoiceModal`: token and potion replacement choices share a `ul` container and render every option in an `li`.
* Added focused regressions requiring accessible div-based token and potion groups, no `ul` or `li` descendants, and a shared grid/flex layout class before changing the implementation.

Final Fix

* Replaced the shared token/potion replacement `ul` and each option `li` with `div` elements in `CaveRewardChoiceModal`.
* Kept the localized current-inventory label on the container and added `role="group"` so the div-based choices retain an accessible group name.
* Added one shared wrapping flex layout for token and potion choices without changing the existing buttons, icon components, replacement identifiers, discard actions, reward rules, or inventory capacities.

Testing Required

* Confirm token replacement modal does not render a `ul`.
* Confirm token replacement modal does not render any `li`.
* Confirm potion replacement modal does not render a `ul`.
* Confirm potion replacement modal does not render any `li`.
* Confirm the current token bag area uses divs.
* Confirm the current potion slots area uses divs.
* Confirm replacement options are arranged with grid or flex.
* Confirm selecting an existing token to remove still works.
* Confirm selecting an existing potion to remove still works.
* Confirm discarding the newly gained token still works.
* Confirm discarding the newly gained potion still works.
* Confirm aria labels or accessible group labels are preserved.
* Confirm token icons still display correctly.
* Confirm potion icons still display correctly.
* Confirm English text still uses the correct font.
* Confirm Japanese text still uses the correct font.
* Confirm there are no console errors.
* Update `testing.txt` with regression steps before marking this bug fixed.

---

Bug-008: Cave token rewards should use existing token assignment component instead of auto-adding to token bag

Status

* fixed

Related Feature

* Feature-012: Add Cave Mini Game reward generation, retreat reward handling, loot routing, and roll-again preservation
* Feature-014: Grant Cave Game token and potion rewards, handle full bags/slots, and add Cave reward debug buttons
* EPIC-007 Feature-003: Add battle reward generation and reward assignment flow
* EPIC-007 Bug-001: Reward token assignment should use shared Spells modal-style drag-and-drop component

Bug Summary

* Cave Game token rewards are currently planned to be added directly into the player’s token bag.
* However, the game already has a component that lets players assign newly gained tokens properly.
* This component is already used when a player gains tokens from winning battles.
* Cave Game token rewards should use that same token assignment component.
* Potions can remain handled as currently planned.

Expected Behaviour

* When a player successfully keeps a token reward from the Cave Game, the game should open the existing token reward assignment component.
* The player should be able to choose where the new token goes using the same flow as battle token rewards.
* The player should be able to place the token into:

  * a valid spell slot.
  * the token bag.
  * the discard area.
* The Cave Game should not simply auto-add token rewards to the token bag.
* Potion rewards should keep the existing Feature-014 behaviour:

  * auto-add to potion slots if there is room.
  * ask which potion to replace or whether to discard the new potion if full.

Required Fix

* Replace the Cave Game token auto-add behaviour with the existing token reward assignment flow.
* Reuse the same component currently used for battle token rewards.
* Do not create a separate Cave-only token assignment UI.
* Pass the generated Cave token into the shared assignment component as the new reward token.
* Preserve all existing token assignment rules:

  * assign to spell slot.
  * assign to token bag.
  * discard reward token.
  * handle full token bag.
  * preserve committed token rules.
  * preserve spell slot capacity rules.
* Keep Cave Game potion reward handling unchanged.

Token Reward Flow Requirement

* If the player retreats from the Cave Game with a generated token reward:

  * process that token through the existing token assignment component.
* The token assignment should happen before the final return-to-board flow completes.
* If the player also found a loot chest:

  * token assignment should happen before routing to the Loot Chest page.
* If the player also found a roll-again potion:

  * preserve that roll-again state while the token assignment flow is open.
  * after token assignment and any loot flow, the same player should remain active and receive the roll-again modal.
* If the player does not have roll again:

  * after token assignment and any loot flow, the turn should advance normally and show the next-turn modal.

Suggested Flow Without Loot Chest

```txt id="4pfrnl"
Cave retreat
Generated token reward exists
Open shared token assignment component
Player assigns or discards token
Process potion reward if any
Return to board
Apply roll-again or next-player behaviour
```

Suggested Flow With Loot Chest

```txt id="psuxym"
Cave retreat
Generated token reward exists
Open shared token assignment component
Player assigns or discards token
Process potion reward if any
Open Loot Chest page
After loot flow completes, apply roll-again or next-player behaviour
```

Shared Component Requirement

* Use the same token assignment component already used when gaining a token from battle rewards.
* The Cave token reward should behave like a normal gained token reward.
* Do not duplicate the component.
* Do not duplicate drag-and-drop logic.
* Do not create separate Cave token placement rules.
* Any future fixes to the shared assignment component should automatically benefit Cave token rewards too.

Reward State Requirement

* Cave reward state should retain the generated token until token assignment is complete.
* Do not lose the generated token during route changes, modals, or reward processing.
* Once assignment is complete:

  * apply the chosen token placement/discard result.
  * clear the pending Cave token reward from the processing queue.
* If the player encountered an ogre:

  * do not open token assignment.
  * the Cave token reward is lost.

Potion Reward Requirement

* Potion rewards should remain as described in Feature-014.
* Do not send potion rewards through the token assignment component.
* Potion rewards should:

  * auto-add to potion slots if there is room.
  * show a full potion slots replacement/discard modal if there is no room.
* This bug only changes Cave token reward handling.

Full Token Bag Requirement

* Full token bag handling should now be delegated to the existing token assignment component.
* Do not use the separate Cave token replacement modal if the shared assignment component already handles full token bag placement/discard.
* The player should be able to discard the new token if they do not want it.
* The player should be able to place the token into a valid spell slot if available.
* The player should be able to place it in the token bag if space exists or if the component supports replacement.

Translation Requirement

* Reuse all existing token assignment translations.
* Do not add new Cave-specific token assignment text unless absolutely necessary.
* The shared component should continue to support:

  * English.
  * Japanese.
  * correct language fonts.
* Any text-only modal triggered by this flow should still use `larger-text` on its main `<p>` tag.

No `ul` / `li` Requirement

* Preserve the global UI rule from Bug-007.
* Do not introduce `ul` or `li` elements in any new Cave token reward flow.
* Use `div` elements with flex or grid layouts.

Expected Behaviour After Fix

* Cave Game token rewards open the existing token assignment component.
* The player can assign the Cave token reward into a spell slot.
* The player can assign the Cave token reward into the token bag.
* The player can discard the Cave token reward.
* Cave Game no longer automatically puts token rewards into the token bag.
* Potion rewards still use the existing potion slot logic.
* Loot chest routing still works after token assignment.
* Roll-again state is preserved after token assignment.
* Turn flow remains correct after all reward processing.

Out Of Scope

* No changes to Cave Game probability table.
* No changes to token rarity odds.
* No changes to potion rarity odds.
* No changes to potion reward handling except preserving existing behaviour.
* No changes to battle token reward assignment except reusing it.
* No changes to token effects.
* No changes to spell slot capacity.
* No changes to actual loot chest reward generation.
* No unrelated reward UI redesign.

Suspected Cause

* Feature-014 planned Cave token rewards as an inventory add/replacement flow.
* The existing battle token reward assignment component is a better fit because it already handles new token placement.
* Reusing it will reduce duplicated token reward logic.

Attempted Fixes

* Feature-014 added Cave reward granting rules.
* This bug updates the token reward part of that flow before implementation is finalised.
* Reproduced the current direct token-bag mutation in `createCaveRewardGrant` and the separate full-bag token modal in the real Cave/App flow.
* Added failing regressions requiring a pending Cave token assignment, the existing reward-assignment contract, and assignment completion before potion, loot, roll-again, or turn return.

Final Fix

* Changed Cave token grants from direct bag insertion/full-bag replacement to a `pendingAssignment` transaction that preserves the generated token item without mutating inventory.
* Reused the existing `RewardPage`, `SpellTokenAssignment`, reward drop helpers, and shared token mutation methods through a Cave-tagged reward assignment; no Cave-specific assignment component, route, drag logic, translation, or styling was added.
* Shared assignment now records Cave token placement to a spell slot, token bag, full-bag replacement, or discard before clearing the pending transaction.
* Returning from assignment restores the persisted Cave retreat summary, then keeps the existing potion replacement, Loot Chest, roll-again, and next-player flows in their required order.
* Removed the Cave token replacement resolver from active use; the Cave choice modal now receives potion choices only, while ogre losses still create no assignment.

Testing Required

* Confirm Cave Game token reward no longer auto-adds directly to the token bag.
* Confirm retreating with a Cave token reward opens the shared token assignment component.
* Confirm the Cave token shown in assignment is the generated token.
* Confirm the player can place the Cave token into a spell slot.
* Confirm the player can place the Cave token into the token bag.
* Confirm the player can discard the Cave token.
* Confirm full token bag handling works through the shared assignment component.
* Confirm Cave token assignment uses English for English players.
* Confirm Cave token assignment uses Japanese for Japanese players.
* Confirm correct fonts are applied.
* Confirm no new `ul` or `li` elements are introduced.
* Confirm potion rewards still auto-add to potion slots if there is room.
* Confirm full potion slots still use the potion replacement/discard modal.
* Confirm token assignment happens before routing to Loot Chest if loot chest was found.
* Confirm roll-again state is preserved while token assignment is open.
* Confirm retreat with roll again and no loot returns to the board with the same player.
* Confirm retreat with loot and roll again preserves same-player behaviour through Loot Chest.
* Confirm retreat without roll again advances to the next player after reward processing.
* Confirm the next-turn modal appears after any player-to-player turn change.
* Confirm ogre loss does not open token assignment.
* Confirm there are no console errors.
* Update `testing.txt` with regression steps before marking this bug fixed.

---

Bug-009: Cave token assignment should use cave background

Status

* fixed

Related Feature

* Feature-010: Add Cave Mini Game with debug trigger, reward tracking, retreat flow, and roll-again reward
* Feature-012: Add Cave Mini Game reward generation, retreat reward handling, loot routing, and roll-again preservation
* Feature-014: Grant Cave Game token and potion rewards, handle full bags/slots, and add Cave reward debug buttons
* Bug-008: Cave token rewards should use existing token assignment component instead of auto-adding to token bag

Bug Summary

* When the player gains a token from the Cave Game and is taken to the token assignment component, the background should remain the cave background.
* The token assignment flow should visually feel like it belongs to the Cave Game reward sequence.
* It should not show the normal gameplay background, battle background, reward background, or a blank/default background.

Expected Behaviour

* If the token assignment component is opened because of a Cave Game token reward:

  * use the Cave Game background.
  * keep the token assignment UI/modal on top of that background.
  * preserve the existing token assignment functionality.
* Cave reward token assignment should use:

```txt id="wvhgen"
src/images/miniGames/cave.png
```

* This should only affect token assignment when the source is the Cave Game.

Actual Behaviour

* The token assignment component does not use the cave background when opened after the Cave Game.

Required Fix

* Pass source/background context into the shared token assignment flow.
* When the token assignment source is Cave Game, render the page/background using the cave background image.
* Do not hard-code the cave background for all token assignment flows.
* Battle token rewards should keep their existing battle/reward background behaviour.
* Normal Spells modal usage from the gameplay page should keep its existing gameplay/modal behaviour.

Suggested Source Metadata

```js id="yk1ltk"
{
  source: "cave",
  backgroundImage: "src/images/miniGames/cave.png"
}
```

* Exact implementation can vary.
* The important requirement is that the shared assignment component can visually adapt based on where the reward came from.

Background Requirement

* Cave token assignment background image:

```txt id="n9jbgy"
src/images/miniGames/cave.png
```

* The image should:

  * cover the screen.
  * not tile.
  * remain behind the token assignment UI.
  * preserve readability of the token assignment modal/content.
* The token assignment modal itself should remain usable and readable.

Shared Component Requirement

* Continue using the existing token assignment component from battle rewards.
* Do not create a separate Cave-only token assignment component.
* The shared component should support receiving background/source context.
* Cave-specific visual background should not duplicate token assignment logic.

Flow Requirement

* Cave Game retreat with token reward should proceed like this:

```txt id="6n3nve"
Cave Game retreat
Token reward exists
Open shared token assignment component
Use cave background behind assignment UI
Player assigns or discards token
Continue normal Cave reward flow
```

* After token assignment is complete:

  * process potion reward if present.
  * route to Loot Chest page if loot chest was found.
  * otherwise return to the board.
  * preserve roll-again or next-turn behaviour as already specified.

Scope Restriction

* This bug only changes the background used during Cave-sourced token assignment.
* Do not change the token assignment rules.
* Do not change token drag-and-drop behaviour.
* Do not change token rarity generation.
* Do not change potion handling.
* Do not change loot chest routing.
* Do not change roll-again behaviour.

Expected Behaviour After Fix

* Gaining a token from the Cave Game opens the existing token assignment flow.
* The assignment screen uses the cave background.
* The player can still place the token into a spell slot.
* The player can still place the token into the token bag.
* The player can still discard the token.
* After assignment, the Cave reward flow continues correctly.
* Other token assignment sources are visually unchanged.

Out Of Scope

* No new Cave-only token assignment UI.
* No changes to normal Spells modal background.
* No changes to battle reward token assignment background unless it is currently broken.
* No changes to Cave Game probability logic.
* No changes to generated token rarity logic.
* No changes to potion reward handling.
* No changes to Loot Chest page.
* No unrelated reward screen redesign.

Suspected Cause

* The shared token assignment component may be defaulting to gameplay or reward page background.
* The Cave Game source may not be passing background context into the assignment flow.
* The assignment component may not yet support source-specific backgrounds.

Attempted Fixes

* Bug-008 changed Cave token rewards to use the existing token assignment component.
* This bug adds the Cave-specific visual background requirement for that flow.
* Reproduced that Cave reward assignment already carries `source: 'cave'` but `RewardPage` ignores it and resolves the synthetic `fields` environment through the battle background helper.
* Added a failing RewardPage regression requiring Cave-sourced assignment to use `src/images/miniGames/cave.png` while preserving the existing battle background assertion.

Final Fix

* Reused the existing `source: 'cave'` assignment metadata to select the imported `src/images/miniGames/cave.png` asset in `RewardPage`.
* Preserved `getBattleBackgroundSource` for every non-Cave reward, so battle environments and invalid-environment fallback remain unchanged.
* Reused the existing `.reward-page` background positioning, no-repeat, cover, full-width, and full-height rules; no assignment component, route, flow logic, or new styling was added.

Testing Required

* Confirm retreating from Cave Game with a token reward opens the token assignment component.
* Confirm the token assignment screen uses `src/images/miniGames/cave.png` as the background.
* Confirm the cave background covers the screen.
* Confirm the cave background does not tile.
* Confirm the token assignment UI remains readable.
* Confirm the token can still be assigned to a spell slot.
* Confirm the token can still be assigned to the token bag.
* Confirm the token can still be discarded.
* Confirm completing token assignment continues the Cave reward flow.
* Confirm potion reward handling still works after token assignment.
* Confirm loot chest routing still works after token assignment.
* Confirm roll-again state is preserved after token assignment.
* Confirm battle token reward assignment does not incorrectly use the cave background.
* Confirm normal gameplay Spells modal does not incorrectly use the cave background.
* Confirm there are no console errors.
* Update `testing.txt` with regression steps before marking this bug fixed.


---

Bug-009: Cave token assignment should use cave background

Status

* not started

Related Feature

* Feature-010: Add Cave Mini Game with debug trigger, reward tracking, retreat flow, and roll-again reward
* Feature-012: Add Cave Mini Game reward generation, retreat reward handling, loot routing, and roll-again preservation
* Feature-014: Grant Cave Game token and potion rewards, handle full bags/slots, and add Cave reward debug buttons
* Bug-008: Cave token rewards should use existing token assignment component instead of auto-adding to token bag

Bug Summary

* When the player gains a token from the Cave Game and is taken to the token assignment component, the background should remain the cave background.
* The token assignment flow should visually feel like it belongs to the Cave Game reward sequence.
* It should not show the normal gameplay background, battle background, reward background, or a blank/default background.

Expected Behaviour

* If the token assignment component is opened because of a Cave Game token reward:

  * use the Cave Game background.
  * keep the token assignment UI/modal on top of that background.
  * preserve the existing token assignment functionality.
* Cave reward token assignment should use:

```txt id="wvhgen"
src/images/miniGames/cave.png
```

* This should only affect token assignment when the source is the Cave Game.

Actual Behaviour

* The token assignment component does not use the cave background when opened after the Cave Game.

Required Fix

* Pass source/background context into the shared token assignment flow.
* When the token assignment source is Cave Game, render the page/background using the cave background image.
* Do not hard-code the cave background for all token assignment flows.
* Battle token rewards should keep their existing battle/reward background behaviour.
* Normal Spells modal usage from the gameplay page should keep its existing gameplay/modal behaviour.

Suggested Source Metadata

```js id="yk1ltk"
{
  source: "cave",
  backgroundImage: "src/images/miniGames/cave.png"
}
```

* Exact implementation can vary.
* The important requirement is that the shared assignment component can visually adapt based on where the reward came from.

Background Requirement

* Cave token assignment background image:

```txt id="n9jbgy"
src/images/miniGames/cave.png
```

* The image should:

  * cover the screen.
  * not tile.
  * remain behind the token assignment UI.
  * preserve readability of the token assignment modal/content.
* The token assignment modal itself should remain usable and readable.

Shared Component Requirement

* Continue using the existing token assignment component from battle rewards.
* Do not create a separate Cave-only token assignment component.
* The shared component should support receiving background/source context.
* Cave-specific visual background should not duplicate token assignment logic.

Flow Requirement

* Cave Game retreat with token reward should proceed like this:

```txt id="6n3nve"
Cave Game retreat
Token reward exists
Open shared token assignment component
Use cave background behind assignment UI
Player assigns or discards token
Continue normal Cave reward flow
```

* After token assignment is complete:

  * process potion reward if present.
  * route to Loot Chest page if loot chest was found.
  * otherwise return to the board.
  * preserve roll-again or next-turn behaviour as already specified.

Scope Restriction

* This bug only changes the background used during Cave-sourced token assignment.
* Do not change the token assignment rules.
* Do not change token drag-and-drop behaviour.
* Do not change token rarity generation.
* Do not change potion handling.
* Do not change loot chest routing.
* Do not change roll-again behaviour.

Expected Behaviour After Fix

* Gaining a token from the Cave Game opens the existing token assignment flow.
* The assignment screen uses the cave background.
* The player can still place the token into a spell slot.
* The player can still place the token into the token bag.
* The player can still discard the token.
* After assignment, the Cave reward flow continues correctly.
* Other token assignment sources are visually unchanged.

Out Of Scope

* No new Cave-only token assignment UI.
* No changes to normal Spells modal background.
* No changes to battle reward token assignment background unless it is currently broken.
* No changes to Cave Game probability logic.
* No changes to generated token rarity logic.
* No changes to potion reward handling.
* No changes to Loot Chest page.
* No unrelated reward screen redesign.

Suspected Cause

* The shared token assignment component may be defaulting to gameplay or reward page background.
* The Cave Game source may not be passing background context into the assignment flow.
* The assignment component may not yet support source-specific backgrounds.

Attempted Fixes

* Bug-008 changed Cave token rewards to use the existing token assignment component.
* This bug adds the Cave-specific visual background requirement for that flow.

Final Fix

* Not fixed yet.

Testing Required

* Confirm retreating from Cave Game with a token reward opens the token assignment component.
* Confirm the token assignment screen uses `src/images/miniGames/cave.png` as the background.
* Confirm the cave background covers the screen.
* Confirm the cave background does not tile.
* Confirm the token assignment UI remains readable.
* Confirm the token can still be assigned to a spell slot.
* Confirm the token can still be assigned to the token bag.
* Confirm the token can still be discarded.
* Confirm completing token assignment continues the Cave reward flow.
* Confirm potion reward handling still works after token assignment.
* Confirm loot chest routing still works after token assignment.
* Confirm roll-again state is preserved after token assignment.
* Confirm battle token reward assignment does not incorrectly use the cave background.
* Confirm normal gameplay Spells modal does not incorrectly use the cave background.
* Confirm there are no console errors.
* Update `testing.txt` with regression steps before marking this bug fixed.
