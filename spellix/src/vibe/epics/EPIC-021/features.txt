---

Epic-021: Music system and Nothing event healing

Status

* in progress

Epic Goal

* Add persistent background music to the game.
* Give the player basic music controls through the settings menu.
* Update Nothing events so they provide a small healing benefit instead of only advancing the turn.

Global Implementation Notes

* Preserve the existing modal, font, and language rules.
* Do not use `ul` or `li` in new UI.
* Use `div` with flex/grid for new sections.
* Text-only modal paragraphs should use `larger-text`.
* Any turn advancement must still preserve the next-turn modal invariant.
* Do not break settings menu visibility rules from previous epics.
* Music should persist across route/page changes if possible.

---

---

Feature-001: Add persistent random background music with global floating toggle button

Status

* done

Related Feature(s)

* EPIC-018 Feature-012: Only show settings button on gameplay page
* EPIC-020 Feature-001: Make gameplay layout scale to fit screen height
* EPIC-020 Feature-014: Add rules modal inside settings menu
* App shell / routing / global UI controls

Goal

* Add background music to the game.
* The user has added 10 songs into:

```txt id="w9zw6u"
src/music/
```

* Music should automatically start if possible.
* Music should continue regardless of what page the player is on.
* Add a floating music toggle button in the top-left corner of the screen.
* The music toggle button should always be visible on every page.
* The music toggle button should use Font Awesome icons to show whether music is on or off.
* Songs should play in a random order.
* Every song should play once before any song repeats.
* While adding the music toggle button, update the settings button to use the same visual styling.

Important Change From Previous Version

* Do not add the music controls inside the settings menu.
* The music control should now be a global floating button.
* The button should always be present, even on pages where the settings button is hidden.

Browser Autoplay Note

* Modern browsers often block audio autoplay until the player interacts with the page.
* The game should still attempt to start music automatically.
* If autoplay is blocked:

  * do not crash.
  * do not show an app-level error.
  * wait for the first user interaction.
  * then start music automatically unless the player has toggled music off.

Required Music Behaviour

* Music should start automatically when the game opens if the browser allows it.
* If autoplay is blocked, music should start after the first user interaction.
* Music should continue across page changes.
* Music should not restart from the beginning every time the route changes.
* Music should not create multiple overlapping audio instances.
* The player should be able to toggle music on and off from the floating music button.
* If the player turns music off manually, music should stay off until they turn it on again.
* If the current song ends, the next song should start automatically.

Persistent Music Requirement

* The music player should live at the app shell/context level.

* It should not be mounted inside individual pages such as:

  * start page.
  * game setup.
  * story page.
  * gameplay.
  * battle.
  * rules.
  * settings rules modal.
  * village.
  * cave.
  * river.
  * decision.
  * loot chest.
  * winner page.

* Route changes should not unmount the music system.

* Route changes should not reset music state.

Suggested Architecture

```txt id="tky38n"
App
MusicProvider
GlobalFloatingControls
Router
Pages
```

Suggested React structure:

```jsx id="za7j6t"
<MusicProvider>
  <GlobalFloatingControls />
  <Router>
    <AppRoutes />
  </Router>
</MusicProvider>
```

* Exact structure can vary.
* The important rule is that both the music state and audio element survive route changes.

Music Files Requirement

* Load all 10 songs from `src/music/`.
* Store them in one music track list.
* The track list should be the source of truth for available songs.
* If the project uses Vite, `import.meta.glob` can be used.
* If explicit imports are easier, use explicit imports.

Suggested explicit import direction:

```js id="wsc6fj"
import song1 from "../music/song1.mp3";
import song2 from "../music/song2.mp3";
import song3 from "../music/song3.mp3";

export const musicTracks = [
  song1,
  song2,
  song3
];
```

Suggested Vite glob direction:

```js id="l7tbf7"
const musicModules = import.meta.glob("../music/*", {
  eager: true,
  query: "?url",
  import: "default"
});

export const musicTracks = Object.values(musicModules);
```

* Use the approach that fits the project setup.
* Do not hard-code only one song.
* Do not rely on the browser automatically discovering files from the folder without importing them.
* If track order from glob is inconsistent, that is acceptable because the game shuffles tracks anyway.

Random Order Requirement

* Songs should play in a random order.
* Every song should play once before any song repeats.
* Use a shuffled bag/queue.
* When the queue is empty, create a new shuffled queue from all songs.

Correct Shuffle Behaviour

```txt id="fy8elh"
1. Start with all 10 songs.
2. Shuffle them.
3. Play each song once.
4. When all 10 have played, create a new shuffled list.
5. Start the next cycle.
```

* A song should not appear twice within the same 10-song cycle.
* The next cycle does not need to use the same order as the previous cycle.
* If possible, avoid making the first song of the new cycle the same as the final song of the previous cycle.
* If avoiding that is awkward, it is acceptable as long as each full cycle still contains all 10 songs once.

Example Valid Cycle

```txt id="fayl1z"
Song 2
Song 4
Song 7
Song 10
Song 1
Song 3
Song 9
Song 8
Song 6
Song 5
```

Example Next Valid Cycle

```txt id="bgddqt"
Song 4
Song 10
Song 3
Song 2
Song 7
Song 8
Song 5
Song 1
Song 6
Song 9
```

Important

* The examples are illustrative of random order.
* The implementation rule is stricter:

  * all 10 songs should play once before repeats.
  * no duplicate songs inside a single 10-song cycle.

Suggested Shuffle Helper

```js id="x7wg73"
function shuffleTracks(tracks) {
  const shuffled = [...tracks];

  for (let index = shuffled.length - 1; index > 0; index -= 1) {
    const randomIndex = Math.floor(Math.random() * (index + 1));
    [shuffled[index], shuffled[randomIndex]] = [
      shuffled[randomIndex],
      shuffled[index]
    ];
  }

  return shuffled;
}
```

Suggested Music State

```js id="cb9s7b"
const musicState = {
  tracks,
  queue,
  currentTrack,
  isMusicEnabled,
  isPlaying,
  autoplayBlocked,
  hasInitialPlayAttemptHappened,
  hasUserInteracted
};
```

Playback Flow

```txt id="mdbaop"
1. App loads.
2. MusicProvider creates a shuffled queue from all songs.
3. MusicProvider selects the first track.
4. MusicProvider attempts to play.
5. If play succeeds, music continues.
6. If play is blocked, wait for first user interaction.
7. If music is enabled, try again after the first user interaction.
8. When a song ends, select the next song in the queue.
9. When the queue is empty, create a new shuffled queue.
10. Continue until the player turns music off.
```

Autoplay Block Handling

* Attempt to call `audio.play()`.
* If the promise rejects:

  * set `autoplayBlocked` to true.
  * wait for a click/touch/keydown interaction.
* On first interaction:

  * try to play again.
  * only do this if music is still enabled.
* Remove first-interaction listeners after successful playback or once no longer needed.

Suggested direction:

```js id="cixncn"
audio.play().catch(() => {
  setAutoplayBlocked(true);
});
```

* Do not repeatedly spam play attempts every render.
* Do not create an infinite loop of failed autoplay attempts.

Floating Music Button Requirement

* Add a floating button in the top-left corner of the screen.
* This button should always be present.
* It should appear on every page, including:

  * start page.
  * game setup.
  * story page.
  * rules page.
  * gameplay.
  * battle.
  * village.
  * cave.
  * river.
  * decision.
  * loot chest.
  * settings modal.
  * winner page.

Position Requirement

* The button should be fixed to the viewport.
* It should be:

```txt id="pi9imk"
top: 10px
left: 10px
```

* It should remain in the same place regardless of route/page.
* It should not move when the gameplay page scales.
* It should sit above normal page content.

Suggested CSS:

```css id="jgxuh9"
.music-toggle-button {
  position: fixed;
  top: 10px;
  left: 10px;
  z-index: 9999;
}
```

Icon Requirement

* Use Font Awesome icons.
* When music is enabled/playing, show:

```txt id="d3vowd"
faVolume
```

* When music is disabled/paused, show:

```txt id="n3blm5"
faVolumeXmark
```

* The icon should visually communicate the current toggle state.
* Icon size should be:

```txt id="l5j1hi"
30px x 30px
```

Visual Styling Requirement

* Music toggle button should have:

  * brown background.
  * yellow icon.
  * basic hover effect.
  * no text label required.
  * consistent styling with current game UI.
* The button should look like a small floating game control.

Suggested CSS:

```css id="paf0jh"
.floating-icon-button {
  position: fixed;
  width: 50px;
  height: 50px;
  background: var(--modal-brown);
  color: var(--modal-yellow);
  border: 2px solid var(--modal-yellow);
  border-radius: 10px;
  display: flex;
  align-items: center;
  justify-content: center;
  cursor: pointer;
  z-index: 9999;
  transition: transform 0.15s ease, filter 0.15s ease;
}

.floating-icon-button svg {
  width: 30px;
  height: 30px;
}

.floating-icon-button:hover {
  transform: scale(1.08);
  filter: brightness(1.1);
}
```

* Use the project’s actual brown/yellow variables if they exist.
* Do not introduce a colour that clashes with existing modal/button styling.

Music Button Accessibility Requirement

* Add an accessible label.
* The label should change depending on state.

English examples:

```txt id="i6vzhn"
Turn music off
Turn music on
```

Japanese examples:

```txt id="h3dhwj"
音楽をオフにする
音楽をオンにする
```

* Use `aria-label`.
* The visible UI can remain icon-only.

Toggle Behaviour

* Clicking the button when music is on:

  * pauses the current track.
  * keeps the current playback position.
  * sets music enabled state to false.
  * changes icon to `faVolumeXmark`.

* Clicking the button when music is off:

  * sets music enabled state to true.
  * resumes the current track from its paused position if possible.
  * if no current track exists, starts the next track in the queue.
  * changes icon to `faVolume`.

Song End Behaviour

* When the current song ends:

  * move to the next track in the shuffled queue.
  * play it automatically if music is enabled.
* If the queue is empty:

  * generate a new shuffled queue.
  * start the first track in the new queue.

Settings Button Styling Requirement

* While adding the music toggle button, update the existing settings button to use the exact same visual styling.
* The settings button should remain on the right-hand side of the screen when it is visible.
* It should use:

  * brown background.
  * yellow icon.
  * icon size `30px x 30px`.
  * same button size as music button.
  * same border radius.
  * same hover effect.
  * same fixed positioning style.

Settings Button Position Requirement

* Settings button should remain on the right-hand side of the screen.
* When visible, it should be:

```txt id="vdpu0d"
top: 10px
right: 10px
```

Suggested CSS:

```css id="iijsw6"
.settings-floating-button {
  position: fixed;
  top: 10px;
  right: 10px;
  z-index: 9999;
}
```

* The settings button should keep its existing visibility rules.
* If settings is only visible on gameplay, keep it only visible on gameplay.
* Do not make the settings button visible on every page unless another feature explicitly says to.

Shared Button Class Requirement

* Music button and settings button should share a base class.

Suggested structure:

```jsx id="ill98v"
<button className="floating-icon-button music-toggle-button">
  <FontAwesomeIcon icon={isMusicEnabled ? faVolume : faVolumeXmark} />
</button>

{showSettingsButton && (
  <button className="floating-icon-button settings-floating-button">
    <FontAwesomeIcon icon={faGear} />
  </button>
)}
```

* Use the actual settings icon currently used by the project.
* Do not change settings button behaviour.
* Only update its styling and right-side positioning.

Z-Index Requirement

* The floating music button should remain visible above page content.
* It should not be hidden behind:

  * starry background.
  * gameplay board.
  * battle page.
  * village page.
  * modal backgrounds.
* If a blocking modal is open, the music button may remain clickable unless it causes modal interaction problems.
* If the project intentionally blocks all background controls behind modal overlays, document the decision. Default preference is that the music toggle remains available.

Volume Requirement

* No volume slider is required in this feature.
* Use a sensible default volume.
* If the project already has a default audio volume constant, use it.

Error Handling

* If one music file fails to load:

  * skip that track.
  * continue with the next track if possible.
  * log a developer warning.
* If no tracks can be loaded:

  * keep the button visible but disabled, or hide it safely.
  * do not crash the app.

Out Of Scope

* No settings menu music section.
* No volume slider.
* No track title display.
* No skip button.
* No playlist menu.
* No mute icon in settings.
* No separate battle music.
* No sound effects.
* No music selection menu.
* No saving music preference between browser sessions unless simple localStorage is already used and safe.
* No change to settings button visibility rules.
* No gameplay mechanic changes.

Testing Checkpoints

* Confirm all 10 songs are loaded from `src/music/`.
* Open the game and confirm music attempts to start.
* If browser autoplay blocks music, click anywhere and confirm music starts.
* Confirm the floating music button appears on the start page.
* Confirm the floating music button appears on game setup.
* Confirm the floating music button appears on gameplay.
* Confirm the floating music button appears on battle pages.
* Confirm the floating music button appears on village pages.
* Confirm the floating music button appears on rules page.
* Confirm the button is top-left, 10px from top and left.
* Confirm the icon is yellow on a brown background.
* Confirm the icon is 30px x 30px.
* Confirm the hover effect works.
* Confirm `faVolume` appears when music is on.
* Confirm `faVolumeXmark` appears when music is off.
* Click the button while music is playing.
* Confirm music pauses.
* Confirm icon changes to `faVolumeXmark`.
* Navigate pages while music is paused.
* Confirm music stays paused.
* Click the button again.
* Confirm music resumes.
* Confirm icon changes to `faVolume`.
* Navigate between pages while music is playing.
* Confirm music continues and does not restart.
* Confirm multiple songs do not overlap.
* Let a song finish.
* Confirm the next song starts automatically.
* Confirm songs play in shuffled order.
* Confirm no song repeats within one full 10-song cycle.
* Confirm a new shuffled cycle starts after all songs have played.
* Confirm settings button uses the same visual styling.
* Confirm settings button remains top-right when visible.
* Confirm settings button visibility rules are unchanged.
* Confirm there are no console errors.
* Update `testing.txt` before marking this feature done.

Implementation Notes

* Added one app-shell music player and audio element before the route tree so playback state and the current track survive route changes without overlapping players.
* Explicitly imported all 10 MP3 files into one track list and used a Fisher-Yates shuffled queue that exhausts each cycle before rebuilding, while avoiding an immediate cycle-boundary repeat when possible.
* Attempted playback on load, retried blocked autoplay after click, touch, or keyboard interaction, and preserved the current track position when toggled off and back on.
* Added the always-visible bilingual top-left volume toggle with the requested Font Awesome icons, dimensions, colours, hover behavior, and inaccessible-track disabled state.
* Reused the same floating-button base style for the existing settings button at the requested top-right position without changing its gameplay-only visibility or behavior.
* Used a default volume of `0.35`; browser-session persistence remains in app state and no localStorage, settings music section, volume slider, skip control, or other out-of-scope music UI was added.
* Failed tracks log a developer warning and are removed from the available queue; exhausting all tracks disables the music control without crashing the app.


---

Feature-002: Nothing events heal the player by 5 or 10 HP using village heal presentation

Status

* done

Related Feature(s)

* EPIC-016 Feature-006: Add Nothing events for environment landings
* EPIC-016 Feature-008: Trigger board events based on landed environment
* EPIC-020 Feature-006: Standardise health bar text colour and font
* EPIC-020 Feature-011: Add village action choice page
* EPIC-020 Feature-012: Implement Wandsmith village option

Goal

* Update Nothing events so they heal the player a small amount.
* A Nothing event should randomly heal the player by either `5 HP` or `10 HP`.
* The heal should be shown in the same visual style and flow as the existing village heal screen.
* Nothing events should still keep their existing flavour text and environment background.

Required Behaviour

* When the player receives a Nothing result after landing on a square:

  * show the existing Nothing event modal/page.
  * use the landed environment background.
  * show the correct Nothing event flavour text.
  * show the player image and health bar in the same layout/style as the village heal screen.
  * randomly choose a heal amount of either `5` or `10`.
  * apply the heal to the current player.
  * clamp healing to the player’s max HP.
  * animate/update the health bar in the same way the village heal screen does.
  * enable Continue after the heal presentation has completed.
  * Continue returns to the board and advances to the next player.

Heal Amount Requirement

* The heal amount should be randomly selected from:

```txt id="83f92a"
5
10
```

* Both values should have equal chance.

Suggested implementation direction:

```js id="8b48qc"
const healAmount = Math.random() < 0.5 ? 5 : 10;
```

Health Clamp Requirement

* Healing must not exceed the player’s current max HP.
* Use the player’s actual max HP, not a hard-coded value.
* Max HP may be above 100 because of Health or Shiny Health tokens.

Example:

```txt id="ajv1vi"
Current HP: 96
Max HP: 100
Heal roll: 10
Actual heal: 4
Final HP: 100
```

* The displayed health bar should end at the actual final HP.

Village Heal Presentation Requirement

* The Nothing heal should visually match the village heal screen.
* Reuse the village heal component/helper if possible.
* The player should see:

  * player image.
  * health bar.
  * health increasing after a short delay.
  * Continue button disabled until the heal has resolved.

Suggested flow:

```txt id="h89oyy"
1. Nothing event opens.
2. Player image and current health bar are shown.
3. Continue is disabled.
4. Wait using the same timing as the village heal screen.
5. Apply the random heal amount.
6. Health bar updates using the same village heal presentation.
7. Continue becomes enabled.
8. Continue returns to board and advances the turn.
```

* Do not create a completely different healing UI for Nothing events.
* The player should immediately recognise it as the same style of healing feedback used in villages.

Nothing Text Requirement

* Preserve the existing Nothing flavour text.
* Existing texts remain:

```txt id="gzlqff"
Field: You rest in an open field / 広々とした野原で休みます
Hills: You stop on a hilltop / 丘の頂上で足を止めます
Gravel: You sit on a boulder to rest / 大きな岩に腰掛けて休みます
Mud: You take a moment to wash your boots / 少し立ち止まってブーツの泥を洗い落とします
Stream: You sit by the stream for a while / 小川のそばに座って、しばらく休みます
```

* If Nothing events are later available for additional environments, use the same healing behaviour for them too.

Health Bar Styling Requirement

* The health bar text must follow EPIC-020 Feature-006:

  * black text.
  * `Unkempt` font.
  * applies in English and Japanese.

Continue Button Requirement

English:

```txt id="osp04w"
Continue
```

Japanese:

```txt id="2fbtlz"
続ける
```

* Continue should be disabled until the healing presentation has completed.
* Clicking Continue should:

  * return to board.
  * advance to next player.
  * show the next-turn modal first.

State Requirement

* Update the player’s real game-state health.
* Do not only update the modal’s local display value.
* The healed HP should persist after returning to the board.
* If the player enters battle later, the healed HP should be used.

Full Health Requirement

* If the player is already at full health:

  * do not exceed max HP.
  * still show the Nothing event and village-style heal presentation.
  * health bar remains full.
  * Continue becomes enabled after the normal short presentation delay.

Debug Requirement

* Nothing events triggered through debug should use the same healing behaviour.
* Nothing events triggered through normal landing event selection should use the same healing behaviour.

Turn Requirement

* Nothing events still consume the player’s landing event.
* After the Nothing heal resolves and Continue is clicked:

  * return to board.
  * advance to next player.
  * preserve the next-turn modal invariant.

Out Of Scope

* No change to Nothing event probability.
* No new Nothing event texts.
* No heal values other than 5 or 10.
* No overhealing above max HP.
* No change to village Rest healing.
* No potion interaction.
* No new separate heal animation if the village heal presentation can be reused.

Testing Checkpoints

* Trigger a Nothing event from debug.
* Confirm the correct environment background appears.
* Confirm the correct Nothing flavour text appears.
* Confirm player image appears.
* Confirm health bar appears in the same style as village healing.
* Confirm Continue is disabled before the heal resolves.
* Confirm the heal amount is either 5 or 10.
* Confirm the health bar updates like the village heal screen.
* Confirm health does not exceed max HP.
* Trigger Nothing at 96 / 100 HP with a 10 HP heal.
* Confirm final HP becomes 100.
* Trigger Nothing at full health.
* Confirm the modal still resolves safely.
* Click Continue.
* Confirm game returns to board.
* Confirm turn advances.
* Confirm next-turn modal appears first.
* Trigger Nothing through normal event selection.
* Confirm the same healing behaviour happens.
* Confirm healed HP persists after returning to board.
* Confirm health bar text is black and uses Unkempt.
* Confirm English and Japanese text display correctly.
* Confirm there are no console errors.
* Update `testing.txt` before marking this feature done.

Implementation Notes

* Added an isolated equal-chance Nothing-event heal selector that returns only `5` or `10` and can receive a deterministic random function in tests.
* Kept both debug-triggered and normal board-landing Nothing events on the existing shared `/nothing-event` route, preserving every environment background and bilingual flavour string.
* Reused the existing player piece-image resolver and shared `HealthBar`, including its black Unkempt text and width transition, with the same 1-second delay used by village Rest healing.
* Applied the selected heal to the encounter player's real context state and clamped it against that player's actual `maxHealth`, including token-increased maximum health and already-full players.
* Disabled Continue until the delayed health update resolves, then preserved the existing gameplay return, next-player advancement, and pending next-turn modal flow.
* Made no changes to Nothing-event probability, village Rest, potions, event text, routes, or other gameplay mechanics.

---

Feature-003: Update SOS potion data descriptions

Status

* done

Related Feature(s)

* EPIC-012 Feature-003: Potion data source of truth
* EPIC-021 Feature-001: Add persistent random background music
* EPIC-020 Feature-007: Implement SOS potion

Goal

* Update the SOS potion data description in both English and Japanese.
* SOS has now been implemented.
* The description should match the new behaviour: teleporting to the nearest village.

Updated SOS Potion Entry

```txt id="vyi8d8"
Feature?: Y
Implemented?: Y
Name: SOS
Japanese Name: SOS
Battle/Board: Board
Rarity: Common
Description: Teleport to the nearest village
Japanese Description: 最寄りの村にテレポートする
Colour: Light Blue
```

Required Data Change

* Replace the old SOS description if it still says:

```txt id="5c3fju"
Teleport to the field village
```

* Replace it with:

```txt id="w8qcri"
Teleport to the nearest village
```

* Replace the old Japanese description if it still says:

```txt id="vfqnkm"
フィールドの村にテレポートする。
```

* Replace it with:

```txt id="dygg1d"
最寄りの村にテレポートする
```

Source Of Truth Requirement

* The SOS potion data should now use the updated table entry above.
* Keep all other SOS data unchanged:

  * name remains `SOS`.
  * Japanese name remains `SOS`.
  * type remains `Board`.
  * rarity remains `Common`.
  * colour remains `Light Blue`.
  * implemented status remains `Y`.

Display Requirement

* The updated SOS description should appear everywhere potion descriptions are shown:

  * potion tooltip.
  * potion reward modal.
  * potion replacement modal.
  * active potion display if applicable.
  * debug potion list.
  * rules page, if potion descriptions are shown there.

Functionality Requirement

* This feature only updates data/descriptions.
* SOS functionality should remain as specified in EPIC-020 Feature-007:

  * teleport to nearest village.
  * if two villages are equal distance, choose randomly.
  * discard the potion.
  * begin the normal village sequence.
  * consume the player’s turn.

Out Of Scope

* No change to SOS behaviour.
* No change to SOS rarity.
* No change to SOS colour.
* No change to board potion trigger rules.
* No change to village logic.
* No change to potion UI layout.

Testing Checkpoints

* View SOS in English.
* Confirm description says `Teleport to the nearest village`.
* View SOS in Japanese.
* Confirm description says `最寄りの村にテレポートする`.
* Confirm old `field village` wording no longer appears.
* Confirm SOS still appears as a Board potion.
* Confirm SOS still appears as Common rarity.
* Confirm SOS still uses Light Blue colour.
* Confirm SOS still teleports to the nearest village when used.
* Confirm there are no console errors.
* Update `testing.txt` before marking this feature done.

Implementation Notes

* Confirmed the English SOS source description already used the required `Teleport to the nearest village` wording.
* Updated the Japanese SOS source description to the exact required `最寄りの村にテレポートする` text by removing only the obsolete trailing full stop.
* Kept the SOS id, English and Japanese names, Board availability, Common rarity, light-blue colour, and implemented nearest-village behavior unchanged.
* Reused the existing central potion definition and description helpers, so tooltips, rewards, replacement displays, active displays, and other shared potion consumers receive the updated text without UI changes.
* Made no changes to teleport selection, tie randomization, potion consumption, village visits, confirmation flow, or potion layout.

---

Feature-004: Fix overflowing modal action button positioning

Status

* done

Related Feature(s)

* EPIC-020 Feature-001: Make gameplay layout scale to fit screen height
* EPIC-020 Feature-014: Add rules modal inside settings menu
* EPIC-020 Feature-015: Prevent horizontal scrollbar in spells modal during token drag
* Settings modal
* Debug menu modal

Goal

* Fix modal layouts when the modal content overflows on the y axis.
* Action buttons should never be placed over the top of overflowing modal content.
* Action buttons should always appear at the bottom of the modal content.
* A clear example of the current problem is the Close button at the bottom of the debug menu.

Problem Summary

* Some modals now overflow vertically.
* When this happens, the modal action buttons can overlap the overflowing content.
* This makes the content harder to read and can make the buttons feel incorrectly positioned.
* The modal should treat action buttons as part of the content flow, not as an overlay on top of the content.

Required Behaviour

* Modal action buttons should always appear after the modal content.
* Buttons should not cover text, form controls, debug options, token lists, potion lists, or any other modal content.
* If a modal needs vertical scrolling:

  * the content should scroll safely.
  * the buttons should remain reachable.
  * the buttons should still appear at the bottom of the content.
* The Close button in the debug menu should appear below all debug content, not over it.

Important Layout Rule

* Action buttons should not be absolutely positioned over modal body content.
* Action buttons should not use fixed positioning inside the modal.
* Action buttons should not float above scrollable content.
* Action buttons should participate in the normal modal layout flow.

Correct Layout Direction

```txt id="v85xi2"
Modal container
Modal title
Modal body content
Modal action buttons
```

Incorrect Layout Direction

```txt id="z38ue9"
Modal container
Modal title
Modal body content
Action buttons visually overlaying the body content
```

Suggested Structure

```jsx id="ol84vu"
<div className="modal-background">
  <div className="modal-content">
    <div className="modal-body">
      {content}
    </div>

    <div className="modal-actions">
      {buttons}
    </div>
  </div>
</div>
```

Suggested CSS Direction

```css id="e7cddp"
.modal-content {
  display: flex;
  flex-direction: column;
  max-height: calc(100vh - 48px);
  overflow-y: auto;
}

.modal-body {
  position: relative;
}

.modal-actions {
  position: relative;
  display: flex;
  justify-content: center;
  gap: 20px;
  margin-top: 40px;
  padding-bottom: 20px;
  flex-shrink: 0;
}
```

Debug Menu Requirement

* The debug menu should be specifically checked.
* The Close button at the bottom of the debug menu should:

  * appear after all debug menu content.
  * not overlay the debug controls.
  * be reachable by scrolling if the debug menu is taller than the screen.
  * keep the existing button style.

Scrollable Modal Requirement

* For long modals, prefer this behaviour:

  * the modal panel has a maximum height.
  * the panel scrolls vertically.
  * the buttons are at the end of the scrollable content.
* Do not pin the buttons on top of the content unless a future feature explicitly asks for a sticky footer.

Button Group Requirement

* Existing modal button styling should be preserved.
* Buttons should remain bottom-centred unless a specific modal has a different existing layout requirement.
* Multiple action buttons should keep their existing horizontal or vertical arrangement.
* Secondary/cancel buttons should keep their secondary style.

Affected Areas To Check

* Debug menu.
* Settings modal.
* Rules modal inside settings.
* Potion confirmation modals.
* Potion replacement modals.
* Token assignment modals.
* Wandsmith/spells modal.
* Loot Chest reward modals.
* Decision outcome modals.
* Hazard modals.
* Village choice and heal modals.
* Any modal with long content or scrollable content.

Out Of Scope

* No redesign of modal visuals.
* No change to modal background images.
* No change to button text.
* No change to modal behaviour.
* No change to horizontal overflow handling, except where already covered by EPIC-020 Feature-015.
* No sticky footer requirement.

Testing Checkpoints

* Open the debug menu.
* Reduce browser height until the debug menu overflows vertically.
* Confirm the Close button does not overlay debug content.
* Scroll to the bottom of the debug menu.
* Confirm the Close button appears after all debug content.
* Open settings modal and confirm buttons are correctly placed.
* Open settings rules modal and confirm Back to Settings is below the content.
* Open a potion replacement modal with full potion slots.
* Confirm action buttons do not overlap potion choices.
* Open token assignment modal and confirm Confirm/Cancel buttons do not overlap token content.
* Open village choice modal and confirm buttons sit below the prompt.
* Confirm vertically overflowing modals remain usable.
* Confirm non-overflowing modals still look correct.
* Confirm there are no console errors.
* Update `testing.txt` before marking this feature done.

Implementation Notes

* Kept the existing shared default modal structure with the body followed by the action row inside the vertically scrollable, height-limited panel.
* Changed the shared modal body from a shrinkable flex child to a content-height flex child so overflowing controls cannot paint through the body box and underneath the action row.
* Made the shared action row a non-shrinking flex child, preserving its existing bottom-centred layout, wrapping, spacing, button styles, and `margin-top: auto` placement for short modals.
* Added shared modal regression coverage for panel scrolling, body/action ordering, non-shrinking content, and the absence of absolute, fixed, or sticky action positioning.
* Added a debug-specific regression confirming Close is rendered only in the shared action row after the debug body content.
* Made no modal copy, background, route, horizontal-overflow, sticky-footer, button-style, or gameplay behavior changes.

---

Feature-005: Hide normal potions section when Active Potion section is visible

Status

* done

Related Feature(s)

* EPIC-012 Feature-002: One potion per turn limits and Active Potion section
* EPIC-013 Feature-004: Starting Charge potion behaviour
* EPIC-013 Feature-008: Double Dice and Triple Dice
* EPIC-020 Feature-008: Implement Metal Detector potion
* EPIC-020 Feature-009: Implement Smokescreen potion

Goal

* Simplify the gameplay potion UI.
* When the Active Potion section is visible, the normal potions section should be hidden.
* This prevents the UI from becoming too tall and helps with the responsive layout changes.

Required Behaviour

* If the current player has an active potion shown in the Active Potion section:

  * hide the normal potions section.
  * show only the Active Potion section.
* If the current player does not have an active potion:

  * show the normal potions section as usual.
  * hide the Active Potion section.

Definition Of Normal Potions Section

* The normal potions section is the gameplay UI area that shows the player’s available potion slots.
* This includes:

  * potion slot display.
  * available board potion Use buttons.
  * empty potion slots.
  * potion tooltips.

Definition Of Active Potion Section

* The Active Potion section is the gameplay UI area introduced for potions that are currently affecting this turn.
* Examples include:

  * Starting Charge.
  * Roll Choice after selecting a roll.
  * Double Dice.
  * Triple Dice.
  * Spellbound effect on target.
  * Heavy Weight effect on target.
  * Troublemaker while waiting for roll.
  * Devine Chance while waiting for roll.
  * Storm Master while waiting for roll.
  * Metal Detector.
  * Smokescreen.

Display Rule

```txt id="0u9wfc"
Active Potion visible = hide normal potions section
Active Potion hidden = show normal potions section
```

Suggested JSX Direction

```jsx id="vwcm4d"
const hasActivePotion = Boolean(currentPlayer.activePotion);

return (
  <div className="gameplay-potion-area">
    {!hasActivePotion && (
      <NormalPotionsSection player={currentPlayer} />
    )}

    {hasActivePotion && (
      <ActivePotionSection activePotion={currentPlayer.activePotion} />
    )}
  </div>
);
```

* Use the actual project state names.
* If the active potion state supports multiple active potions, hide the normal section whenever the active potion list is not empty.

Potion Use Rules

* This feature only changes visibility.
* It does not change potion-use logic.
* If a potion is active, the player should not see or use normal board potion buttons until the Active Potion section clears.
* Existing one-board-potion-per-turn logic still applies.
* Existing one-battle-potion-per-turn logic still applies separately.

Turn Flow Requirement

* When the active potion resolves and is removed:

  * the normal potions section may become visible again if the same player is still in a valid pre-roll gameplay state.
* If the turn advances:

  * the next player’s potion UI should render according to their own active potion state.

Responsive Layout Requirement

* Hiding the normal potions section should reduce vertical space used by the gameplay UI.
* Do not leave a large empty gap where the normal potions section was hidden.
* The Active Potion section should occupy the potion area cleanly.

Out Of Scope

* No changes to potion data.
* No changes to potion effects.
* No changes to potion limits.
* No changes to battle potion section.
* No redesign of Active Potion component.
* No new active potion behaviour.

Testing Checkpoints

* Start a turn with no active potion.
* Confirm normal potions section is visible.
* Confirm Active Potion section is hidden.
* Use a board potion that moves to Active Potion.
* Confirm normal potions section disappears.
* Confirm Active Potion section is visible.
* Resolve the active potion.
* Confirm UI updates correctly.
* Use Metal Detector.
* Confirm normal potion section hides while Metal Detector is active.
* Use Smokescreen.
* Confirm normal potion section hides while Smokescreen is active.
* Start another player’s turn.
* Confirm their potion UI depends on their own active potion state.
* Confirm no empty vertical gap remains.
* Confirm there are no console errors.
* Update `testing.txt` before marking this feature done.

Implementation Notes

* Reused the existing `PotionList`, `ActivePotionSection`, and current-player `activePotion` state without changing any component, potion data, or effect logic.
* Changed the gameplay potion area to render exactly one section: Active Potion when the current player has an active potion, otherwise the normal Potions inventory.
* Normal potion slots, tooltips, capacity, and Use controls are therefore absent while an active potion is shown and return automatically when that state clears or the turn changes to a player without an active potion.
* Kept the conditional directly in the existing potion area so hiding the inventory leaves no placeholder element or extra vertical gap.
* Added gameplay regressions for the no-active baseline, Roll Choice activation and resolution, Metal Detector, Smokescreen, targeted Spellbound turn state, Devine Chance, localization, and player-specific state transitions.
* Made no changes to potion effects, usage limits, battle potions, active-potion styling, routes, or gameplay layout outside the requested visibility rule.

---

Feature-006: Show token bag capacity and use Unkempt for Japanese spell column numbers

Status

* done

Related Feature(s)

* EPIC-003 Feature-002: Add spell token assignment system
* EPIC-010 Feature-015: Redesign Spell Token Assignment page
* EPIC-011 Feature-008: Spell Token Assignment must display merged columns as one effective droppable column
* EPIC-020 Feature-012: Implement Wandsmith village option
* EPIC-020 Feature-017: Change Japanese font to M PLUS Rounded 1c

Goal

* Improve clarity in the spells modal and modifying spells component.
* Show the token bag capacity next to the Token Bag title.
* Ensure spell column capacity numbers use `Unkempt` font in Japanese.
* Numbers should always use `Unkempt`.

Part 1: Token Bag Capacity Display

Required Behaviour

* In the spells modal and modifying spells component, display the token bag capacity next to the token bag title.
* If the token bag is empty and has 5 total spaces, show:

English:

```txt id="sb10ue"
Token Bag 0/5
```

Japanese:

```txt id="0f5zi0"
トークンバッグ 0/5
```

* The first number is the number of tokens currently in the token bag.
* The second number is the token bag’s maximum capacity.

Capacity Format

```txt id="a2ohh6"
{title} {currentCount}/{maxCapacity}
```

Examples:

```txt id="3viyrn"
Token Bag 0/5
Token Bag 3/5
Token Bag 5/5
```

Japanese examples:

```txt id="7r1yl5"
トークンバッグ 0/5
トークンバッグ 3/5
トークンバッグ 5/5
```

Where This Should Appear

* Normal spells modal.
* First spell assignment.
* Wandsmith spells modal.
* Redo potion spell modification modal.
* Any modifying spells component that shows the token bag.
* Any reward token assignment version if it uses the same token bag component.

Suggested JSX Direction

```jsx id="oswozu"
<h2 className={`token-bag-title ${languageClass}`}>
  {language === "jp" ? "トークンバッグ" : "Token Bag"}{" "}
  <span className="number-text">
    {tokenBag.length}/{tokenBagCapacity}
  </span>
</h2>
```

* Use the real token bag array/state names.
* Use the real capacity source of truth.
* Do not hard-code `5` if token bag capacity can change in the future.

Token Bag Capacity Source Of Truth

* Use the current token bag maximum capacity from player state or game constants.
* If token bag max capacity is always 5 right now, it is acceptable to use the existing constant.
* If future features allow token bag capacity changes, this display should update automatically.

Part 2: Numbers Should Always Use Unkempt

Required Behaviour

* Numbers in the spells modal should use `Unkempt`, even in Japanese.
* Specifically, the capacity numbers for spell columns in the Japanese version should use `Unkempt`.
* The token bag capacity numbers should also use `Unkempt`.
* This rule applies to numeric text, not necessarily surrounding Japanese labels.

Examples

Japanese token bag title:

```txt id="xkd0fo"
トークンバッグ 3/5
```

* `トークンバッグ` should use the Japanese font.
* `3/5` should use `Unkempt`.

Japanese spell column capacity:

```txt id="2i7fug"
2/5
```

* `2/5` should use `Unkempt`.

CSS Requirement

* Add or reuse a class for number text.

Suggested CSS:

```css id="9yr28d"
.number-text,
.capacity-number,
.token-bag-capacity,
.spell-column-capacity {
  font-family: "Unkempt", cursive;
}
```

* Use more specific selectors if the Japanese font class overrides this.
* Numbers should remain readable and aligned.

Suggested More Specific CSS

```css id="nyu8ea"
.jp .number-text,
.jp .capacity-number,
.jp .token-bag-capacity,
.jp .spell-column-capacity {
  font-family: "Unkempt", cursive;
}
```

Spell Column Capacity Requirement

* In Japanese, any spell column capacity number should use `Unkempt`.
* This includes:

  * normal columns.
  * edge columns.
  * columns affected by Grey capacity.
  * merged/effective columns.
  * Wandsmith mode.
  * Redo mode.
  * first spell assignment.
  * reward token assignment if spell columns are shown.

Merged Column Requirement

* Merged column labels such as `1+2` should also use `Unkempt` because they are numeric.
* Capacity values on merged columns should use `Unkempt`.
* Do not let merged column labels switch to Japanese font.

Examples:

```txt id="mq28km"
1+2
2+3
5/6
```

* All of these should use `Unkempt`.

Language Requirement

* English:

  * token bag title can continue using existing English font.
  * numbers should use `Unkempt`, which should already match the English numeric style.

* Japanese:

  * Japanese words should use Japanese font from EPIC-020 Feature-017.
  * numbers should use `Unkempt`.

Out Of Scope

* No change to token bag capacity rules.
* No change to spell column capacity rules.
* No change to Grey capacity behaviour.
* No change to White merge behaviour.
* No change to spell modal layout beyond capacity text.
* No change to token drag-and-drop.
* No change to Wandsmith functionality.

Testing Checkpoints

* Open normal spells modal in English.
* Confirm title shows `Token Bag 0/5` when empty.
* Add tokens to bag.
* Confirm count updates correctly.
* Fill token bag.
* Confirm title shows `Token Bag 5/5`.
* Switch to Japanese.
* Confirm title shows `トークンバッグ 0/5` when empty.
* Confirm the Japanese label uses the Japanese font.
* Confirm `0/5` uses `Unkempt`.
* Check spell column capacity numbers in Japanese.
* Confirm capacity numbers use `Unkempt`.
* Create or view a merged column.
* Confirm merged label such as `1+2` uses `Unkempt`.
* Use Grey token to increase adjacent column capacity.
* Confirm updated capacity numbers use `Unkempt`.
* Open Wandsmith.
* Confirm token bag capacity appears and updates.
* Open Redo potion spell modification.
* Confirm token bag capacity appears and updates.
* Confirm there are no console errors.
* Update `testing.txt` before marking this feature done.

Implementation Notes

* Reused the existing shared `SpellTokenAssignment` used by normal spells, first setup, Wandsmith, Redo, Decision rewards, and battle rewards; no new component or route was added.
* Added the displayed token-bag count beside the localized title and reused the existing `TOKEN_BAG_MAX_CAPACITY` constant instead of duplicating the current maximum of 5.
* Counted the currently displayed bag contents so staged reward additions and replacements keep the heading synchronized with the visible tokens.
* Kept the localized token-bag label under the existing English or Japanese font while applying `Unkempt` only to bag capacity, spell-column capacity, and numeric/merged column labels.
* Made no changes to bag or column capacity rules, Grey or White mechanics, drag-and-drop, Wandsmith behavior, modal layout, or other styling.


---

Feature-007: Update browser tab title and favicon

Status

* done

Related Feature(s)

* Start page / app shell
* Browser metadata
* Public HTML / Vite entry files

Goal

* Improve how the game appears in the browser tab.
* Change the browser tab/site name to:

```txt id="vshx7p"
Spellix
```

* Update the favicon to use:

```txt id="b8jttx"
src/images/favicon.png
```

Required Behaviour

* The browser tab title should show:

```txt id="hz2n78"
Spellix
```

* The favicon shown in the browser tab should use the new image:

```txt id="lj47ru"
src/images/favicon.png
```

* This should work in:

  * local development.
  * production build.
  * browser tab display.
  * bookmarks/favourites where supported by the browser.

Title Requirement

* Update the app’s HTML metadata so the page title is `Spellix`.
* If the project uses Vite, this will usually be in:

```txt id="m60h1m"
index.html
```

Suggested direction:

```html id="g42ppo"
<title>Spellix</title>
```

* If the app also changes `document.title` from React, update that logic as well.
* Do not leave older placeholder titles in the app.

Favicon Requirement

* Use the favicon image located at:

```txt id="hb6d7m"
src/images/favicon.png
```

* If importing from `src` is not supported directly inside `index.html`, copy or reference the favicon using the project’s existing asset handling pattern.
* Do not use the old default Vite/React favicon.
* Do not leave multiple conflicting favicon links if one causes the old icon to remain visible.

Suggested Vite Direction

If the project can reference the source image safely:

```html id="xp18ru"
<link rel="icon" type="image/png" href="/src/images/favicon.png" />
```

Alternative safer direction:

```txt id="eyjzsl"
Move or copy favicon.png to public/favicon.png
```

Then use:

```html id="hr6szq"
<link rel="icon" type="image/png" href="/favicon.png" />
```

* Use the approach that works best with the current project setup.
* The final app should still conceptually use the favicon from `src/images/favicon.png`; if copied to `public`, document that implementation detail in `features.txt` or `testing.txt`.

Browser Cache Note

* Browsers often cache favicons aggressively.
* If the favicon does not appear to change immediately, test in:

  * hard refresh.
  * incognito/private window.
  * cleared browser cache.
  * production build preview.

Out Of Scope

* No logo redesign.
* No homepage visual changes.
* No generated favicon variants required.
* No Apple touch icon required.
* No manifest/theme-colour changes unless already present and easy to update safely.
* No change to game name inside story/rules text unless it already depends on the site title.

Testing Checkpoints

* Start local development.
* Confirm browser tab title says `Spellix`.
* Confirm browser tab favicon uses the new image.
* Confirm old React/Vite favicon no longer appears.
* Run production build.
* Preview production build.
* Confirm title still says `Spellix`.
* Confirm favicon still appears correctly.
* Test with hard refresh or incognito if favicon cache causes confusion.
* Confirm there are no console errors.
* Update `testing.txt` before marking this feature done.

Implementation Notes

* Reused the user-provided `src/images/favicon.png` as the source asset and copied it unchanged to `public/favicon.png`, which is the Create React App-compatible location for static HTML metadata.
* Updated the single favicon link to the existing `%PUBLIC_URL%` deployment pattern so local development and the `/spellix/` production base path both resolve the PNG correctly.
* Changed the HTML title and the existing manifest name fields to `Spellix`, and replaced the manifest's old `.ico` entry with the verified 1254x1254 PNG metadata.
* Added deployment regression coverage for the exact title, one PNG favicon link, source/public byte parity, manifest identity, and removal of old `React App` and `.ico` metadata references.
* Made no changes to React pages, app routes, homepage content, logo assets, Apple touch icon, theme colours, or other manifest behavior.

---

Feature-008: Remove starting Guard tokens

Status

* done

Related Feature(s)

* EPIC-003 Feature-002: Add spell token assignment system
* EPIC-018 Feature-001: Update token data, rename Deflect tokens, and add Shiny Health
* EPIC-021 Feature-006: Show token bag capacity and use Unkempt for Japanese spell column numbers

Goal

* Update the player starting tokens.
* Players should no longer start with any Guard tokens.
* The only starting tokens should now be 5 Red Damage tokens.

Previous Starting Tokens

```txt id="cu5qqw"
5 Red Damage tokens
2 Blue Guard tokens
```

New Starting Tokens

```txt id="krfivn"
5 Red Damage tokens
```

Required Behaviour

* Every player should start the game with exactly 5 Red Damage tokens.
* Players should not start with any Blue Guard tokens.
* This applies to every player colour and every player count.
* The starting token setup should be the same for:

  * 2-player games.
  * 3-player games.
  * 4-player games.
  * 5-player games.
  * 6-player games.

Starting Token Location Requirement

* The 5 Red Damage tokens should appear wherever starting tokens currently appear at game start.
* If starting tokens are placed in the token bag first, the token bag should contain:

```txt id="vzkecj"
5/5 tokens
```

* If starting tokens are immediately assigned during first spell assignment, only Red Damage tokens should be available.
* No Blue Guard token should be available during initial spell setup.

Token Bag Capacity Interaction

* If the token bag capacity is still 5 by default, the starting token bag should now begin full with 5 Red Damage tokens.
* EPIC-021 Feature-006 should display this correctly as:

English:

```txt id="sqgoia"
Token Bag 5/5
```

Japanese:

```txt id="vqct8x"
トークンバッグ 5/5
```

* Do not increase token bag capacity to preserve the old 7-token start.
* The removal of the 2 Guard tokens is intentional.

Starting Token Protection Requirement

* Any existing logic that treats starting tokens as protected should continue to work.
* The 5 Red Damage tokens should still be treated as starting tokens if the game currently tracks that distinction.
* Since Blue Guard tokens are no longer starting tokens, they should not be protected as starting tokens.

Battle Loss Requirement

* If battle loss or death token-removal logic protects starting tokens:

  * protect the 5 Red Damage starting tokens.
  * do not assume there are 7 starting tokens.
  * do not expect 2 starting Guard tokens to exist.

Save / Load Requirement

* New games should use the new starting token setup.
* Existing save files may still contain Blue Guard tokens if they were created before this feature.
* Do not forcibly remove Guard tokens from loaded save files unless a save migration explicitly requires it.
* This feature changes new-game initialisation only.

Out Of Scope

* No change to Red Damage token behaviour.
* No change to Blue Guard token behaviour once earned normally.
* No change to Guard token rarity.
* No change to battle reward pools.
* No change to token bag capacity.
* No change to spell slot capacity.
* No change to first spell assignment rules.
* No change to existing save files unless needed for compatibility.

Testing Checkpoints

* Start a new 2-player game.
* Confirm each player starts with exactly 5 Red Damage tokens.
* Confirm no player starts with Blue Guard tokens.
* Start games with 3, 4, 5, and 6 players.
* Confirm every player starts with only 5 Red Damage tokens.
* Open first spell assignment.
* Confirm only Red Damage tokens are available.
* Confirm token bag capacity displays correctly after EPIC-021 Feature-006.
* Confirm battle loss/death token protection still works with only 5 starting tokens.
* Confirm Blue Guard tokens can still be earned later through normal rewards if available.
* Load an older save file if possible.
* Confirm existing save data is not unexpectedly altered.
* Confirm there are no console errors.
* Update `testing.txt` before marking this feature done.

Implementation Notes

* New players are initialised with exactly 5 protected Red Damage tokens from the existing `starting` source.
* The shared starting-token count is now `5`, so first spell assignment completion and new-game setup use the same source of truth.
* Existing player token bags are cloned without migration, preserving Blue Guard tokens already present in older saves.
* The English and Japanese first-assignment warnings now require all 5 starting tokens.
* Earned Guard behaviour, reward pools, token bag capacity, and spell capacity are unchanged.

---

Feature-009: Standardise battle overlay icon width

Status

* done

Related Feature(s)

* EPIC-014 Feature-004: Cosmic Intervention battle potion
* EPIC-014 Feature-005: Shields Down battle potion
* EPIC-014 Feature-006: Thaw battle potion
* EPIC-017 Feature-005: Improve Deflect animation
* EPIC-020 Feature-005: Show final hit, death icon, and complete animations before battle ends
* EPIC-021 Feature-010: Change battle death icon colour

Goal

* Standardise the size of all icons that appear over character images in battle.
* Battle overlay icons should no longer scale to the full width of the character image.
* Every battle overlay icon should use a fixed width of `80px`.
* Icon height should be automatic so the icon keeps its natural aspect ratio.
* Existing animation behaviour should not change.

Problem Summary

* Battle overlay icons currently become the same width as the character image they appear over.
* This makes some icons look too large.
* Different battle effects can feel visually inconsistent.
* The desired behaviour is for all battle overlay icons to have the same width.

Required Behaviour

* Any icon that appears over a character image in battle should have:

```txt id="n8e9fh"
width: 80px
height: auto
```

* The icon should remain horizontally centred over the character image.
* The icon should remain vertically centred over the character image.
* The icon should keep its natural ratio.
* Existing animations should continue to work exactly as they do now.

Affected Battle Overlay Icons

* This applies to every icon that appears over a battle character image, including:

  * Guard shield icons.
  * Deflect icon.
  * potion effect icons.
  * death skull icon.
  * snowflake/freeze icon.
  * Cosmic Intervention bolt icon.
  * Shields Down gavel icon.
  * healing potion bottle icon.
  * Thaw icon if displayed over a character.
  * any future battle overlay icon.

Icon Size Requirement

```css id="hc9v13"
.battle-character-overlay-icon {
  width: 80px;
  height: auto;
}
```

* Use the project’s actual class names.
* Prefer one shared battle overlay icon class if possible.
* Do not set height to `80px`.
* Do not stretch icons.
* Do not use character image width for icon width.

Positioning Requirement

* Icons should remain centred over the relevant character image.
* The centre point should not change because of the new width.
* The icon should be centred both:

  * horizontally.
  * vertically.

Suggested positioning direction:

```css id="hxpc2a"
.battle-character-overlay-icon {
  position: absolute;
  left: 50%;
  top: 50%;
  width: 80px;
  height: auto;
  transform: translate(-50%, -50%);
  pointer-events: none;
}
```

Animation Compatibility Requirement

* Existing animations should not be affected by this change.
* If animations currently use `transform`, do not overwrite them accidentally.
* If centring also uses `transform`, combine transforms carefully.

Example safe direction:

```css id="u3l94a"
.battle-character-overlay-icon {
  --overlay-base-transform: translate(-50%, -50%);
}
```

* Animation keyframes should preserve centring while applying scale/shake/rotation.
* Do not remove:

  * grow animation.
  * shake animation.
  * fade animation.
  * jitter animation.
  * travel animation.
  * flash animation.

Important Transform Note

* If an existing animation uses:

```css id="s4pze0"
transform: scale(1.2);
```

* And centring uses:

```css id="erxyqw"
transform: translate(-50%, -50%);
```

* The animation may break centring unless both are combined.

Correct direction:

```css id="me20pv"
transform: translate(-50%, -50%) scale(1.2);
```

* Check every battle icon animation after changing the shared icon width.

Specific Icon Notes

* Guard shield should use `80px` width while remaining centred.
* Deflect `faBan` should use `80px` width while keeping its green animation.
* Freeze/snowflake should use `80px` width while keeping freeze animation.
* Potion bottle icon should use `80px` width while keeping potion application animation.
* Death skull should use `80px` width and the colour from Feature-010.
* Cosmic Intervention bolt should no longer force itself to character image height.
* Shields Down gavel should no longer force itself to character image height.

Superseded Behaviour

* Previous battle icon sizing that made icons match character image width is superseded by this feature.
* Previous Cosmic Intervention sizing bugs that requested the bolt match character image height are superseded for general battle overlay sizing.
* The new shared rule is:

```txt id="qf6fj6"
Battle overlay icons are always 80px wide.
Height is automatic.
```

Out Of Scope

* No changes to battle damage values.
* No changes to icon choice.
* No changes to battle effect timing.
* No changes to battle animation sequence.
* No changes to character image size.
* No changes to non-battle icons.
* No changes to token or potion component icons outside battle overlays.

Testing Checkpoints

* Trigger Guard in battle.
* Confirm shield icon is 80px wide.
* Confirm shield remains centred over the character.
* Trigger Deflect.
* Confirm Deflect icon is 80px wide.
* Confirm Deflect animation still works.
* Trigger Freeze.
* Confirm snowflake/freeze icon is 80px wide.
* Confirm freeze animation still works.
* Trigger a healing potion in battle.
* Confirm potion icon is 80px wide.
* Confirm potion animation still works.
* Trigger Cosmic Intervention.
* Confirm bolt icon is 80px wide.
* Confirm bolt remains centred and animated.
* Trigger Shields Down.
* Confirm gavel icon is 80px wide.
* Confirm animation still works.
* Trigger a battle death.
* Confirm skull icon is 80px wide.
* Confirm skull grows/shakes without losing centring.
* Confirm icons keep natural height ratio.
* Confirm no icon stretches or squashes.
* Confirm there are no console errors.
* Update `testing.txt` before marking this feature done.

Implementation Notes

* Added one shared `battle-character-overlay-icon` class with `80px` width and automatic height.
* Applied the shared class to battle death, Deflect, Guard, Freeze, healing potion, Cosmic Intervention, and Shields Down icons.
* Removed only the old actor-sized icon declarations; existing positions, colours, timing, and transform keyframes are unchanged.
* Battle healing passes the class through the existing shared healing component, while non-battle healing keeps its existing width animation.
* The battle-only width uses a narrow important override so the shared healing keyframes remain unchanged outside battle.

---

Feature-010: Change battle death icon colour to light grey

Status

* done

Related Feature(s)

* EPIC-020 Feature-005: Show final hit, death icon, and complete animations before battle ends
* EPIC-021 Feature-009: Standardise battle overlay icon width

Goal

* Update the battle death icon colour.
* The death icon should use a lighter grey colour.
* The new colour should be:

```txt id="cb7qq9"
#BABABA
```

Required Behaviour

* When a character reaches 0 HP in battle and the death icon appears:

  * use Font Awesome `faSkullCrossbones`.
  * set the icon colour to `#BABABA`.
  * keep the same death animation.
  * keep the same placement over the defeated character.
  * keep the same battle-end timing.

Colour Requirement

```css id="zjdcd4"
.battle-death-icon {
  color: #BABABA;
}
```

* Use the actual project class name if different.
* This colour should apply only to the battle death icon unless the same class is intentionally shared.

Animation Requirement

* Do not change the death icon animation.
* It should still:

  * appear after health reaches 0.
  * grow from the centre.
  * shake.
  * complete before the battle ends.

Interaction With Feature-009

* The death icon should also follow Feature-009.
* It should be:

```txt id="tztm47"
width: 80px
height: auto
colour: #BABABA
```

Out Of Scope

* No changes to death timing.
* No changes to death icon type.
* No changes to respawn skull icon unless it uses the same battle death class intentionally.
* No changes to battle rewards.
* No changes to battle loss penalties.
* No changes to other grey icons.

Testing Checkpoints

* Trigger lethal damage against an enemy.
* Confirm the skull icon appears after health reaches 0.
* Confirm the skull icon colour is `#BABABA`.
* Confirm the skull icon remains centred over the enemy.
* Trigger lethal damage against the player.
* Confirm the player death skull icon colour is `#BABABA`.
* Confirm the death animation still works.
* Confirm battle ends only after the death animation.
* Confirm there are no console errors.
* Update `testing.txt` before marking this feature done.

Implementation Notes

* Changed only the battle death icon colour from `#555` to `#BABABA`.
* Kept `faSkullCrossbones`, the shared `80px` width and automatic height, centred placement, grow-and-shake keyframes, and `1.5s` battle timing unchanged.
* Respawn icons and other grey icons were not changed.

---

Feature-011: Token tooltips should only show description

Status

* done

Related Feature(s)

* EPIC-017 Feature-006: Correct token names and separate descriptions
* EPIC-018 Feature-001: Update token data, rename Deflect tokens, and add Shiny Health
* EPIC-020 Feature-004: Make Buff common and update its description
* EPIC-014 Feature-007: Make potion tooltips behave like token tooltips

Goal

* Update token tooltip content.
* Token tooltips currently show both the token name and token description.
* Token tooltips should now show only the description.
* Token names should still appear elsewhere where the UI intentionally displays token names.

Problem Summary

* The token tooltip now contains:

  * token name.
  * token description.
* This is too much repeated text.
* The tooltip should only explain what the token does.

Required Behaviour

* Token tooltip content should only show the token description.
* Do not show the token name inside the tooltip.
* This applies in both English and Japanese.
* The tooltip should still use the correct language.
* The tooltip should still appear next to the cursor using existing tooltip behaviour.

Correct Tooltip Example

For Damage token in English:

```txt id="ps1a9v"
Plus 10 Damage
```

Incorrect Tooltip Example

```txt id="s0xkeo"
Damage
Plus 10 Damage
```

Correct Japanese Tooltip Example

```txt id="gp924k"
ダメージ＋10
```

Incorrect Japanese Tooltip Example

```txt id="ajxtlf"
ダメージ
ダメージ＋10
```

Scope

* Apply to token tooltips in:

  * token bag.
  * spell assignment modal.
  * Wandsmith.
  * Redo potion spell editing.
  * reward token assignment.
  * battle reward choice modal.
  * committed spell display.
  * battle spell columns.
  * debug token displays if they use token tooltips.

Name Display Requirement

* Token names should still appear where they are part of the main UI, for example:

  * reward cards that intentionally show token names.
  * token data tables if any exist.
  * rules page token list.
  * selected reward summaries.
* Only the tooltip content should remove the name.

Potion Tooltip Requirement

* This feature is only about token tooltips.
* Potion tooltips can keep their current content unless a later feature changes them.
* Do not accidentally remove potion names from potion tooltip UI if that is not intended.

Suggested Implementation Direction

* Find the shared token tooltip component or helper.
* Remove title/name rendering from the tooltip body.
* Keep description rendering.

Suggested JSX direction:

```jsx id="i16lsi"
<div className="token-tooltip">
  <p>{token.description[language]}</p>
</div>
```

Instead of:

```jsx id="l14ctl"
<div className="token-tooltip">
  <h3>{token.name[language]}</h3>
  <p>{token.description[language]}</p>
</div>
```

* Use the actual token data structure.

Out Of Scope

* No changes to token descriptions.
* No changes to token names.
* No changes to token behaviour.
* No changes to potion tooltips.
* No changes to tooltip positioning.
* No changes to tooltip styling except removing name spacing if needed.

Testing Checkpoints

* Hover Damage token.
* Confirm tooltip only shows `Plus 10 Damage`.
* Hover Guard token.
* Confirm tooltip only shows its description.
* Hover Buff token.
* Confirm tooltip only shows updated Buff description.
* Hover Shiny Health token.
* Confirm tooltip only shows its description.
* Switch to Japanese.
* Hover several tokens.
* Confirm tooltips only show Japanese descriptions.
* Confirm token names still appear in reward cards/rules where intended.
* Confirm potion tooltips are unaffected.
* Confirm tooltip positioning still works.
* Confirm there are no console errors.
* Update `testing.txt` before marking this feature done.

Implementation Notes

* Reused the shared `Token` component used by token bags, spell assignment, Wandsmith, Redo editing, rewards, committed spells, battle displays, and debug displays; no new component or abstraction was needed.
* Native token tooltips now use only the localized token description already provided through `aria-describedby`.
* Intentional visible token names controlled by `showName`, token data, token behavior, accessibility linkage, focus behavior, and tooltip positioning are unchanged.
* The separate `PotionIcon` tooltip implementation and all potion consumers are unchanged.

---

Feature-012: Add Browser Compatibility section to top of rules text

Status

* done

Related Feature(s)

* EPIC-018 Feature-009: Add start-page Rules button and bilingual Rules page
* EPIC-020 Feature-013: Add village actions section to rules page
* EPIC-020 Feature-014: Add rules modal inside settings menu
* EPIC-020 Feature-001: Make gameplay layout scale to fit screen height

Goal

* Add a Browser Compatibility section to the top of the rules text.
* The section should appear in both English and Japanese.
* It should explain how players can use browser zoom if the board or menus do not fit naturally on their screen.

Placement Requirement

* Add this section to the top of the rules content.
* It should appear before the existing first rules section.
* It should appear in both:

  * the main Rules page.
  * the settings Rules modal, if it reuses the same rules content.

Section Title

English:

```txt id="itln3q"
Browser Compatibility
```

Japanese:

```txt id="u3825z"
ブラウザの互換性
```

English Rules Text

```txt id="t6z7jb"
If the board or menus do not fit onto your screen naturally, please use the browser's zoom feature to adjust this. Either hold Ctrl and scroll with your mouse, or hold Ctrl and then press + or - to zoom in and out.
```

Japanese Rules Text

```txt id="w284zb"
ボードやメニューが画面に自然に収まらない場合は、ブラウザのズーム機能で調整してください。Ctrlキーを押しながらマウスホイールをスクロールするか、Ctrlキーを押しながら＋または－を押すと、拡大・縮小できます。
```

Style Requirement

* Match the existing rules page section style.
* The title should use the same style as other rules section titles.
* Body text should use the same style as other rules body text.
* Use correct English/Japanese fonts.
* Do not make this section look like an error warning.
* It should be normal rules/help text.

Content Requirement

* Include the exact meaning of:

  * board or menus may not fit naturally.
  * use browser zoom to adjust.
  * Ctrl + mouse scroll.
  * Ctrl + plus/minus.
* Japanese should be natural and clear.
* Do not mention mobile support.
* Do not promise full responsiveness on every device.

Rules Modal Requirement

* If rules content is shared, this should automatically appear in:

  * start page rules.
  * settings rules modal.
* If they are separate components, update both.

Out Of Scope

* No changes to browser zoom handling.
* No custom in-game zoom controls.
* No layout redesign.
* No change to responsive scaling.
* No change to rules sections except adding this new top section.
* No change to settings modal behaviour.

Testing Checkpoints

* Open the Rules page from the start/home screen.
* Confirm Browser Compatibility appears at the top.
* Confirm it appears before all other rules sections.
* Confirm English title and text are correct.
* Switch to Japanese.
* Confirm Japanese title and text are correct.
* Open Rules from settings.
* Confirm Browser Compatibility appears at the top there too.
* Confirm rules content still scrolls correctly.
* Confirm there are no duplicate Browser Compatibility sections.
* Confirm there are no console errors.
* Update `implementation.txt` and `testing.txt` before marking this feature done.

Implementation Notes

* Added one Browser Compatibility object at index `0` of both existing `RULES_CONTENT` section arrays using the supplied English and Japanese copy.
* Reused the existing shared `RulesBody`, so the section appears on the main Rules page and in the settings Rules modal with the existing section markup, fonts, and scroll behavior.
* No new component, style, route, zoom control, layout behavior, or responsive behavior was created or changed.
* `implementation.txt` was not updated because the main project rules require the three-file vibe workflow and take priority over the legacy checkpoint in this feature text.

---

Feature-013: Add Charge and Buff battle animations

Status

* done

Related Feature(s)

* EPIC-014 Feature-004: Cosmic Intervention battle potion
* EPIC-017 Bug-002: Guard animations should always be first in battle animation queue
* EPIC-021 Feature-009: Standardise battle overlay icon width
* EPIC-020 Feature-005: Show final hit, death icon, and complete animations before battle ends
* EPIC-020 Feature-004: Make Buff common and update its description

Goal

* Add visible battle animations for Charge and Buff effects.
* Reuse the existing Cosmic Intervention lightning bolt animation.
* Charge and Buff animations should play over the character who triggered the effect.
* Charge should always be first in the battle animation queue.
* Buff should always play immediately after Charge.
* Both should resolve before all other battle animations.

Required Behaviour

* When a battle roll triggers Charge, show a lightning bolt animation over the character who triggered Charge.
* When a battle roll triggers Buff, show a lightning bolt animation over the character who triggered Buff.
* The animation should play over the caster/triggering character, not the opponent.
* Charge animation should use the existing Cosmic Intervention bolt colour.
* Buff animation should use purple:

```txt id="xq5gyn"
#69419e
```

Animation Priority Requirement

* Charge should always be the first animation in the queue.
* Buff should always be second, directly after Charge.
* All other animations should happen after Charge and Buff.

Required queue priority:

```txt id="qwe7s4"
1. Charge
2. Buff
3. Guard
4. Damage
5. Deflect
6. Counter
7. Freeze
8. Potion effects
9. Death icon
```

* If Guard is currently forced to the front of the animation queue from an earlier bug fix, update the queue rule so Charge and Buff now take priority over Guard.
* Guard should still happen before damage and other ordinary effects.
* Charge has priority over Buff.

Example: Charge Only

```txt id="wv7fo8"
1. Player rolls a column containing Charge.
2. Charge lightning animation plays over the player character.
3. Any other effects then animate.
```

Example: Buff Only

```txt id="du70az"
1. Player rolls a column containing Buff.
2. Buff purple lightning animation plays over the player character.
3. Any other effects then animate.
```

Example: Charge And Buff Together

```txt id="ml3xzt"
1. Player rolls a column containing both Charge and Buff.
2. Charge lightning animation plays over the player character.
3. Buff purple lightning animation plays over the player character.
4. Guard, damage, deflect, counter, freeze, or other effects then animate.
```

Animation Source Requirement

* Reuse the existing Cosmic Intervention lightning bolt animation.
* Do not create a totally new animation if the existing one can be shared.
* The animation should keep the same behaviour:

  * lightning bolt appears.
  * jitter/flash behaviour remains.
  * fade timing remains.
  * animation duration remains.
  * battle controls remain disabled during animation resolution.

Target Requirement

* Cosmic Intervention currently plays the bolt over the enemy being struck.
* Charge and Buff should instead play the bolt over the character who triggered the effect.
* If the player triggers Charge or Buff:

  * play the animation over the player character.
* If the enemy triggers Charge or Buff:

  * play the animation over the enemy character.

Correct Target Examples

```txt id="zm8ycz"
Player rolls Charge: animation over player.
Player rolls Buff: animation over player.
Enemy rolls Charge: animation over enemy.
Enemy rolls Buff: animation over enemy.
```

Incorrect Target Examples

```txt id="ayz8eq"
Player rolls Charge: animation over enemy.
Player rolls Buff: animation over enemy.
Enemy rolls Charge: animation over player.
Enemy rolls Buff: animation over player.
```

Charge Animation Requirement

* Use the existing Cosmic Intervention lightning bolt animation.
* Keep the current lightning bolt colour.
* Keep the current animation timing.
* Keep the current animation movement/shake/flash.
* Only change the target position so it appears over the triggering character.

Buff Animation Requirement

* Use the same lightning bolt animation as Charge/Cosmic Intervention.
* The bolt colour should be:

```txt id="vdgsxm"
#69419e
```

* This is the same purple as the Buff token.
* The animation should appear over the triggering character.
* Keep the same timing and movement style as the existing bolt animation.

Battle Overlay Icon Size Requirement

* Charge and Buff battle overlay icons must follow EPIC-021 Feature-009.
* The lightning bolt icon should be:

```txt id="fycmzq"
width: 80px
height: auto
```

* It should remain horizontally and vertically centred over the triggering character image.
* Do not stretch the icon.
* Do not make the icon the same width as the character image.

Queue Implementation Direction

* Battle animations should be queued by effect priority before playback.
* Charge and Buff should be added to the queue before ordinary battle effects.

Suggested priority map:

```js id="qolj29"
const battleAnimationPriority = {
  charge: 10,
  buff: 20,
  guard: 30,
  damage: 40,
  deflect: 50,
  counter: 60,
  freeze: 70,
  potion: 80,
  death: 90
};
```

* Lower numbers mean earlier animation.
* Use the project’s actual animation queue system if one already exists.
* Do not break existing animation sequencing.

Suggested Animation Event Shape

```js id="i8woza"
{
  type: "charge",
  target: "self",
  icon: faBolt,
  colour: "current-cosmic-intervention-yellow",
  sourceCharacterId: currentBattleActor.id
}
```

```js id="oi7uss"
{
  type: "buff",
  target: "self",
  icon: faBolt,
  colour: "#69419e",
  sourceCharacterId: currentBattleActor.id
}
```

* Use actual state names from the battle system.
* The important rule is that the target is the triggering character.

Interaction With Existing Charge Behaviour

* This feature adds animation only.
* Existing Charge functionality should remain unchanged.
* Charge should still charge the relevant slots according to existing rules.
* The animation should not change:

  * how long Charge lasts.
  * which columns become charged.
  * how charged columns are displayed.
  * how charged effects resolve.

Interaction With Existing Buff Behaviour

* This feature adds animation only.
* Existing Buff functionality should remain unchanged.
* Buff should still apply to adjacent columns according to existing rules.
* The animation should not change:

  * which columns are buffed.
  * how much damage/guard is increased.
  * how long the Buff lasts.
  * merged/effective column adjacency logic.

Timing Requirement

* Charge and Buff animations should happen before the visual effects they enable.
* If Charge causes later attacks/guards to become stronger, the player should see the Charge animation first.
* If Buff causes adjacent damage/guard to increase, the player should see the Buff animation before the affected damage/guard animation.
* Battle result logic should remain correct even though the animation queue now includes extra steps.

Controls Requirement

* Battle controls should stay disabled while Charge or Buff animation is playing.
* The player should not be able to roll, use another potion, or advance battle while these animations are resolving.
* Existing battle control disable logic should include these new animation phases.

Out Of Scope

* No change to Charge token mechanics.
* No change to Buff token mechanics.
* No new token descriptions.
* No change to Cosmic Intervention behaviour.
* No change to Cosmic Intervention colour.
* No change to battle damage numbers.
* No change to battle reward flow.
* No sound effect required.
* No new icon required beyond reusing `faBolt`.

Testing Checkpoints

* Give the player a Charge token in a battle spell column.
* Roll that column.
* Confirm the lightning bolt animation plays over the player character.
* Confirm Charge animation plays before all other battle animations.
* Confirm Charge still affects the battle exactly as before.
* Give the enemy a Charge token.
* Make the enemy roll that column.
* Confirm the lightning bolt animation plays over the enemy character.
* Give the player a Buff token.
* Roll that column.
* Confirm a purple `#69419e` lightning bolt animation plays over the player character.
* Confirm Buff animation plays after Charge if both exist.
* Confirm Buff animation plays before Guard, Damage, Deflect, Counter, Freeze, and other effects.
* Give the enemy a Buff token.
* Make the enemy roll that column.
* Confirm the purple lightning bolt appears over the enemy character.
* Roll a column containing both Charge and Buff.
* Confirm Charge animation plays first.
* Confirm Buff animation plays second.
* Confirm all other animations play after both.
* Confirm battle overlay icon width is 80px and height is automatic.
* Confirm icons remain centred over the character image.
* Confirm animations do not lose their centring.
* Confirm Cosmic Intervention still works normally over the opponent.
* Confirm battle controls are disabled during Charge and Buff animations.
* Confirm lethal hits still wait for the full animation queue before ending battle.
* Confirm there are no console errors.
* Update `implementation.txt` and `testing.txt` before marking this feature done.

---

Feature-014: Update Charge token description

Status

* done

Related Feature(s)

* EPIC-018 Feature-001: Update token data, rename Deflect tokens, and add Shiny Health
* EPIC-021 Feature-013: Add Charge and Buff battle animations
* EPIC-011 Feature-004: Add non-battle token effects

Goal

* Update the Charge token description in English and Japanese.
* The description should now clearly explain that Charge increases damage or guard by 5.
* The Charge token remains a rare Yellow token.
* This is a data/text update only.

Updated Charge Token Entry

```txt id="c7yt4v"
Colour: Yellow
Name: Charge
Japanese Name: チャージ
Description: Charge all your slots next turn, increasing their damage or guard by 5
Japanese Description: 次のターン、すべてのスロットをチャージし、ダメージまたはガードを5増加させる。
Rarity: Rare
```

Required Behaviour

* Replace the current Charge token English description with:

```txt id="kkx8sw"
Charge all your slots next turn, increasing their damage or guard by 5
```

* Replace the current Charge token Japanese description with:

```txt id="eqld3f"
次のターン、すべてのスロットをチャージし、ダメージまたはガードを5増加させる。
```

* Keep the Charge token name unchanged.

English name:

```txt id="ryitr7"
Charge
```

Japanese name:

```txt id="r9x5vq"
チャージ
```

* Keep the Charge token colour unchanged:

```txt id="qp7x2j"
Yellow
```

* Keep the Charge token rarity unchanged:

```txt id="lowxeb"
Rare
```

Display Requirement

* The updated Charge description should appear everywhere token descriptions are shown:

  * token tooltip.
  * token bag.
  * spell assignment modal.
  * Wandsmith.
  * Redo spell modification.
  * token reward modals.
  * battle reward token choices.
  * rules page token section, if token descriptions are listed there.
  * debug token displays.

Tooltip Requirement

* If EPIC-021 Feature-011 has been implemented, the Charge token tooltip should only show the description.
* It should not show the token name inside the tooltip.

Correct English tooltip after Feature-011:

```txt id="xdrgla"
Charge all your slots next turn, increasing their damage or guard by 5
```

Correct Japanese tooltip after Feature-011:

```txt id="hifqbz"
次のターン、すべてのスロットをチャージし、ダメージまたはガードを5増加させる。
```

Functionality Requirement

* This feature does not change Charge behaviour.
* Charge should still charge all slots next turn.
* Charge should still increase damage or guard by 5.
* Charge should still be a rare token.
* Charge should still use the battle animation from Feature-013 once that feature is implemented.

Out Of Scope

* No change to Charge mechanics.
* No change to Charge rarity.
* No change to Charge colour.
* No change to Charge animation timing.
* No change to Buff.
* No change to potion Charger.
* No change to battle damage calculations.

Testing Checkpoints

* View Charge token in English.
* Confirm description says `Charge all your slots next turn, increasing their damage or guard by 5`.
* View Charge token in Japanese.
* Confirm description says `次のターン、すべてのスロットをチャージし、ダメージまたはガードを5増加させる。`.
* Confirm Charge is still Yellow.
* Confirm Charge is still Rare.
* Confirm Charge tooltip only shows the description if Feature-011 is complete.
* Confirm old Charge wording no longer appears.
* Confirm Charge still works in battle.
* Confirm Charge animation from Feature-013 still works if implemented.
* Confirm there are no console errors.
* Update `implementation.txt` and `testing.txt` before marking this feature done.

Completion Notes

* Updated the shared Yellow token definition with the supplied English and Japanese Charge descriptions.
* Reused `TOKEN_DEFINITIONS`, `getTokenDescription`, and the shared `Token` component so existing token surfaces receive the new copy without component changes.
* Added focused data and bilingual description-only tooltip coverage; no new runtime component, mechanic, style, or animation was created.

---

Feature-015: Clean up settings menu layout and add End Game confirmation

Status

* done

Related Feature(s)

* EPIC-018 Feature-012: Only show settings button on gameplay page
* EPIC-020 Feature-014: Add rules modal inside settings menu
* EPIC-021 Feature-004: Fix overflowing modal action button positioning
* EPIC-021 Feature-001: Add persistent random background music with global floating toggle button

Goal

* Improve the visual layout of the settings menu.
* Make the settings title use the correct language and font.
* Stack settings buttons vertically in a consistent order.
* Give the Close button secondary styling.
* Add an End Game confirmation modal before the game actually ends.

Settings Title Requirement

* The settings modal should have a title at the top.
* The title should be horizontally centred.
* The title should use `larger-text`.
* The title should use the correct language.
* The title should use the correct font for the current language.

English title:

```txt id="h3y7vf"
Settings
```

Japanese title:

```txt id="r1le1t"
設定
```

Suggested JSX direction:

```jsx id="k2bdnv"
<h2 className={`settings-title larger-text ${languageClass}`}>
  {language === "jp" ? "設定" : "Settings"}
</h2>
```

Suggested CSS direction:

```css id="puwv0m"
.settings-title {
  text-align: center;
  margin-bottom: 40px;
}
```

Button Order Requirement

* Settings menu buttons should be vertically stacked in this exact order:

```txt id="phcvui"
Debug
Save Game
Rules
End Game
Close
```

Japanese order:

```txt id="gt7j9a"
デバッグ
ゲームを保存
ルール
ゲームを終了
閉じる
```

* If Debug Mode is turned off or debug controls are hidden, the Debug button should be hidden while preserving the order of the remaining buttons.
* If Save Game is unavailable for any reason, hide or disable it according to existing rules, while preserving the order of remaining buttons.
* Do not reorder Rules, End Game, or Close.

Button Layout Requirement

* Buttons should be vertically stacked.
* Buttons should be horizontally centred.
* Buttons should use consistent width.
* Buttons should have consistent spacing.
* Use `div` layout, not `ul` or `li`.

Suggested JSX direction:

```jsx id="vwj6ez"
<div className="settings-button-stack">
  {debugEnabled && (
    <button>Debug</button>
  )}

  <button>Save Game</button>
  <button>Rules</button>
  <button>End Game</button>
  <button className="secondary-button">Close</button>
</div>
```

Suggested CSS direction:

```css id="s7rwve"
.settings-button-stack {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 18px;
}
```

Close Button Requirement

* The Close button should use secondary colouring.
* It should visually look different from the main action buttons.
* It should still be clearly clickable.
* It should close the settings modal as it currently does.

Suggested class:

```jsx id="wvhyzt"
<button className="secondary-button">
  {language === "jp" ? "閉じる" : "Close"}
</button>
```

End Game Confirmation Requirement

* Clicking End Game should not immediately end the game.
* It should open an “Are you sure?” confirmation modal.
* The game should only end if the player selects Yes.
* If the player selects No, the confirmation modal closes and the settings modal opens again.

Confirmation Text

English:

```txt id="egoe1d"
Are you sure you want to end the game?
```

Japanese:

```txt id="lae5j1"
ゲームを終了してもよろしいですか？
```

* The confirmation text should:

  * use the correct language.
  * use the correct font.
  * use `larger-text`.
  * be horizontally centred.

Confirmation Buttons

English:

```txt id="xl1y6d"
Yes
No
```

Japanese:

```txt id="t3h170"
はい
いいえ
```

Required Confirmation Behaviour

```txt id="h7gosf"
1. Player opens settings.
2. Player clicks End Game.
3. End Game confirmation modal opens.
4. Player clicks No.
5. Confirmation modal closes.
6. Settings modal is shown again.
7. Player clicks End Game again.
8. Confirmation modal opens.
9. Player clicks Yes.
10. Game ends using the existing End Game behaviour.
```

No Button Requirement

* Selecting No should:

  * close the confirmation modal.
  * return to the settings modal.
  * not end the game.
  * not reset game state.
  * not navigate away.

Yes Button Requirement

* Selecting Yes should:

  * run the existing End Game logic.
  * close settings/confirmation as needed.
  * navigate to the correct start/home page according to existing End Game behaviour.
  * not require any further confirmation.

Suggested State Direction

```js id="tqc1zl"
const [settingsStep, setSettingsStep] = useState("main");

// possible values:
// "main"
// "rules"
// "confirmEndGame"
```

Suggested Render Direction

```jsx id="pqggh6"
{settingsStep === "main" && (
  <SettingsMainMenu />
)}

{settingsStep === "rules" && (
  <SettingsRulesModal />
)}

{settingsStep === "confirmEndGame" && (
  <ConfirmEndGameModal />
)}
```

Modal Layout Requirement

* The confirmation modal should use the standard modal background.
* The confirmation modal should follow existing modal button styling.
* Buttons should be bottom-centred.
* Do not use full-width buttons unless the settings modal already uses full-width buttons consistently.
* Do not use `ul` or `li`.

Interaction With Rules Modal

* The Rules button should still open the settings rules modal.
* Back to Settings from the rules modal should still return to the main settings menu.
* The End Game confirmation should not interfere with this flow.

Interaction With Modal Overflow Fix

* EPIC-021 Feature-004 requires modal action buttons to sit at the bottom of content.
* Apply the same rule here.
* Buttons should not overlay the confirmation text.

Out Of Scope

* No change to settings button visibility rules.
* No change to Save Game behaviour.
* No change to Debug behaviour.
* No change to Rules content.
* No change to End Game destination except adding confirmation.
* No redesign of the whole settings background.
* No new sound effect.

Testing Checkpoints

* Open the settings modal in English.
* Confirm title says `Settings`.
* Confirm title is centred.
* Confirm title uses `larger-text`.
* Confirm title uses the correct English font.
* Switch to Japanese.
* Confirm title says `設定`.
* Confirm title uses the correct Japanese font.
* Confirm buttons are vertically stacked.
* Confirm button order is Debug, Save Game, Rules, End Game, Close.
* Confirm Close uses secondary colouring.
* Click Close and confirm settings closes.
* Open settings again.
* Click End Game.
* Confirm confirmation modal appears.
* Confirm text says `Are you sure you want to end the game?`.
* Confirm text uses `larger-text`.
* Click No.
* Confirm settings modal returns.
* Confirm game is not ended.
* Click End Game again.
* Click Yes.
* Confirm existing End Game behaviour runs.
* Repeat End Game confirmation in Japanese.
* Confirm Japanese text says `ゲームを終了してもよろしいですか？`.
* Confirm no buttons overlap content.
* Confirm there are no console errors.
* Update `implementation.txt` and `testing.txt` before marking this feature done.

Completion Notes

* Reused the existing `Modal`, `Button`, settings view state, current-player language, Rules view, Save Game handler, Debug handler, and End Game reset/navigation handler; no new component, route, dependency, or game-state flow was created.
* Added the requested bilingual centred title, language classes, vertical fixed-width action stack, exact action order, and secondary Close action in the existing settings modal.
* End Game now opens a bilingual confirmation view; No returns to the unchanged settings menu without ending the game, while Yes calls the existing End Game behavior.
* Existing Debug visibility, gameplay-only Save Game visibility, Rules return flow, settings background, End Game destination, and unrelated UI remain unchanged.

---

Feature-016: Add Shiny Buff rare token

Status

* not started

Related Feature(s)

* EPIC-018 Feature-001: Update token data, rename Deflect tokens, and add Shiny Health
* EPIC-020 Feature-004: Make Buff common and update its description
* EPIC-021 Feature-003: Elite Tower wins should reward 3 distinct rare tokens only
* EPIC-021 Feature-013: Add Charge and Buff battle animations
* EPIC-021 Feature-011: Token tooltips should only show description

Goal

* Add a new rare token: Shiny Buff.
* Shiny Buff is the rare version of the common Buff token.
* It should work exactly like common Buff, but with a buff value of `10` instead of `5`.
* Common Buff and Shiny Buff should stack together.

New Token Entry

```txt id="g2sxcm"
Colour: Purple/Yellow Outline
Name: Shiny Buff
Japanese Name: 輝強化
Description: Buff the two columns adjacent to the column buff is in, increasing their damage or guard by 10 next turn only.
Japanese Description: バフが配置されている列の両隣の2列を強化し、次のターンのみダメージまたはガードを10増加させる。
Rarity: Rare
```

Visual Requirement

* Shiny Buff should use:

  * Purple fill.
  * Yellow outline.
* It should follow the same visual pattern as the other shiny tokens.
* The yellow outline should be consistent with:

  * Shiny Damage.
  * Shiny Guard.
  * Shiny Counter.
  * Shiny Deflect.
  * Shiny Health.

Suggested visual direction:

```txt id="g7f0xy"
Base colour: Buff purple
Outline: yellow
Rarity: Rare
```

Token Data Requirement

* Add a new token ID for Shiny Buff.
* Do not reuse the common Buff token ID.
* Suggested ID:

```txt id="cruj6w"
shinyBuff
```

* Shiny Buff should be distinct from common Buff in:

  * reward generation.
  * tooltip data.
  * token bag.
  * spell slots.
  * battle resolution.
  * rare token pools.
  * save/load data.

Required English Data

```txt id="tz0wd9"
Name: Shiny Buff
Description: Buff the two columns adjacent to the column buff is in, increasing their damage or guard by 10 next turn only.
Rarity: Rare
```

Required Japanese Data

```txt id="o0rygi"
Japanese Name: 輝強化
Japanese Description: バフが配置されている列の両隣の2列を強化し、次のターンのみダメージまたはガードを10増加させる。
```

Rarity Requirement

* Shiny Buff should be rare.
* It should appear in rare token reward pools.
* It should not appear in common token reward pools.
* It should be eligible for Elite Tower rare-only rewards from EPIC-021 Feature-003.
* It should be eligible for second-Elite village rare token rewards from EPIC-020 Feature-010.

Functionality Requirement

* Shiny Buff works exactly like common Buff, but with a value of `10`.
* It buffs the two adjacent columns.
* The buff affects damage or guard next turn only.
* It should use the same adjacent-column logic as common Buff.
* It should use the same merged/effective column adjacency logic as common Buff.
* It should not buff itself.
* Edge columns only buff one adjacent column.

Buff Value Requirement

```txt id="hq88lj"
Common Buff value: 5
Shiny Buff value: 10
```

Stacking Requirement

* Common Buff and Shiny Buff should stack.
* Multiple Buff-type tokens in the same rolled column should add together.
* If a player rolls a column containing:

  * 1 common Buff.
  * 1 Shiny Buff.
* the adjacent columns should be buffed by:

```txt id="r9a44e"
5 + 10 = 15
```

* This means the next-turn damage or guard increase for adjacent columns should be `15`.

Stacking Example

```txt id="pbq4eb"
Column 3 contains:
1 Common Buff
1 Shiny Buff

Player rolls 3.

Columns 2 and 4 receive:
+15 buff for next turn only.
```

Merged Column Requirement

* Shiny Buff should respect merged/effective columns.
* If the rolled column is a merged column:

  * use effective adjacency.
  * buff adjacent effective columns.
* If an adjacent effective column is itself merged:

  * the buff applies to that effective merged column.
  * rolling either number in that merged column next turn should receive the buff.

Battle Animation Requirement

* Shiny Buff should use the Buff battle animation from EPIC-021 Feature-013.
* The animation should play over the triggering character.
* The animation colour should remain Buff purple:

```txt id="oq88sy"
#69419e
```

* If common Buff and Shiny Buff are triggered together, do not play the same animation twice unless existing animation design expects one animation per token.
* Preferred behaviour:

  * one Buff animation plays for the combined Buff effect.
  * combined buff value is applied.

Tooltip Requirement

* Shiny Buff tooltip should use the current tooltip rules.
* If EPIC-021 Feature-011 is implemented, tooltip should show description only.

Correct English tooltip:

```txt id="u0edq4"
Buff the two columns adjacent to the column buff is in, increasing their damage or guard by 10 next turn only.
```

Correct Japanese tooltip:

```txt id="qtdlko"
バフが配置されている列の両隣の2列を強化し、次のターンのみダメージまたはガードを10増加させる。
```

* The tooltip should not show the token name if Feature-011 is complete.

Reward Pool Requirement

* Add Shiny Buff to:

  * rare token pool.
  * rare reward generation.
  * Elite Tower rare reward pool.
  * village rare token reward pool.
  * debug token grant tools if they exist.
* Do not add Shiny Buff to:

  * common token pool.
  * starting tokens.
  * common village reward pool.

Save / Load Requirement

* If save/load stores token IDs, add Shiny Buff to recognised token data.
* Saved games containing Shiny Buff should load correctly.
* Older saves without Shiny Buff should still load normally.

Rules Requirement

* If the Rules page token section lists all tokens, add Shiny Buff.
* Use the same formatting as other shiny tokens.
* If rules only summarise token categories, update only if needed.

Out Of Scope

* No change to common Buff value.
* No change to common Buff rarity.
* No change to Charge.
* No new potion.
* No change to token capacity.
* No change to starting tokens.
* No new Shiny Buff-specific animation colour.
* No change to Buff animation timing.

Testing Checkpoints

* Confirm Shiny Buff appears in token data.
* Confirm Shiny Buff is Purple with Yellow outline.
* Confirm Shiny Buff rarity is Rare.
* Confirm Shiny Buff does not appear in common reward pools.
* Confirm Shiny Buff can appear in rare reward pools.
* Confirm Shiny Buff can appear in Elite Tower rare rewards.
* Confirm Shiny Buff can appear in second-Elite village rare rewards.
* Put Shiny Buff in a spell column.
* Roll that column in battle.
* Confirm adjacent columns are buffed by 10.
* Put common Buff and Shiny Buff in the same column.
* Roll that column.
* Confirm adjacent columns are buffed by 15.
* Confirm Shiny Buff does not buff its own column.
* Confirm edge-column behaviour is correct.
* Confirm merged/effective column adjacency works.
* Confirm Buff animation plays over the triggering character.
* Confirm tooltip shows correct English description.
* Confirm tooltip shows correct Japanese description.
* Confirm save/load recognises Shiny Buff if save/load is implemented.
* Confirm there are no console errors.
* Update `implementation.txt` and `testing.txt` before marking this feature done.
