---

Feature-001: Add persistent random background music with global floating toggle button

Test Status

* passed

Manual Test Steps

1. Open the app in a browser that permits autoplay and then in one that blocks autoplay.
   Expected: Music starts immediately when permitted; otherwise the first click, touch, or key press starts it without an app error.
2. Visit the start, setup, story, rules, gameplay, battle, village, cave, river, decision, loot, settings-modal, and winner flows.
   Expected: One music button remains fixed 10px from the top-left on every page and one audio track continues without restarting or overlapping during route changes.
3. Inspect the enabled music control in English and Japanese, hover it, turn music off, navigate, and turn it on again.
   Expected: The brown 50px button has a yellow 30px `faVolume` icon and localized off label, changes to `faVolumeXmark` and the localized on label while paused, stays paused across navigation, and resumes the same track at its preserved position.
4. Enter gameplay before rolling, begin movement, and complete the next-turn flow.
   Expected: The settings button uses the same base button style at top-right 10px while retaining its existing gameplay-only visibility rules.
5. Let at least 11 tracks complete while recording their order.
   Expected: Each of the 10 tracks plays exactly once in the first cycle, a newly shuffled cycle then starts, tracks advance automatically, and no audio overlaps.
6. Block one MP3 request and then block all MP3 requests using browser developer tools.
   Expected: One failed track logs a warning and is skipped; exhausting all tracks safely disables the music button without crashing.
7. Inspect the browser console throughout the flow.
   Expected: No application errors occur; only the requested developer warning appears for deliberately failed tracks.

Automated Tests Completed

* The pre-implementation focused run failed all 4 suites as expected because the queue, track list, player, and app-shell wiring did not yet exist.
* All 10 focused music tests pass across the shuffled queue, no duplicates within a cycle, cycle-boundary repeat avoidance, exactly 10 unique MP3 imports, enabled and disabled Font Awesome states, pause/resume position preservation, autoplay retry, failed-track skipping, all-tracks-failed handling, one app-shell mount, and route persistence.
* Both focused settings-visibility regression tests pass, confirming movement still hides settings and the next-turn flow restores it; the older test file logs JSDOM's existing unimplemented media playback warning because it does not mock `HTMLMediaElement.play`.
* The complete older settings suite was also sampled and retains one unrelated stale battle-health expectation of `100 / 100`; the music and settings-visibility assertions pass independently.
* Scoped ESLint passes for the new music production files and tests plus `AppMusic.test.jsx`; this project has no `npm run lint` script.
* `npm run build` compiles the optimized production build successfully and its output contains all 10 hashed MP3 assets.
* `git diff --check` reports no whitespace errors.

Automated Tests Still Needed

* None for the queue, imported track count, player state transitions, autoplay recovery logic, error fallback, icon states, route persistence, settings visibility regression, scoped lint, or production compilation.

Manual Execution

* Real browser audio output, browser autoplay-policy behavior, playback across every listed route/modal, a full 11-track listening cycle, visual positioning/hover inspection, forced network failures, and browser-console inspection were not run during this automated pass and remain the manual checks above.

---

Feature-002: Nothing events heal the player by 5 or 10 HP using village heal presentation

Test Status

* passed

Manual Test Steps

1. Enable Debug Mode during a gameplay turn, trigger Nothing for Field, Hills, Gravel, Mud, and Stream in English and Japanese, and inspect each event.
   Expected: The selected environment background and existing localized flavour text remain correct, with the current player's image and shared health bar visible.
2. Trigger Nothing below max health and inspect the event immediately and after one second.
   Expected: Continue starts disabled, health remains unchanged during the delay, then real player health increases by either 5 or 10 and Continue enables while the shared health bar animates to the new value.
3. Trigger deterministic 5 HP and 10 HP outcomes while the player's max health is above 100 from health tokens.
   Expected: Both allowed amounts work and the health bar uses the player's actual increased maximum rather than a hard-coded 100.
4. Trigger a 10 HP outcome at 96/100 HP and again at 100/100 HP.
   Expected: The first result ends at 100, the full-health result remains at 100, and both presentations resolve normally after the delay.
5. Click Continue after healing resolves.
   Expected: The healed value persists in game state, gameplay returns, the next player becomes current, and the next-turn modal appears before normal controls are available.
6. Land normally on a square whose selected landing event is Nothing.
   Expected: The same background, flavour text, delayed healing, persistence, Continue gating, and turn flow occur as in the debug-triggered route.
7. Compare Nothing healing with village Rest and inspect the browser console.
   Expected: Player image sizing, health-bar presentation, 1-second timing, black Unkempt health text, and Continue gating match; no application errors occur.

Automated Tests Completed

* The pre-implementation focused run produced 10 expected failures for the missing 5/10 selector, player image, health meter, delayed Continue, clamping, full-health handling, and village-style layout while 14 existing Nothing checks remained green.
* All 24 focused Nothing data and page tests pass across equal-chance boundary selection, every bilingual environment, exact existing text/backgrounds, 5 HP and 10 HP outcomes, max health above 100, 96/100 clamping, full health, player imagery, shared health styling, persistent context health, invalid encounters, and next-turn progression.
* The real app debug Nothing regression passes from a 90 HP Stream encounter through a deterministic 5 HP delayed heal, enabled Continue, gameplay return, next-player state, and pending next-turn modal.
* The normal Stream Nothing board-landing routing regression passes, confirming normal event selection reaches the same Nothing route; the older app-level test logs JSDOM's known unimplemented audio playback warning from the global music player but exits successfully.
* All 12 Village page tests pass, confirming the existing village Rest delay, healing, action choices, rewards, backgrounds, Wandsmith flow, and turn progression remain unchanged.
* The broader combined Village and board-event run passed 20 checks and retained one unrelated stale board-event Debug Mode fixture; the focused normal Nothing route passes independently.
* Scoped ESLint passes for the changed Nothing production and test files plus the updated app-level debug regression; this project has no `npm run lint` script.
* `npm run build` compiles the optimized production build successfully.
* Scoped `git diff --check` passes for every Feature-002 file; the repository-wide check remains blocked only by pre-existing trailing whitespace in the unrelated dirty `StartPage.css`.

Automated Tests Still Needed

* None for heal selection, delayed resolution, context persistence, max-health clamping, full-health safety, bilingual environment preservation, shared health presentation contracts, debug flow, normal route selection, village regression, scoped lint, or production compilation.

Manual Execution

* Interactive browser checks for audible/visual timing, every bilingual environment, naturally selected random outcomes, health-token maximums, normal landing resolution, health persistence into a later battle, visual comparison with village Rest, and browser-console inspection were not run during this automated pass and remain the manual checks above.

---

Feature-003: Update SOS potion data descriptions

Test Status

* passed

Manual Test Steps

1. View SOS in an English potion tooltip, reward/replacement display, and gameplay potion slot where available.
   Expected: Its name remains `SOS` and its description is exactly `Teleport to the nearest village`.
2. Repeat the same displays for a Japanese player.
   Expected: Its name remains `SOS`, its description is exactly `最寄りの村にテレポートする`, and no trailing `。` appears.
3. Inspect SOS metadata through any debug or reward display that exposes it.
   Expected: SOS remains a Common Board potion using the existing light-blue colour.
4. Use SOS with one nearest generated village.
   Expected: Confirmation works normally, the potion is consumed, the player teleports to that village, and the normal village visit begins.
5. Use SOS where two generated villages are equally near, then test a board with no generated villages.
   Expected: Ties still resolve randomly; no-destination use safely retains the potion and does not move the player.
6. Inspect the browser console throughout the display and use flows.
   Expected: No application errors occur.

Automated Tests Completed

* The pre-change focused run produced exactly 3 expected failures for the Japanese trailing `。`, while 10 existing potion data and shared-tooltip checks remained green.
* All 13 focused potion source-of-truth and shared `PotionIcon` tests pass, covering exact bilingual SOS descriptions, Board availability, Common rarity, light-blue colour, unchanged names, English and Japanese tooltip/accessibility output, and absence of deprecated field-village wording from potion data.
* All 5 focused SOS gameplay regressions pass for cancellation, nearest-village teleport and visit startup, potion consumption, no-generated-village safety, Japanese confirmation, nearest-village selection, and random tie resolution.
* Scoped ESLint passes for the changed potion source and tests; this project has no `npm run lint` script.
* `npm run build` compiles the optimized production build successfully.
* Scoped `git diff --check` passes for every Feature-003 file.

Automated Tests Still Needed

* None for exact source metadata, shared tooltip propagation, deprecated wording exclusion, nearest-village behavior, tie handling, no-destination safety, Japanese confirmation, scoped lint, or production compilation.

Manual Execution

* Interactive browser inspection of every potion-description surface, favicon-independent browser-console checks, and a complete SOS use flow were not run during this automated pass and remain the manual checks above.

---

Feature-004: Fix overflowing modal action button positioning

Test Status

* passed

Manual Test Steps

1. Open Debug, reduce the browser height until the panel overflows, and scroll to the end.
   Expected: All debug controls remain readable and the existing Close button appears after them at the bottom of the scrollable content without overlaying anything.
2. Open Settings and then the Settings Rules view at a short browser height.
   Expected: The existing actions remain reachable after their content, including Back to Settings, with no visual or behavioral changes.
3. Open a full-slot potion replacement, token assignment, and potion confirmation modal.
   Expected: Choices, token/potion displays, and form controls remain above the existing action buttons and can be reached by vertical scrolling.
4. Open Spells from gameplay and through Village Wandsmith, including enough token content to make the modal tall.
   Expected: Save and Cancel remain after the spell content; existing horizontal-overflow handling and button arrangement remain unchanged.
5. Check loot chest, decision, hazard, village choice/heal, and a short non-overflowing modal.
   Expected: Actions remain bottom-centred after content, long panels scroll vertically, and short modals retain their prior layout.
6. Inspect the browser console throughout these checks.
   Expected: No application errors occur.

Automated Tests Completed

* The pre-implementation focused regression failed on the new non-shrinking modal-body contract as expected, demonstrating the existing flex collapse that allowed overflowing body content to overlap actions.
* All 8 shared Modal tests pass for body/action source order, panel max-height scrolling, non-shrinking body and actions, no overlay positioning, existing action grouping, default styling, and isolated dice-modal behavior.
* All 21 DebugModal tests pass, including the new regression confirming Close is outside the debug body and inside the following shared action row after all debug content.
* The complete Loot Chest Found, Tokensmith replacement, potion confirmation, Rules page, and Village page suites pass alongside the debug suite, covering representative loot, full-bag token, potion, rules, village, and Wandsmith consumers.
* The broader Spells, Hazard, and app-settings suites retain four unrelated stale assertions for previously updated bilingual spell instructions, hazard title spacing, and an assigned debug enemy's 85 HP; their remaining 22 behavioral checks pass and none of those failures involve modal action flow.
* Scoped ESLint passes for the shared Modal component/tests and DebugModal regression; this project has no `npm run lint` script.
* `npm run build` compiles the optimized production build successfully.
* Scoped `git diff --check` passes for every Feature-004 code, test, and vibe file.

Automated Tests Still Needed

* None for shared modal structure, scroll ownership, action ordering, non-overlay positioning, debug Close placement, representative consumer behavior, scoped lint, or production compilation.

Manual Execution

* Browser-height reduction, real scrolling, visual overlap inspection, every listed modal state, and browser-console inspection were not run during this automated pass and remain the manual checks above.

---

Feature-005: Hide normal potions section when Active Potion section is visible

Test Status

* passed

Manual Test Steps

1. Start an English and Japanese gameplay turn with no active potion and at least one normal potion.
   Expected: The localized normal Potions section, slots, capacity, tooltips, and eligible Use controls are visible; Active Potion is absent.
2. Activate Roll Choice and select a roll.
   Expected: The normal Potions section disappears, Active Potion shows Roll Choice and the selected number, and no empty potion-area gap remains.
3. Roll to resolve Roll Choice without advancing away from the current gameplay view.
   Expected: Active Potion disappears and the normal Potions section returns with its updated inventory.
4. Activate Metal Detector and then Smokescreen in separate turns.
   Expected: Only Active Potion is shown while each effect remains active; normal potion slots, tooltips, capacity, and Use controls are not visible.
5. Apply Spellbound, Heavy Weight, Troublemaker, Devine Chance, or another target/roll-waiting active effect to a player and advance to that player where required.
   Expected: That player sees only their Active Potion section until the effect clears, while another player without an active potion sees their own normal inventory.
6. Enter battle with battle potions available.
   Expected: The existing battle potion section and battle-potion usage rules are unchanged.
7. Inspect the gameplay layout and browser console throughout these flows.
   Expected: Hiding the normal section removes its vertical space and produces no application errors.

Automated Tests Completed

* The pre-implementation GameplayPage run produced exactly 5 Feature-005 visibility failures for Metal Detector, Smokescreen, targeted Spellbound state, Roll Choice, and an initially active localized potion while 62 existing checks remained green.
* The completed GameplayPage run passes 67 of 75 checks, including all 7 Feature-005 scenarios for the no-active baseline, activation, resolution, Metal Detector, Smokescreen, player-specific Spellbound state, and Devine Chance while storm-blocked.
* The remaining 8 GameplayPage failures are unrelated stale assertions for token tooltip text, expanded spell instructions, random Buy and Sell rewards, dice-result copy, and previously changed player/spell image sizing.
* All 17 existing `ActivePotionSection` and `PotionList` component tests pass, confirming their rendering, localization, capacity, buttons, tooltip, and styling contracts remain unchanged.
* Production-only scoped ESLint passes for `GameplayPage.jsx`; linting the existing `GameplayPage.test.jsx` still reports 12 unrelated pre-existing `testing-library/no-node-access` violations outside the Feature-005 changes.
* `npm run build` compiles the optimized production build successfully.
* Scoped `git diff --check` passes for every Feature-005 code, test, and vibe file.

Automated Tests Still Needed

* None for the requested current-player visibility rule, active-state transitions, named persistent effects, localization, component regressions, production lint, or production compilation.

Manual Execution

* Interactive browser inspection of vertical space, every active-potion effect, battle potion isolation, player switching, tooltip absence, and browser-console output was not run during this automated pass and remains covered by the manual steps above.

---

Feature-006: Show token bag capacity and use Unkempt for Japanese spell column numbers

Test Status

* passed

Manual Test Steps

1. Open the normal Spells modal with an empty, partially filled, and full token bag.
   Expected: The localized heading shows the real count as `Token Bag 0/5`, `Token Bag 3/5`, and `Token Bag 5/5`, or the Japanese equivalent.
2. Switch to Japanese and inspect the token-bag heading and normal spell columns.
   Expected: The Japanese title remains in the Japanese font while its capacity, column labels, and column capacities use `Unkempt`.
3. View edge columns, a Grey-adjusted column, and a merged column such as `1+2`.
   Expected: Every numeric label and capacity uses `Unkempt`, and the existing effective capacities remain unchanged.
4. Open first assignment, Wandsmith, and Redo spell modification in both languages.
   Expected: Each shared view shows the correct live bag count and keeps all existing token movement behavior.
5. Open Decision and battle reward token assignment, then stage a reward in the bag and stage a full-bag replacement.
   Expected: The same capacity heading appears, an added staged token increases the displayed count, and a staged replacement preserves the count.
6. Inspect the browser console throughout these flows.
   Expected: No application errors occur.

Automated Tests Completed

* The initial focused test command timed out before Jest emitted output; its orphaned Node processes were stopped before the successful retry.
* All 24 focused `SpellTokenAssignment` tests pass for empty/partial/full capacity formatting, staged reward counts, Japanese label separation, `Unkempt` numeric classes, normal and edge columns, Grey-adjusted capacity, merged `1+2` labels/effective capacity, Redo movement, and reward assignment.
* The complete `SpellsModal` run passes all 9 relevant normal, first-setup, Wandsmith, Redo, layout, and capacity-title checks in English and Japanese.
* The same modal run retains 2 unrelated stale bilingual instruction-copy assertions from earlier features; neither failure involves token-bag capacity, numeric classes, or Feature-006 behavior.
* Production-only scoped ESLint passes for `SpellTokenAssignment.jsx`; the existing test file retains 2 unrelated pre-existing `testing-library` violations at its old merged-column `container.querySelectorAll` assertion.
* `npm run build` compiles the optimized production build successfully.
* Scoped `git diff --check` passes for every Feature-006 code and test file.

Automated Tests Still Needed

* None for shared capacity rendering, live displayed counts, existing maximum-capacity reuse, Japanese number-font hooks, Grey/merged capacities, mode reuse, scoped production lint, or production compilation.

Manual Execution

* Interactive browser font rendering, drag-and-drop count updates, every listed modal entry path, and browser-console inspection were not run during this automated pass and remain the manual checks above.

---

Feature-007: Update browser tab title and favicon

Test Status

* passed

Manual Test Steps

1. Start the local development server and open the app.
   Expected: The browser tab title is `Spellix` and the tab icon uses the supplied PNG rather than the default React icon.
2. Navigate between app routes and refresh a nested hash route.
   Expected: The title and favicon remain unchanged and no route introduces conflicting metadata.
3. Preview the production build under the configured `/spellix/` base path.
   Expected: The title remains `Spellix` and `/spellix/favicon.png` loads successfully.
4. Add or inspect a bookmark/favourite where the browser supports an icon.
   Expected: The site is identified as `Spellix` and uses the new favicon where supported.
5. If the old icon appears, hard refresh, use a private window, or clear the browser favicon cache.
   Expected: The supplied favicon appears after cached metadata is bypassed.
6. Inspect the browser console and network panel.
   Expected: The title, manifest, and favicon load without application or missing-asset errors.

Automated Tests Completed

* The pre-implementation focused metadata run failed on the old `React App` title as expected; the later manifest regression failed on its remaining old short name before that metadata was updated.
* All 3 focused deployment tests pass for the existing GitHub Pages contract, hash routing, exact `Spellix` HTML title, one `%PUBLIC_URL%/favicon.png` link, source/public byte parity, manifest names, PNG dimensions, and absence of old `.ico` manifest references.
* Scoped ESLint passes for `deploymentConfig.test.js`; this project has no `npm run lint` script.
* `npm run build` compiles the optimized production build successfully with the configured `/spellix/` base path.
* The generated HTML contains `<title>Spellix</title>` and `/spellix/favicon.png`, while the generated manifest contains the `Spellix` name and PNG icon entry.
* SHA-256 hashes match across `src/images/favicon.png`, `public/favicon.png`, and `build/favicon.png`, confirming the supplied image is copied unchanged into production.
* Scoped `git diff --check` passes for every Feature-007 text file.

Automated Tests Still Needed

* None for source/public asset parity, exact metadata, manifest identity, GitHub Pages path generation, scoped lint, or production compilation.

Manual Execution

* A real browser tab, favicon cache behavior, bookmark display, local development server, production preview server, network panel, and browser console were not interactively inspected during this automated pass and remain the manual checks above.

---

Feature-008: Remove starting Guard tokens

Test Status

* passed

Manual Test Steps

1. Start new games with 2, 3, 4, 5, and 6 players.
   Expected: Every player starts with exactly 5 protected Red Damage tokens and no Blue Guard tokens.
2. Open first spell assignment in English and Japanese.
   Expected: Only the 5 Red Damage tokens are available, the bag heading shows `5/5`, and the warning requires all 5 starting tokens.
3. Assign fewer than 5 starting tokens, then assign all 5.
   Expected: Confirmation remains disabled until all 5 are assigned, then becomes enabled.
4. Trigger battle loss or death token removal after earning removable tokens.
   Expected: The 5 starting Red Damage tokens remain protected without any seven-token assumption.
5. Earn a Blue Guard token through a normal reward.
   Expected: Guard can still be earned and behaves normally; reward pools and capacities are unchanged.
6. Load an older save containing a Blue Guard token in a player bag.
   Expected: The existing bag is preserved rather than migrated or stripped.
7. Inspect the browser console throughout these flows.
   Expected: No application errors occur.

Automated Tests Completed

* The pre-implementation focused run produced exactly 8 expected Feature-008 failures while 58 existing checks remained green.
* All 59 focused game setup, spell setup, translation, and death-protection tests pass, including 2- through 6-player setup, five-token completion, protected starting metadata, and existing-bag preservation.
* All 56 reward, battle-reward, and save-game compatibility tests pass, confirming earned Guard rewards and save serialization remain unchanged.
* The complete GameplayPage run passes 70 of 75 checks, including every Feature-008 first-assignment check at 0, 1, 4, and 5 assigned tokens. Its 5 remaining failures are unrelated stale assertions for Black tooltip copy, random Buy and Sell rewards, dice-result copy, player-piece sizing, and spell-player image sizing.
* The complete SpellsModal run passes 10 of 11 relevant checks for the five-red-token bag, `5/5` capacity, no starting Guard, assignment completion, and bilingual warning. Its remaining failure is an unrelated stale `20px` warning-size assertion against the existing `18px` stylesheet.
* The focused App test command could not produce a usable result because the suite ran globally, emitted JSDOM `HTMLMediaElement.play` errors, and crashed in source-map support before tests executed.
* Scoped ESLint passes for all changed production files and focused unit tests. The existing App, SpellsModal, and GameplayPage integration test files retain 27 unrelated pre-existing Testing Library lint violations.
* `npm run build` compiles the optimized production build successfully.
* The stale-assumption search finds no production references to seven starting tokens or two starting Guard tokens.
* Scoped `git diff --check` passes for every Feature-008 code, test, and vibe file.

Automated Tests Still Needed

* None for new-game token creation, first-assignment completion, protection metadata, existing-save preservation, reward compatibility, localization, scoped lint, or production compilation.

Manual Execution

* Interactive 2- through 6-player setup, browser drag-and-drop assignment, earned Guard rewards, an actual older save, death flow, and browser-console inspection were not run during this automated pass and remain covered by the manual steps above.

---

Bug-001: Favicon is not appearing

Test Status

* blocked by stale GitHub Pages deployment

Manual Regression Steps

1. Start the local development server and open the app in a private window.
   Expected: The tab title is `Spellix`, the browser requests `/spellix/favicon.png`, and the Spellix PNG appears without a network error.
2. Build and preview the production output under the configured `/spellix/` base path.
   Expected: The generated HTML references `/spellix/favicon.png`, the request returns `200 image/png`, and the tab displays the Spellix favicon.
3. Deploy the verified production build to GitHub Pages, then open `https://luket0207.github.io/spellix/` in a private window.
   Expected: The live HTML title is `Spellix`, its only favicon link points to `/spellix/favicon.png`, and that URL returns the intended PNG rather than `404`.
4. Hard refresh and reopen the live tab if an old icon remains cached.
   Expected: The new PNG appears after the stale favicon cache is bypassed.

Automated Tests Completed

* The live GitHub Pages check reproduces the bug: the page returns `200` but still contains `React App` and `/spellix/favicon.ico`, while `/spellix/favicon.png` returns `404`.
* The exact local development favicon URL returns `200 image/png`, has a `998908`-byte body, and matches `src/images/favicon.png` by SHA-256.
* All 3 focused deployment tests pass for the CRA GitHub Pages contract, hash routing, exact `Spellix` metadata, one PNG favicon link, source/public byte parity, and manifest metadata.
* Scoped ESLint passes for `deploymentConfig.test.js`; this project has no `npm run lint` script.
* `npm run build` compiles successfully for `/spellix/`, emits the correct PNG favicon link, and copies `favicon.png` into the production output.

Automated Tests Still Needed

* Recheck the live HTML and `/spellix/favicon.png` response after an explicitly approved deployment.

Manual Execution

* A real browser tab and favicon cache were not interactively inspected. The live HTTP failure and local HTTP success were reproduced directly.

---

Bug-002: Favicon still not appearing after adding ico file

Test Status

* passed

Manual Regression Steps

1. Start the local development server and open the app in a private window.
   Expected: The title remains `Spellix`, the tab shows the supplied favicon, and the page contains only the ICO favicon link.
2. Inspect the Network panel request for `/spellix/favicon.ico`.
   Expected: The request returns `200` with an icon content type and does not fall back to HTML or return `404`.
3. Build and preview the production output under `/spellix/`.
   Expected: The generated HTML references `/spellix/favicon.ico`, the copied asset loads, and the tab displays the supplied favicon.
4. Hard refresh, reopen the tab, or clear the favicon cache if the previous PNG/default icon remains.
   Expected: The ICO-linked asset appears after stale browser metadata is bypassed.
5. After a separately approved deployment, repeat the private-window and Network-panel checks on GitHub Pages.
   Expected: The live site serves the new ICO metadata and asset rather than the older deployed favicon configuration.

Automated Tests Completed

* The pre-implementation focused run passed 2 checks and failed exactly 1 regression because the HTML still linked `%PUBLIC_URL%/favicon.png` instead of the required ICO.
* All 3 focused deployment tests pass after implementation for the CRA GitHub Pages contract, hash routing, exact `Spellix` title, one ICO favicon link, source/public ICO parity, and unchanged PNG manifest metadata.
* The exact local development URL `/spellix/favicon.ico` returns `200 image/x-icon`, has a `998908`-byte body, and matches `src/images/favicon.ico` by SHA-256.
* `npm run build` compiles successfully for `/spellix/`; generated HTML uses `/spellix/favicon.ico`, and `build/favicon.ico` matches the source asset by SHA-256.
* Scoped ESLint passes for `deploymentConfig.test.js`; this project has no `npm run lint` script.

Automated Tests Still Needed

* None for the source metadata, single-link cleanup, local HTTP delivery, build output, asset parity, scoped lint, or production compilation.

Manual Execution

* Visual inspection in a real browser, favicon cache clearing, production preview in a browser, and post-deployment GitHub Pages verification were not run. Bug-001 remains blocked on that separate deployment step.

---

Feature-009: Standardise battle overlay icon width

Test Status

* passed

Manual Test Steps

1. Trigger Guard for both the player and enemy in battle.
   Expected: Each shield is `80px` wide with automatic height, remains centred, and its guard amount stays centred over it.
2. Trigger Deflect and Freeze on both battle actors.
   Expected: The ban and snowflake icons remain centred at `80px` width while their existing enter, shake, fade, and exit animations remain unchanged.
3. Use a healing potion during battle.
   Expected: The bottle is `80px` wide, grows by its existing scale animation, shakes three times, fades, and remains centred over the player.
4. Use Cosmic Intervention and Shields Down.
   Expected: The bolt and gavel are `80px` wide with automatic height and retain their existing movement, flicker, timing, centring, and resolution behavior.
5. Trigger lethal damage against the player and enemy.
   Expected: Each skull is `80px` wide, remains centred while growing and shaking, and battle completion still waits for the animation.
6. Use a healing potion on the normal gameplay board.
   Expected: The existing non-battle healing animation remains unchanged.
7. Inspect the browser console throughout these flows.
   Expected: No application errors occur.

Automated Tests Completed

* The pre-implementation focused run passed 61 of 75 checks and produced 10 expected Feature-009 failures for the missing shared size rule/classes and old actor-sized healing behavior, plus 4 unrelated stale battle assertions.
* The completed focused battle and healing run passes 70 of 75 checks, including every Feature-009 class, fixed-size, centring, animation, and effect-resolution assertion.
* The 5 remaining battle failures are unrelated stale assertions for panel background sizing, battle spacing, death-result copy, random-call count, and an existing guard-flow state expectation.
* The complete GameplayPage run passes 70 of 75 checks, including the non-battle Small Heal animation regression. Its 5 remaining failures are unrelated stale tooltip, random reward, dice copy, and player-image sizing assertions.
* Production-only scoped ESLint passes for `BattlePage.jsx`, `HealingPotionAnimation.jsx`, and the focused healing unit test.
* The existing `BattlePage.test.jsx` retains 104 pre-existing `testing-library/no-node-access` violations; Feature-009 added no direct DOM-access pattern.
* `npm run build` compiles the optimized production build successfully.
* The source audit confirms all seven current battle overlay types use the shared class and none retain actor-sized width or height declarations.
* Scoped `git diff --check` passes for every Feature-009 code, test, and vibe file.

Automated Tests Still Needed

* None for shared class coverage, fixed width/automatic height, existing keyframe preservation, effect resolution, non-battle healing isolation, scoped production lint, or production compilation.

Manual Execution

* Real-browser visual size, natural aspect ratio, centring, animation playback, and browser-console inspection were not run and remain covered by the manual steps above.

---

Feature-010: Change battle death icon colour to light grey

Test Status

* passed

Manual Test Steps

1. Trigger lethal damage against an enemy.
   Expected: The `faSkullCrossbones` icon appears after health reaches 0, is `#BABABA`, remains centred at `80px` width with automatic height, and completes its grow-and-shake animation before battle resolution.
2. Trigger lethal damage against the player.
   Expected: The same light-grey, centred death icon and unchanged animation complete before the loss flow continues.
3. Trigger or inspect any separate respawn skull and other grey icons.
   Expected: Their colours and behavior remain unchanged.
4. Inspect the browser console throughout both lethal-damage flows.
   Expected: No application errors occur.

Automated Tests Completed

* The pre-implementation BattlePage run passed 60 of 66 checks and failed the new Feature-010 regression on the old `#555` colour, alongside 5 unrelated stale battle assertions.
* The completed BattlePage run passes 61 of 66 checks, including the Feature-010 colour regression and existing player/enemy death-flow checks that wait for the animation before resolving battle.
* The same 5 unrelated stale failures remain for panel background sizing, battle spacing, death-result copy, enemy-freeze random-call count, and an existing guard-flow state expectation.
* Scoped production ESLint passes for `BattlePage.jsx`.
* `npm run build` compiles the optimized production build successfully.
* The source audit confirms only `.battle-death-icon` uses `#BABABA`; `faSkullCrossbones`, the shared `80px`/automatic-height class, centred placement, grow-and-shake keyframes, and `1.5s` timing remain unchanged.
* The existing `BattlePage.test.jsx` retains 104 pre-existing `testing-library/no-node-access` violations; Feature-010 changed only a static CSS expectation in that file.
* Scoped `git diff --check` passes for every Feature-010 code, test, and vibe file.

Automated Tests Still Needed

* None for the death icon colour, shared size contract, animation source contract, existing death-flow timing, scoped production lint, or production compilation.

Manual Execution

* Real-browser enemy/player lethal-damage playback, visual colour and centring inspection, respawn-icon comparison, and browser-console inspection were not run and remain covered by the manual steps above.

---

Feature-011: Token tooltips should only show description

Test Status

* passed

Manual Test Steps

1. Hover Damage, Guard, Buff, and Shiny Health tokens in the token bag and spell assignment modal.
   Expected: Each cursor tooltip contains only the token description, such as `Plus 10 Damage`, with no repeated token name.
2. Check token tooltips in Wandsmith, Redo spell editing, reward assignment, battle reward choices, committed spells, battle spell columns, and debug token displays.
   Expected: Every token tooltip uses the same description-only content and existing cursor placement.
3. Switch between English and Japanese and hover several tokens.
   Expected: The tooltip contains only the description in the selected language.
4. Inspect reward cards, rules token lists, selected reward summaries, and any token display that intentionally shows a resting name.
   Expected: Token names remain visible outside the tooltip where the existing UI intentionally renders them.
5. Hover potion icons in normal and editing flows.
   Expected: Potion tooltip content and behavior remain unchanged.
6. Inspect the browser console throughout these flows.
   Expected: No application errors occur.

Automated Tests Completed

* The pre-implementation focused run passed 150 of 174 checks and produced 18 expected Feature-011 failures for name-plus-description token titles, alongside 6 unrelated stale assertions.
* The identical completed run passes 168 of 174 checks, including every updated English/Japanese token tooltip assertion across the shared component, token bag and spell assignment, committed spells, rewards, debug displays, and the existing Gameplay death-token regression.
* The same 6 unrelated stale failures remain for token glow timing, reward potion-grid styling, random Buy and Sell rewards, dice-result copy, current-player image alignment, and spell-player image height.
* All 27 Wandsmith/Buy and Sell, Redo/Copy-Paste, Tokensmith, and `PotionIcon` isolation checks pass.
* Scoped production ESLint passes for `Token.jsx`.
* The changed test files retain 56 pre-existing Testing Library direct-node/container lint violations in `CommittedSpellSlots.test.jsx`, `SpellTokenAssignment.test.jsx`, and `RewardPage.test.jsx`; Feature-011 only changed title expectations and added no direct-node access.
* `npm run build` compiles the optimized production build successfully.
* The source audit confirms all current token consumers reuse `Token`, its native title uses only `tooltipDescription`, no name-description concatenation remains, and no potion source file changed.
* Scoped `git diff --check` passes for every Feature-011 code, test, and vibe file.

Automated Tests Still Needed

* None for shared token-tooltip content, English/Japanese localization, intentional visible names, Wandsmith and Redo consumers, reward and committed-spell consumers, potion isolation, scoped production lint, or production compilation.

Manual Execution

* Real-browser cursor tooltip rendering and placement, every listed gameplay surface, visible-name review, potion comparison, and browser-console inspection were not run and remain covered by the manual steps above.

---

Feature-012: Add Browser Compatibility section to top of rules text

Test Status

* passed

Manual Test Steps

1. Open Rules from the start page in English.
   Expected: `Browser Compatibility` appears once as the first rules section before `Tokens`, with the supplied browser-zoom paragraph.
2. Switch the Rules page to Japanese.
   Expected: `ブラウザの互換性` appears once as the first section before `トークン`, with the supplied Japanese paragraph.
3. Open Rules from gameplay Settings in English and Japanese.
   Expected: The same compatibility section and selected-language text appear once at the top of the shared rules sections.
4. Scroll through both Rules views and use their existing Back controls.
   Expected: Existing scrolling, section styling, language fonts, navigation, and modal behavior remain unchanged.
5. Inspect browser zoom controls and the browser console.
   Expected: The text provides guidance only, no custom zoom control was added, and no application errors occur.

Automated Tests Completed

* The pre-implementation Rules page run passed 7 of 9 checks and failed exactly the 2 new English/Japanese first-section regressions because `Tokens` and `トークン` were still first.
* The completed Rules page suite passes all 9 checks for exact bilingual copy, first-section ordering, one occurrence per language, rendered content, language switching, existing navigation, existing sections, and scrollable modal styling.
* Two existing App settings-modal tests now assert the exact English/Japanese compatibility heading and paragraph, but the App suite cannot execute them: it emits unrelated JSDOM `HTMLMediaElement.play` errors and crashes in `source-map-support` before reporting any tests, even when a test-name filter is requested.
* Scoped production ESLint passes for `rulesContent.js`, and scoped test ESLint passes for `RulesPage.test.jsx`.
* `App.test.jsx` retains 14 pre-existing Testing Library/Jest lint violations outside the Feature-012 assertions; the new assertions add none.
* `npm run build` compiles the optimized production build successfully.
* The source audit confirms each language has exactly one compatibility section at `sections[0]`, `RulesBody` renders the arrays in order, and both `RulesPage` and the settings modal render that same shared component.
* Scoped `git diff --check` passes for every Feature-012 code, test, and vibe file.

Automated Tests Still Needed

* Rerun the two settings-modal integration tests after the unrelated App test harness audio/source-map crash is fixed.

Manual Execution

* Real-browser start-page and settings Rules flows, visual first-section placement, scrolling, language fonts, browser zoom guidance, and console inspection were not run and remain covered by the manual steps above.

---

Feature-013: Add Charge and Buff battle animations

Test Status

* passed

Manual Test Steps

1. Roll a player column containing Charge, Buff, Guard, Damage, and Freeze.
   Expected: Yellow Charge and purple Buff bolts play over the player for 1.5 seconds each, followed by Guard, Damage, and Freeze without overlap.
2. Trigger Charge and Buff during an enemy turn.
   Expected: Both bolts play over the enemy caster, with Charge before Buff.
3. Inspect both bolts over each battle actor.
   Expected: They reuse the Cosmic Intervention jitter, flash, fade, timing, and centred `80px` width with automatic height; Buff uses `#69419e`.
4. Use Cosmic Intervention.
   Expected: Its existing yellow bolt still plays over the opponent and resolves the potion normally.
5. Try dice, potion, and debug battle controls while Charge or Buff is playing.
   Expected: All controls remain disabled until the animation queue resolves.
6. Trigger lethal damage in a queue that also contains Charge, Buff, and Freeze.
   Expected: The death icon and battle result wait until every queued animation completes.
7. Inspect the browser console throughout these flows.
   Expected: No application errors occur.

Automated Tests Completed

* The pre-implementation BattlePage run passed 61 of 68 checks and produced exactly 2 expected Feature-013 failures for missing Charge/Buff overlays, alongside 5 unrelated stale battle assertions.
* The completed BattlePage run passes 63 of 68 checks, including player and enemy caster targeting, explicit priority sorting, 1.5-second Charge/Buff timing, shared bolt classes, Buff colour, control locking, Guard/Damage/Freeze ordering, and lethal completion after the full queue.
* The same 5 unrelated stale failures remain for panel background sizing, battle spacing, death-result copy, enemy-freeze random-call count, and an existing guard-flow state expectation.
* Scoped production ESLint passes for `GameSetupContext.jsx` and `BattlePage.jsx`.
* `npm run build` compiles the optimized production build successfully.
* `git diff --check` passes with line-ending warnings only.
* The source audit confirms Charge and Buff calculations remain in `calculateBattleTurn`, while only visual queue events, priority scheduling, overlay rendering, and control locking changed.

Automated Tests Still Needed

* Rerun `GameSetupContext.test.jsx` after its unrelated `source-map-support` mapping crash is fixed; the suite currently fails before discovering any tests even after clearing Jest's cache.

Manual Execution

* Real-browser animation playback, visual colour/centring inspection, controls, Cosmic Intervention comparison, lethal flow, and console inspection were not run and remain covered by the manual steps above.

---

Feature-014: Update Charge token description

Test Status

* passed

Manual Test Steps

1. View a Charge token in an English token bag, spell assignment, reward, battle, or debug display.
   Expected: Its description-only tooltip says `Charge all your slots next turn, increasing their damage or guard by 5`.
2. Switch to Japanese and view a Charge token.
   Expected: Its description-only tooltip says `次のターン、すべてのスロットをチャージし、ダメージまたはガードを5増加させる。`.
3. Inspect the Charge token name, colour, and reward category.
   Expected: The names remain `Charge` and `チャージ`, the token remains Yellow, and rarity remains Rare.
4. Trigger Charge in battle and resolve the following charged turn.
   Expected: Charge still affects all slots, adds 5 to applicable damage or guard, and uses the existing Charge bolt animation.
5. Inspect the browser console during these checks.
   Expected: No application errors occur.

Automated Tests Completed

* The pre-implementation token run passed 17 of 20 checks and produced exactly 2 expected Feature-014 failures for the old English/Japanese Charge wording, alongside 1 unrelated stale glow-duration assertion.
* The completed token run passes 19 of 20 checks, including exact bilingual data, unchanged name/rarity metadata, shared tooltip propagation, and description-only tooltip content; only the same unrelated glow assertion remains.
* All 51 `battleTurn` checks pass, including existing Charge activation, next-turn damage/guard increase, use consumption, and unchanged non-Charge behavior.
* The BattlePage suite passes 63 of 68 checks, including the existing Charge animation and queue regression; the same 5 unrelated stale battle assertions remain.
* Scoped ESLint passes for `tokens.js`, `tokens.test.js`, and `Token.test.jsx`.
* `npm run build` compiles the optimized production build successfully.
* The source audit confirms existing token surfaces reuse shared token data/helpers, and the old runtime Charge wording no longer remains in current source data.
* Scoped `git diff --check` passes with line-ending warnings only.

Automated Tests Still Needed

* None for the shared bilingual data, description-only tooltip propagation, unchanged metadata, Charge mechanics, animation regression, scoped lint, or production compilation.

Manual Execution

* Real-browser checks across every listed token surface, visual Yellow/rare confirmation, battle playback, and console inspection were not run and remain covered by the manual steps above.

---

Bug-003: Correct Charge token description value from 5 to 10

Test Status

* passed

Manual Test Steps

1. View a Charge token in an English token bag, spell assignment, reward, battle, or debug display.
   Expected: Its description-only tooltip says `Charge all your slots next turn, increasing their damage or guard by 10` and does not contain the old `5` wording.
2. Switch to Japanese and view a Charge token.
   Expected: Its description-only tooltip says `次のターン、すべてのスロットをチャージし、ダメージまたはガードを10増加させる。`.
3. Inspect the Charge token metadata.
   Expected: The names remain `Charge` and `チャージ`, colour remains Yellow, and rarity remains Rare.
4. Trigger Charge in battle and resolve the following charged turn.
   Expected: Existing Charge mechanics and the Charge bolt animation remain unchanged.
5. Inspect Buff and the Charger potion.
   Expected: Their existing copy and behavior are unchanged.
6. Inspect the browser console throughout these checks.
   Expected: No application errors occur.

Automated Tests Completed

* The reproduction run passed 17 of 20 token checks and failed exactly 2 new Bug-003 assertions on the old English/Japanese `5` wording, alongside 1 unrelated stale glow-duration assertion.
* The completed token run passes 19 of 20 checks, including exact bilingual `10` data, shared description-only tooltip propagation, and unchanged name/rarity metadata; only the same unrelated glow assertion remains.
* All 51 `battleTurn` checks pass, confirming Charge calculations and activation remain unchanged and independent of description data.
* The BattlePage suite passes 63 of 68 checks, including the existing Charge animation regression; the same 5 unrelated stale battle assertions remain.
* Scoped ESLint passes for `tokens.js`, `tokens.test.js`, and `Token.test.jsx`.
* `npm run build` compiles the optimized production build successfully.
* The source audit confirms no Charge runtime description or focused assertion retains the incorrect `5` wording; remaining Buff `5` wording is intentionally unchanged.
* Scoped `git diff --check` passes with line-ending warnings only.

Automated Tests Still Needed

* None for the corrected bilingual data, shared tooltip propagation, unchanged metadata/mechanics/animation, scoped lint, or production compilation.

Manual Execution

* Real-browser checks across all listed token surfaces, metadata, battle animation/mechanics, Buff/Charger isolation, and console inspection were not run and remain covered by the manual steps above.

---

Feature-015: Clean up settings menu layout and add End Game confirmation

Test Status

* passed

Manual Test Steps

1. Open gameplay Settings in English with Debug Mode enabled.
   Expected: The centred `Settings` title uses `larger-text` and the English font; Debug, Save Game, Rules, End Game, and Close appear as equal-width, centred, vertically stacked buttons in that order.
2. Turn Debug Mode off and reopen Settings.
   Expected: Debug is hidden while Save Game, Rules, End Game, and Close retain their relative order.
3. Select Close.
   Expected: Close uses the existing secondary button style and closes Settings without changing game state.
4. Open Rules from Settings and use Back to Settings.
   Expected: The shared Rules view works unchanged and returns to the main settings menu.
5. Select End Game, then select No.
   Expected: The centred confirmation asks `Are you sure you want to end the game?`; No closes the confirmation, restores Settings, and does not reset or navigate.
6. Select End Game again, then select Yes.
   Expected: The existing End Game reset runs once, all settings views close, and navigation returns to the start page.
7. Repeat the menu and both confirmation branches for a Japanese current player.
   Expected: The title, all action labels, confirmation text, Yes/No labels, accessible dialog labels, and fonts are Japanese.
8. Inspect the modal at common viewport sizes and inspect the browser console.
   Expected: Actions stay below the content without overlap, the existing modal background is unchanged, and no application errors occur.

Automated Tests Completed

* The pre-implementation focused settings run passed 16 of 20 checks and produced exactly 4 expected Feature-015 failures for the missing structured title/menu, confirmation flow, confirmed End Game flow, and Japanese localization.
* The completed focused settings run passed all then-current 20 checks, including English/Japanese labels and fonts, exact five-button order, secondary Close, No returning without navigation, Yes using the existing reset/navigation flow, and gameplay-only settings visibility across all routes.
* All 5 Save/Load integration checks pass, including unchanged English/Japanese Save Game access from Settings and existing save behavior.
* The broader settings-button suite passes 8 of 10 checks. Its 2 failures are unrelated stale debug-battle expectations for enemy data and were not changed for Feature-015.
* The monolithic App suite still crashes in `source-map-support` before reporting any tests, even when a Settings Rules name filter is supplied; its relevant Japanese dialog assertions were updated to the new localized accessible label.
* Scoped ESLint passes for `App.jsx` and the focused settings test as it stood for the successful 20-check run.
* `npm run lint` is unavailable because this project has no `lint` script.
* `npm run build` compiles the optimized production build successfully.
* Scoped `git diff --check` passes with line-ending warnings only.

Automated Tests Still Needed

* Rerun the focused settings suite and scoped test lint for the final two supplemental assertions covering hidden-Debug ordering and Rules-to-Settings return. Their attempted run was blocked by the execution approval service usage limit after the core 20-check run and production build had passed.
* Rerun the two existing Settings Rules integration checks if the unrelated App test harness `source-map-support` crash is fixed.

Manual Execution

* Real-browser layout, font, button-overlap, confirmation interaction, reset/navigation, and browser-console checks were not run and remain covered by the manual steps above.

---

Feature-016: Add Shiny Buff rare token

Test Status

* passed

Manual Test Steps

1. Inspect Shiny Buff in English and Japanese on a token surface.
   Expected: It is a rare Purple token with a Yellow outline, the name is `Shiny Buff` or the required Japanese name, and the description-only tooltip uses the required localized 10-point Buff description.
2. Generate common, rare, Elite Tower, second-Elite village, cave, decision, and debug token rewards.
   Expected: Shiny Buff can appear in every applicable rare pool and never appears in common or starting-token pools.
3. Put one Shiny Buff in a middle spell column and roll it in battle.
   Expected: The two adjacent effective columns receive 10 Buff for the next turn only; the triggering column is not buffed.
4. Put common Buff and Shiny Buff in the same column and roll it.
   Expected: Adjacent effective columns receive 15 total Buff and one existing purple Buff animation plays over the triggering character.
5. Repeat with an edge column and merged columns.
   Expected: Edge columns buff only their one neighbour, and merged columns use existing effective-column adjacency for either merged roll number.
6. Save and load a game containing Shiny Buff, then inspect the browser console throughout the above flows.
   Expected: The token round-trips unchanged, older saves remain valid, and no application errors occur.

Automated Tests Completed

* The pre-implementation focused run passed 116 of 127 checks and produced exactly 10 expected Feature-016 failures for missing token data, visuals/localization, battle value, rare-pool availability, village availability, and debug labels, alongside 1 unrelated stale glow-duration assertion.
* The completed focused run passes 134 of 135 checks across token data/UI, battle calculations, rewards, villages, cave rewards, debug tools, and save/load; only the same unrelated stale glow-duration assertion remains.
* All 54 `battleTurn` checks pass, including 10-point Shiny Buff, 15-point common-plus-Shiny stacking, and both roll numbers of a merged effective column.
* The BattlePage suite passes 63 of 68 checks, including combined 15-point pending Buff state and one existing Buff animation; the same 5 unrelated stale battle assertions remain for panel sizing, battle spacing, death-result copy, enemy-freeze random calls, and an existing guard-flow state expectation.
* All 33 DecisionPage checks pass, including Shiny Buff selection from rare and unrestricted token rewards.
* Scoped production and focused-test ESLint passes. The broader BattlePage test file retains 109 pre-existing `testing-library/no-node-access` violations outside this feature's assertions.
* `npm run build` compiles the optimized production build successfully.
* The source audit confirms all rare reward surfaces consume shared token rarity data, save/load preserves token objects generically, and Rules only summarizes token categories, so no additional runtime changes are needed.
* Scoped `git diff --check` passes with line-ending warnings only.

Automated Tests Still Needed

* None for token metadata, shared visuals/tooltips, reward eligibility, debug access, save/load, Buff calculations, merged adjacency, animation reuse, scoped lint, or production compilation.

Manual Execution

* Real-browser visual, reward-generation, battle playback, save/load interaction, and console checks were not run and remain covered by the manual steps above.

---

Feature-017: Make Loot Chest final reward positions truly random after shuffle animation

Test Status

* passed

Manual Test Steps

1. Trigger a Loot Chest and note the three preview rewards.
   Expected: The existing preview shows two generated rewards and one Nothing before all rewards enter identical chests.
2. Follow one visual chest through the existing shuffle animation, then choose it.
   Expected: The revealed reward comes from a mapping assigned after the animation; following the animated chest gives no reliable information about its reward.
3. Repeat the sequence and choose each of the left, middle, and right chests.
   Expected: Every generated reward can appear in every chest position, while the reward set and odds remain unchanged.
4. Wait until Choose is available, change the current language, and choose a chest.
   Expected: Localized text updates without rerandomizing the final chest mapping.
5. Choose a chest, inspect the revealed reward, and select Continue.
   Expected: The already-assigned reward is claimed once and the Loot Chest returns through the existing river, cave, board, or village flow.
6. Inspect the browser console throughout the sequence.
   Expected: No application errors or reward-mapping debug output appears.

Automated Tests Completed

* The pre-implementation focused run passed 13 of 14 checks and failed exactly the new Feature-017 chest-identity assertion because reward-keyed wrappers moved between visual positions after the animation.
* The completed Loot Chest helper, page, and downstream state suites pass all 23 checks.
* Timing coverage confirms final shuffle randomness is not called during preview, reward entry, or visual shuffle, then is called exactly twice for the three-reward Fisher-Yates mapping when the animation completes.
* Position coverage exercises all six three-reward permutations and confirms every reward can occupy every chest.
* Page coverage confirms fixed visual chest DOM identities, changed hidden reward placement, one chosen reveal, and stable reward identity across a language rerender and Continue.
* State coverage confirms unchanged token, potion, Nothing, board, river, cave, village, reward-assignment, and turn-return behavior.
* Scoped production ESLint and LootChestPage test ESLint pass. `lootChest.test.js` also passes scoped lint with only its pre-existing line 42 `jest/no-conditional-expect` rule suppressed; normal lint reports that unchanged existing violation.
* `npm run lint` is unavailable because this project has no `lint` script.
* `npm run build` compiles the optimized production build successfully.
* Scoped `git diff --check` passes with line-ending warnings only.

Automated Tests Still Needed

* None for assignment timing, reward/chest decoupling, complete position reachability, mapping stability, reveal behavior, existing reward generation, or downstream state flows.

Manual Execution

* Real-browser animation tracking, repeated statistical observation, route interaction, and console inspection were not run and remain covered by the manual steps above.

---

Feature-018: Prevent same enemy appearing twice in a row for the same battle level

Test Status

* passed

Manual Test Steps

1. Trigger two random Level 1 battles for different players and record both enemies.
   Expected: The second Level 1 enemy differs from the first, proving the history is shared globally rather than stored per player.
2. Trigger a Level 1 battle, then a Level 2 battle, then another Level 1 battle.
   Expected: The second Level 1 battle excludes the prior Level 1 enemy, while the Level 2 selection does not overwrite Level 1 history.
3. Repeat consecutive random battles for Levels 2 and 3.
   Expected: No enemy repeats directly within its own level; selection remains random among the other enemies.
4. Use Debug to start random level battles.
   Expected: Debug level selection follows the same per-level no-repeat history as board-event battles.
5. Use Debug to choose a specific enemy, then enter both Elite Towers and the Boss battle.
   Expected: Explicit and assigned enemies are used unchanged and do not update or reroll random-level history.
6. Save after a random battle, load the save, and trigger another battle of that level. Also load an older save without enemy history if available.
   Expected: Current saves preserve the previous enemy; legacy saves initialize all history levels safely to empty values.
7. Inspect enemy health, spells, images, rewards, event frequency, and the browser console.
   Expected: Existing enemy/battle data is unchanged and no application errors occur.

Automated Tests Completed

* The pre-implementation focused run passed 40 of 44 checks and produced exactly 4 Feature-018 failures for missing initial history, previous-enemy exclusion, shared random battle tracking, and legacy-state normalization; generic save round-trip already passed.
* The completed selector, initial-state, shared-context, and save suites pass all 46 checks.
* Selector coverage confirms Levels 1, 2, and 3 exclude their own previous IDs while unsupported levels still return null and existing level pools/data remain unchanged.
* Shared-context coverage confirms cross-player history, an intervening level preserving prior history, deterministic no-repeat selection, update-at-selection timing, and explicit Level 4 bypass without consuming randomness.
* Save coverage confirms `lastEnemyByLevel` round-trips with the existing format, while context restore coverage confirms missing legacy history is normalized to null for Levels 1-4.
* The expanded debug and Elite/Boss run passes 79 of 80 checks, including every Feature-018 and fixed-assignment assertion. Its sole failure is an unrelated stale CSS expectation for `100px` maximum height while the existing stylesheet uses `115px`.
* Scoped ESLint passes for all changed production and test files.
* `npm run lint` is unavailable because this project has no `lint` script.
* `npm run build` compiles the optimized production build successfully.
* The source audit confirms only random board/debug level calls pass no enemy ID; explicit debug and fixed Elite/Boss calls retain explicit IDs.
* Scoped `git diff --check` passes with line-ending warnings only.

Automated Tests Still Needed

* None for selector exclusion, per-level/global state, update timing, random entry-point wiring, fixed encounter bypass, save persistence, legacy normalization, scoped lint, or production compilation.

Manual Execution

* Real-browser repeated enemy observation, Debug interaction, full fixed-encounter playthrough, save-file interaction, and console inspection were not run and remain covered by the manual steps above.

---

Feature-019: Add global fullscreen toggle button under music button

Test Status

* passed

Manual Test Steps

1. Open the start page and inspect the two top-left controls.
   Expected: The fullscreen button is directly below music at 70px from the top and 10px from the left, with the same brown/yellow 50px button styling and 30px icon.
2. Select the expand icon while not fullscreen.
   Expected: The browser enters fullscreen; after the browser reports the state change, the icon becomes minimize and the accessible label becomes `Exit fullscreen` or its Japanese equivalent.
3. Select the minimize icon while fullscreen.
   Expected: The browser exits fullscreen; after the browser reports the state change, the icon and label return to Enter fullscreen.
4. Enter or exit fullscreen through browser/system controls where supported.
   Expected: The button updates from the actual browser state without requiring its own click.
5. Deny or block a fullscreen request.
   Expected: The page remains usable, the button continues showing the real non-fullscreen state, and a warning is logged without an application crash.
6. Navigate through start, setup, story, rules, gameplay, battle, village, cave, river, decision, loot chest, winner, and other app routes; open Settings in English and Japanese.
   Expected: The enabled fullscreen control remains globally visible above page/modal content with the correctly localized label.
7. Toggle music and inspect gameplay at common viewport sizes before and after fullscreen.
   Expected: Music, settings visibility, gameplay scaling, and responsive behavior remain unchanged with no console errors.

Automated Tests Completed

* Before implementation, the existing settings assertions passed 20 checks while all 19 new route/modal visibility assertions failed, and the focused fullscreen suite could not load because the component did not exist.
* The completed fullscreen, global visibility, existing music, and app-shell suites pass all 51 checks.
* API coverage confirms standard request/exit calls, no optimistic icon change before browser events, external fullscreen-change synchronization, exact expand/minimize icons, English/Japanese labels, and rejected-request warning behavior.
* Compatibility coverage confirms safe WebKit-prefixed element, request, exit, and change-event fallbacks when standard APIs are unavailable.
* App-shell coverage confirms one global control on every configured route and above open English/Japanese Settings modals while existing gameplay-only settings rules remain unchanged.
* Styling coverage confirms the shared floating button class retains the required 50px brown/yellow styling, 30px icon, hover/focus behavior, and `z-index: 9999`, with only the fullscreen-specific 70px/10px position added.
* Existing MusicPlayer and AppMusic regressions pass, confirming autoplay, playback queue, pause/resume, failure handling, labels, and music placement are unchanged.
* Scoped ESLint passes for all changed production and test files.
* `npm run lint` is unavailable because this project has no `lint` script.
* `npm run build` compiles the optimized production build successfully.
* Scoped `git diff --check` passes for tracked files with line-ending warnings only; the new fullscreen files pass scoped ESLint.

Automated Tests Still Needed

* None for API selection, event-driven state, localization, fallback/rejection behavior, global mounting, shared styling, modal presence, existing music regression, scoped lint, or production compilation.

Manual Execution

* Real-browser fullscreen entry/exit, browser-control/F11 behavior, blocked-permission UX, visual positioning, responsive gameplay scaling, modal pointer interaction, and console inspection were not run and remain covered by the manual steps above.
