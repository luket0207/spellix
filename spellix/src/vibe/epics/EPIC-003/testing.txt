Feature-001: Clean up gameplay control panel

Test Status

* blocked

Current Slice Covered

* The right-side gameplay panel now only renders the current turn sentence and the `Roll Dice` button.
* The old `Current turn` heading, player count text, and player position/debug list are no longer rendered in that panel.
* Existing board rendering, player markers, dice flow, and turn flow remain unchanged.

Manual Test Steps

1. Start a game and go to `/gameplay`.
Expected result: The gameplay page still renders the board and the right-side gameplay panel without console errors.

2. Inspect the right-side gameplay panel.
Expected result: The panel shows `It is currently X player's turn.` and the `Roll Dice` button only.

3. Continue inspecting the same panel.
Expected result: The panel does not show the `Current turn` heading, player count, or any per-player position/debug lines.

4. Click `Roll Dice`, dismiss the dice modal, and complete a valid move.
Expected result: The dice flow still works, movement still works, and the current turn sentence still updates correctly after the turn changes.

5. Inspect the board and settings button after the panel cleanup.
Expected result: The board still renders normally, player pieces still appear on the board, and the settings button is still available.

Automated Tests Completed

* `src/App.test.jsx` now verifies:
  * gameplay navigation still reaches the board and gameplay panel
  * the gameplay page still shows the current turn sentence and `Roll Dice`
  * player state is still observable through board markers instead of the removed gameplay-panel debug text
  * dice rolling, movement, grouped-area rendering, and turn changes still work after the panel cleanup

Checks Run

* `npm run build` passed.
* `$env:CI='true'; npm test -- --watchAll=false --runInBand` failed before running tests because of the existing Jest `source-map-support` error.

Automated Tests Still Needed

* A clean passing Jest run that executes the updated gameplay-panel cleanup assertions in `src/App.test.jsx`.

Notes

* `npm run lint` was not run because there is no `lint` script in `package.json`.
* The remaining automated-test blocker appears unrelated to the Feature-001 gameplay-panel cleanup.

---

Feature-002: Player spell slots, token bag, and spell setup modal

Test Status

* blocked

Current Slice Covered

* Each player still starts with spell data in game setup:
  * 5 red tokens
  * 2 blue tokens
  * 6 spell slots with max capacity 5
  * `hasCommittedInitialSpells: false` until their first save
* The gameplay panel now forces the current player through first-turn spell setup before `Roll Dice` can be used.
* The Spells modal now shows draggable token circles, a token bag drop zone, six spell-slot drop zones, save/cancel confirmations, and committed-token locking after save.
* The Spells modal now lays out the six spell slots horizontally in left-to-right order, with simple horizontal scrolling as the fallback when the modal is narrower.
* The token source area now stays below the spell slots and is labelled `Starting Tokens` during forced setup and `Token Bag` on later visits.
* The token source area now shows token circles only in both forced setup and later Token Bag visits, with no red/blue count text in either case.
* The Spells modal now detects whether the current draft differs from the saved spell data, disables `Save` when there are no unsaved changes, and closes immediately on `Cancel` when nothing has changed.
* After save, the current player's committed spell slots now render in a compact read-only sidebar section below the `Spells` button with the title `Spells`, empty committed spell slots stay visible without placeholder text, the six columns are labelled only with `1` through `6` inside the bordered slot columns, and all columns share a 150px minimum height within the same read-only grid.

Manual Test Steps

1. Start a game and go to `/gameplay`.
Expected result: The gameplay page renders the board, current-turn sentence, `Roll Dice`, and `Spells`, and the first player immediately gets the forced Spells modal.

2. Inspect the forced Spells modal before moving any tokens.
Expected result: The modal shows 6 spell slots in a horizontal left-to-right row, shows `Starting Tokens` below the slots, does not show red/blue count text there, disables `Cancel`, and keeps `Roll Dice` unavailable.

3. Drag tokens from the token bag into spell slots, including moving at least one unsaved token between slots and back to the bag.
Expected result: Draft token moves work inside the modal, and only unsaved tokens can move.

4. Try to place more than 5 tokens in one spell slot.
Expected result: The slot stops accepting additional tokens after reaching 5 tokens.

5. Click `Save` before all 7 starting tokens are placed.
Expected result: The modal stays open and shows a validation message telling the player to place all 7 starting tokens before saving.

6. After placing all 7 tokens, click `Save`, choose `No`, then click `Save` again and choose `Yes`.
Expected result: `No` returns to the modal with the draft preserved; `Yes` commits the setup, closes the modal, and enables `Roll Dice`.

7. Inspect the right-side gameplay panel after saving, then reopen `Spells`.
Expected result: The sidebar shows a read-only `Spells` section for the current player, the six committed spell slots fit in the compact horizontal layout, committed tokens are no longer draggable, any empty committed slots show no `No committed tokens` placeholder text, the columns show only `1` through `6` instead of `Slot 1` through `Slot 6`, and each column is at least 150px tall.

8. End the turn and reach the next player.
Expected result: The next player gets the same forced first-turn spell setup flow once, while previously committed players keep their saved spell slots.

9. Reopen the Spells modal after the current player has already committed spells.
Expected result: The slot row still appears horizontally, the token source area is still inside the modal below the slots, its heading changes to `Token Bag`, and it still does not show red/blue count text.

10. Reopen the Spells modal for a player with no new token changes and do not move anything.
Expected result: `Save` is disabled, `Cancel` closes the modal immediately, and no cancel confirmation appears.

11. Save a player setup with uneven token counts across different committed spell columns and inspect the gameplay sidebar again.
Expected result: All six committed spell columns remain the same height, shorter columns stretch to match the tallest column, and no token stack is cut off.

Automated Tests Completed

* `src/features/spells/spellSetup.test.js` passes and now verifies:
  * bag-to-slot token movement
  * full-slot rejection at 5 tokens
  * slot-to-bag token returns for unsaved tokens
  * unchanged draft state reports no spell changes
  * moved draft state reports spell changes
  * restoring a moved token to its original location reports no spell changes
* `src/features/spells/SpellsModal.test.jsx` passes and now verifies:
  * the spell slot area renders before the token source area
  * all 6 slot labels render in order
  * forced setup uses `Starting Tokens`
  * forced setup does not render red/blue count text
  * later visits use `Token Bag`
  * later visits also do not render red/blue count text
* `src/features/spells/CommittedSpellSlots.test.jsx` passes and now verifies:
  * the committed gameplay-sidebar title is `Spells`
  * all 6 committed slot numbers render as `1` through `6`
  * the committed gameplay display does not render `Slot 1`-style labels
  * the committed display uses its dedicated compact read-only classes
  * committed tokens still render in the read-only slot display
  * empty committed spell slots stay visible without rendering `No committed tokens`
* `src/pages/GameplayPage.test.jsx` passes and now verifies:
  * reopening the Spells modal with no draft changes disables `Save`
  * clicking `Cancel` with no draft changes closes the modal immediately without showing the cancel confirmation
* `src/App.test.jsx` was updated to cover:
  * forced spell modal and blocked dice rolling for an uncommitted player
  * committed-player route rendering with the read-only spell sidebar
  * committed-player spell modal cancel confirmation flow
  * gameplay-ready setup data for the existing dice and movement tests

Checks Run

* `npm run build` passed.
* `$env:CI='true'; npm test -- --watchAll=false --runInBand` ran `src/features/spells/spellSetup.test.js`, `src/features/spells/SpellsModal.test.jsx`, `src/features/spells/CommittedSpellSlots.test.jsx`, and `src/pages/GameplayPage.test.jsx` successfully, but `src/App.test.jsx` still failed to start because of the existing Jest `source-map-support` error.

Automated Tests Still Needed

* A clean passing Jest run for the updated `src/App.test.jsx` suite once the `source-map-support` startup problem is resolved.
* DOM-level automated coverage for the full drag-and-drop and confirmation flow once the app-level Jest suite can run reliably.

Notes

* `npm run lint` was not run because there is no `lint` script in `package.json`.
* `npm run build`, the spell helper tests, the focused Spells modal tests, the committed spell display tests, and the focused gameplay-page spell test all passed for this slice.
* Equal-height/min-height behavior for the committed gameplay columns still relies mainly on manual verification because the current focused tests cover the read-only structure and labels more reliably than CSS layout measurements in this Jest setup.

---

Feature-003: Debug menu token admin tools and token bag capacity handling

Test Status

* blocked

Current Slice Covered

* The settings modal now includes a `Debug` button that opens a separate debug modal without changing routes or replacing the existing settings flow.
* The debug modal now uses a scrollable panel, shows the current player colour when gameplay is active, and provides a simple token-type selector plus `Give Token` action.
* The debug token tool now supports Red, Blue, Orange, Green, Light Blue, Light Green, Black, White, Purple, Yellow, and Grey tokens.
* Given debug tokens are now added only to the current playerâ€™s token bag, stay uncommitted, and are available for later use in the Spells modal.
* When the current playerâ€™s token bag already has 5 tokens, the debug modal now shows a discard-or-replace choice that keeps the bag at 5 tokens after replacement.
* The debug tool is intentionally disabled until the current player completes their initial spell setup because the current implementation still keeps starting setup tokens in the token bag before that first save.

Manual Test Steps

1. Start a game, complete the current playerâ€™s forced first-turn spell setup, and wait until normal gameplay resumes.
Expected result: The current player can now use `Roll Dice` and `Spells`, and their initial setup is committed.

2. Open the settings button, then click `Debug`.
Expected result: The settings modal closes, a separate `Debug` modal opens, and the modal shows the current player colour plus a token-type control.

3. Inspect the `Debug` modal before giving a token.
Expected result: The modal stays scrollable, shows a `Give Token` button, and offers all supported token types in the selector.

4. Choose `Purple` and click `Give Token` while the current playerâ€™s token bag has space.
Expected result: The modal stays open, shows a success message, and the token is added to the current playerâ€™s token bag only.

5. Close the debug modal, open `Spells`, and inspect the `Token Bag` area for the same player.
Expected result: The newly given token appears in that playerâ€™s token bag as an uncommitted token and can be dragged into a spell slot like any other later token.

6. Reopen `Debug` and keep giving tokens until the current playerâ€™s token bag reaches 5 tokens, then try to give one more token.
Expected result: The modal does not add the extra token immediately and instead shows the full-bag discard-or-replace choice.

7. Choose `Discard New Token`.
Expected result: The new token is discarded, the existing token bag stays unchanged, and no committed spell-slot tokens are affected.

8. Trigger the full-bag flow again, select one existing bag token, and choose `Replace Selected Token`.
Expected result: Only the selected bag token is replaced, the new token is added, the token bag still contains 5 tokens, and committed spell slots remain unchanged.

9. End the turn or inspect another player afterward.
Expected result: Existing turn order, dice flow, movement, and other playersâ€™ spell/token data remain unchanged.

Automated Tests Completed

* `src/features/debug/tokenBagAdmin.test.js` passes and now verifies:
  * unique debug token IDs are generated from the current playerâ€™s existing token history
  * token-bag capacity checks report correctly below and at the 5-token limit
  * direct bag adds keep new tokens uncommitted
  * full-bag replacement swaps exactly one existing bag token while keeping bag size unchanged
* `src/features/debug/DebugModal.test.jsx` passes and now verifies:
  * the debug modal renders committed-player controls and uses the dedicated debug panel class
  * the full-bag replacement UI appears with discard/replace actions
  * the token-giving controls stay disabled until the current player finishes initial spell setup
* The previously focused spell-related regression tests still pass:
  * `src/features/spells/spellSetup.test.js`
  * `src/features/spells/SpellsModal.test.jsx`
  * `src/features/spells/CommittedSpellSlots.test.jsx`
  * `src/pages/GameplayPage.test.jsx`

Checks Run

* `npm run build` passed.
* `$env:CI='true'; npm test -- --watchAll=false --runInBand` passed `src/features/debug/tokenBagAdmin.test.js`, `src/features/debug/DebugModal.test.jsx`, `src/features/spells/spellSetup.test.js`, `src/features/spells/SpellsModal.test.jsx`, `src/features/spells/CommittedSpellSlots.test.jsx`, and `src/pages/GameplayPage.test.jsx`, but `src/App.test.jsx` still failed on pre-existing stale assertions for removed spell token-count text, no-change spell-cancel behavior, and one unrelated board-generation expectation.

Automated Tests Still Needed

* App-level integration coverage for the settings-to-debug token flow once the stale `src/App.test.jsx` expectations are updated or isolated.
* A clean full Jest run after the existing `src/App.test.jsx` assertions are brought back in line with the current spell UI behavior.

Notes

* `npm run lint` was not run because there is no `lint` script in `package.json`.
* The debug token flow reuses the existing spell token circles for functional token-type visibility rather than introducing a separate visual system.

---

Feature-004: Replace player colour markers with gameplay piece images

Test Status

* passed

Feature Coverage

* Player setup now stores each player's `gender` plus a derived `pieceImage` filename alongside the existing unique colour selection.
* The setup page now includes `Player X gender` selectors using `boy` and `girl`.
* Setup colour choices now stay exclusive by colour only, so another player cannot select the same colour even if they choose a different gender.
* Changing either gender or colour in setup now updates the stored piece-image filename for that player.
* The board now renders each player using the selected `pieceImage` instead of a coloured circle, while keeping the existing shared-square offsets for players on the same square.
* Board piece images now use the required `25px` height and fall back to a simple colour-text label if a mapped image source is missing.
* The gameplay sidebar now shows the current player's selected piece image instead of the old current-turn sentence.
* The turn-change modal now keeps the colour-name text and also shows the selected current-player piece image at the required size.
* The Spells modal now shows a visible `Spells` title plus the selected current-player piece image during both forced setup and later normal visits.

Manual Test Steps

1. Open `/setup`.
Expected result: Each player section now shows both a gender selector and a colour selector.

2. Inspect the default values for Player 1 and Player 2.
Expected result: Each player has a default gender selection, each player still has a unique default colour, and setup remains usable without console errors.

3. Change Player 1 from `boy` to `girl`.
Expected result: The selection updates immediately, and the stored piece-image mapping for Player 1 changes from the male filename for that colour to the matching female filename.

4. Change Player 2's colour to another available colour, then inspect Player 1's colour options again.
Expected result: Colour exclusivity updates by colour only, so colours already taken by other players are not available while newly freed colours become available.

5. Start the game after changing setup selections.
Expected result: The game still starts normally with the updated player setup data.

6. After starting the game, inspect the player pieces on the board.
Expected result: Each player now appears as their selected gameplay piece image instead of a plain coloured circle.

7. Move or otherwise inspect a square with more than one player piece if available in the current test setup.
Expected result: Shared-square player pieces still use the existing offset positions, so multiple pieces remain at least partially visible.

8. Check the gameplay sidebar after the game starts.
Expected result: The old `It is currently X player's turn.` sentence is gone, and the current player's selected piece image is shown at `150px`.

9. End a move so the next player's turn begins.
Expected result: The turn-change modal still says `It is now X player's turn.` using the colour name, and it also shows that player's selected piece image at `150px`.

10. Open the Spells modal during initial forced spell setup for a player who has not saved spells yet.
Expected result: The modal title is exactly `Spells`, the correct current-player piece image appears at `100px`, and the existing drag/drop plus Save/Cancel behaviour still works.

11. Open the Spells modal later for a player who has already committed spells.
Expected result: The modal title is still `Spells`, the correct current-player piece image still appears at `100px`, and the existing drag/drop plus Save/Cancel behaviour still works.

Automated Tests Completed

* `src/features/gameSetup/gameSetup.test.js` passes and now verifies:
  * players are created with default `boy` piece-selection data
  * the piece-image filename helper maps Boy/Girl plus colour combinations to the expected filenames
* `src/pages/GameSetupPage.test.jsx` passes and now verifies:
  * the setup page renders gender selectors with default piece-image-backed state
  * changing gender updates the stored piece-image filename
  * colour options remain exclusive based on colour only
* `src/features/gameBoard/BoardGrid.test.jsx` passes and now verifies:
  * board player markers render the mapped Boy/Girl piece images
  * board piece images use the required `25px` height
  * shared-square offsets still apply when two players occupy the same square
  * missing piece-image mappings fall back safely to a simple label instead of crashing
* `src/pages/GameplayPage.test.jsx` passes and now verifies:
  * the gameplay sidebar shows the current player's selected piece image instead of the old sentence
  * the turn-change modal shows the next player's colour-name text plus the correct selected piece image
  * the sidebar image updates when the turn changes
  * the later-visit Spells modal still keeps Save disabled and closes immediately on Cancel when there are no unsaved changes
* `src/features/spells/SpellsModal.test.jsx` passes and now verifies:
  * the visible modal title is exactly `Spells`
  * the current-player piece image is shown during forced spell setup
  * the current-player piece image is shown during later normal Spells visits
  * the spell-slot and token-source layout remains unchanged

Checks Run

* `npm run build` passed.
* Focused CRA test commands such as `$env:CI='true'; npm test -- --watchAll=false --runInBand src/pages/GameplayPage.test.jsx` and `$env:CI='true'; npm test -- --watchAll=false --runInBand src/features/spells/SpellsModal.test.jsx` could not be used for focused verification because the current Windows CRA/Jest setup still hits an `EPERM: operation not permitted, lstat 'C:\Users\PC'` startup error for that command shape.
* `$env:CI='true'; npm test -- --watchAll=false --runInBand` passed the setup, board, gameplay, Spells, and debug focused suites for this feature, but `src/App.test.jsx` still failed to start because of the existing Jest `source-map-support` error.

Automated Tests Still Needed

* A clean full Jest run after the existing `src/App.test.jsx` startup issue is resolved.

Notes

* `npm run lint` was not run because there is no `lint` script in `package.json`.
* Feature-004 remains intentionally lo-fi and does not add previews, persistence, or any spell/movement rule changes beyond replacing colour markers with the selected piece images in the required UI locations.

---

Feature-005: Improve board piece visibility and highlight current player

Test Status

* passed

Feature Coverage

* Board player piece images now render at `40px` instead of `25px`.
* The board still uses the existing shared-square offset positions and still keeps the same board square size.
* The current player's board piece now gets a simple pulsing light glow.
* The current player's board piece now receives a higher stacking order so it appears in front when multiple players share a square.
* Gameplay now passes the active current player id into the board renderer so the glow and front-layering move when the turn changes.

Manual Test Steps

1. Start a game and inspect the board player pieces.
Expected result: Player pieces now appear larger at `40px` tall while the board squares remain `30px` by `30px`.

2. Check which player's turn it is at the start of gameplay, then inspect that player's board piece.
Expected result: Only the current player's board piece has the pulsing light glow.

3. End a move so the next player's turn begins.
Expected result: The glow moves from the previous current player to the new current player.

4. Inspect or create a case where more than one player shares the same square.
Expected result: The current player's piece appears visually in front of the others while non-current pieces remain at least partially visible.

5. Roll dice, inspect legal move highlights, and complete a move.
Expected result: Dice rolling, move highlighting, movement, the turn-change modal, the right-side current-player image, and the Spells modal all still work as before.

Automated Tests Completed

* `src/features/gameBoard/BoardGrid.test.jsx` passes and now verifies:
  * board player markers render the selected piece images at `40px`
  * shared-square offsets still apply
  * the current player's board piece gets the current-player glow class
  * the current player's board piece gets the higher stacking order on a shared square
  * missing piece-image mappings still fall back safely without crashing
* `src/pages/GameplayPage.test.jsx` passes and now verifies:
  * gameplay passes the current player id through to the board renderer
  * the board current-player id updates when the turn changes
  * the existing sidebar image, turn-change modal image, and Spells modal behavior still work

Checks Run

* `npm run build` passed.
* `$env:CI='true'; npm test -- --watchAll=false --runInBand` passed the setup, board, gameplay, Spells, and debug focused suites for this feature, but `src/App.test.jsx` still failed to start because of the existing Jest `source-map-support` error.

Automated Tests Still Needed

* A clean full Jest run after the existing `src/App.test.jsx` startup issue is resolved.

Notes

* `npm run lint` was not run because there is no `lint` script in `package.json`.
* Feature-005 stays intentionally lo-fi and limits visual work to the requested board piece size increase, current-player glow, and current-player front stacking.

---

Bug-010: Current player piece image in main gameplay UI is stretching to full container width

Test Status

* passed

Manual Regression Steps

1. Start a game and inspect the current player piece in the right-side gameplay UI.
Expected result: The piece image stays `150px` tall, keeps its proportional width, and does not stretch across the full sidebar width.

2. End a move so the next player's turn begins.
Expected result: The current player piece updates to the next player's image and still keeps the same proportional non-stretched sizing.

3. Open the turn-change modal and the Spells modal.
Expected result: The turn-change modal image still renders at `150px`, the Spells modal image still renders at `100px`, and neither flow regresses because of the sidebar image fix.

Automated Tests Completed

* `src/pages/GameplayPage.test.jsx` passes and now verifies:
  * the current player sidebar image still renders at `150px`
  * the current player sidebar image now keeps `align-self: flex-start` and `width: auto`
  * the current player image still updates correctly when the turn changes
  * the turn-change modal and Spells modal image behavior still works

Checks Run

* `npm run build` passed.
* `$env:CI='true'; npm test -- --watchAll=false --runInBand` passed the gameplay suite that covers this sidebar image fix, along with the other existing focused suites, but `src/App.test.jsx` still failed to start because of the existing Jest `source-map-support` error.

Automated Tests Still Needed

* A clean full Jest run after the existing `src/App.test.jsx` startup issue is resolved.

Notes

* `npm run lint` was not run because there is no `lint` script in `package.json`.
* This bug fix stays narrowly scoped to the main gameplay sidebar image and does not change board, turn-change modal, or Spells modal image sizing rules.

---

Bug-011: Current player board glow should be radial behind the piece, not a rectangular pulse

Test Status

* passed

Manual Regression Steps

1. Start a game and inspect the current player's piece on the board.
Expected result: The glow appears behind the piece as a radial light rather than as a rectangular outline around the image.

2. End a move so the next player's turn begins.
Expected result: The radial glow moves to the new current player's piece and remains behind that piece.

3. Inspect a non-current player piece on the board.
Expected result: Non-current player pieces do not show the glow.

Automated Tests Completed

* `src/features/gameBoard/BoardGrid.test.jsx` passes and now verifies:
  * only the current player's wrapper renders a `board-player-marker-glow` element
  * the glow element is rendered behind the current player's piece within the same marker wrapper
  * the current player's wrapper still gets the higher stacking order on a shared square
  * non-current pieces still do not get the glow

Checks Run

* `npm run build` passed.
* `$env:CI='true'; npm test -- --watchAll=false --runInBand` passed the board, gameplay, setup, Spells, and debug focused suites for this bug, but `src/App.test.jsx` still failed to start because of the existing Jest `source-map-support` error.

Automated Tests Still Needed

* A clean full Jest run after the existing `src/App.test.jsx` startup issue is resolved.

Notes

* `npm run lint` was not run because there is no `lint` script in `package.json`.
* This bug fix stays narrowly scoped to the board glow rendering and does not change piece selection, movement logic, or board coordinates.

---

Bug-012: Board pieces should sit on the current square baseline and cascade correctly when stacked

Test Status

* passed

Manual Regression Steps

1. Start a game and inspect a single player piece on the board.
Expected result: The piece visually sits on the baseline of its actual square instead of looking attached to the square above.

2. Move a player piece to another square.
Expected result: The piece still lands on the correct square baseline, and movement plus legal move highlighting still behave the same.

3. Inspect or create a case where multiple players share the same square.
Expected result: Shared-square pieces cascade to the right first, then upward once horizontal space is used, and they do not cascade downward.

4. Inspect the current player on a shared square.
Expected result: The current player still appears in front, and the radial glow still renders behind that piece.

Automated Tests Completed

* `src/features/gameBoard/BoardGrid.test.jsx` passes and now verifies:
  * single and shared-square board pieces anchor from the square baseline with `bottom` offsets
  * shared-square pieces cascade to the right first
  * later shared-square pieces move upward instead of downward
  * the current player's higher stacking order and radial glow behavior still remain intact

Checks Run

* `npm run build` passed.
* `$env:CI='true'; npm test -- --watchAll=false --runInBand` passed the board, gameplay, setup, Spells, and debug focused suites for this bug, but `src/App.test.jsx` still failed to start because of the existing Jest `source-map-support` error.

Automated Tests Still Needed

* A clean full Jest run after the existing `src/App.test.jsx` startup issue is resolved.

Notes

* `npm run lint` was not run because there is no `lint` script in `package.json`.
* This bug fix stays narrowly scoped to board piece positioning and does not change movement coordinates, board dimensions, or piece image selection.

---

Feature-006: Reusable committed spell slot display and reusable token component

Test Status

* passed

Feature Coverage

* The gameplay sidebar still shows committed spell slots, but the read-only display now renders through the shared `CommittedSpellSlotList` component.
* The committed spell display now keeps the required fixed `300px` by `200px` size in its own component CSS and preserves the six-column read-only layout.
* Duplicate committed tokens now consolidate by token type for display only, with counts shown only when more than one matching token exists.
* The shared `Token` component now supports all current token colours and is used by the committed spell display instead of feature-local token markup.
* The Spells modal flow, layout, drag-and-drop, Save/Cancel flow, and first-turn forced setup remain unchanged.

Manual Test Steps

1. Start a game, complete a player's initial spell setup, and return to the normal gameplay sidebar.
Expected result: The right-side gameplay UI still shows the committed `Spells` display below the existing spell controls without console errors.

2. Inspect the committed spell display size and layout.
Expected result: The display keeps a fixed `300px` width and `200px` height, shows six narrow spell columns, and each slot label is a larger number-only label inside its column.

3. Save a spell setup that places duplicate token colours in the same slot, such as two red tokens in one slot and three blue tokens in another.
Expected result: The committed display shows one token per matching colour in each slot, with `2` centred on the red token and `3` centred on the blue token.

4. Inspect a slot that contains only one committed token and at least one empty slot.
Expected result: The single token remains clearly readable without a forced `1` count, and empty slots show only their slot number with no placeholder text.

5. Inspect a slot that contains many different committed token colours and then open the Spells modal again.
Expected result: The committed sidebar remains readable within the fixed height, the display is still read-only, and the Spells modal layout plus drag-and-drop behavior are unchanged.

Automated Tests Completed

* `src/features/spells/CommittedSpellSlots.test.jsx` passes and now verifies:
  * the gameplay committed spell area renders through the shared committed-display classes
  * six number-only slot labels still render
  * duplicate token types consolidate into one rendered token with the correct count label
  * consolidation does not mutate the saved spell-slot data
  * one slot can render up to 7 consolidated token entries
  * empty slots stay visible without `No committed tokens`
* `src/components/tokens/Token.test.jsx` passes and now verifies:
  * duplicate counts render only when `count > 1`
  * all supported token colour variants map to the shared token classes
* The existing regression coverage still passes for unchanged surrounding flows:
  * `src/features/spells/SpellsModal.test.jsx`
  * `src/pages/GameplayPage.test.jsx`

Checks Run

* `npm run build` passed.
* `$env:CI='true'; npm test -- --watchAll=false --runInBand` passed `src/features/spells/CommittedSpellSlots.test.jsx`, `src/components/tokens/Token.test.jsx`, and the other focused spell/gameplay suites, but `src/App.test.jsx` still failed to start because of the existing Jest `source-map-support` error.

Automated Tests Still Needed

* A clean full Jest run after the existing `src/App.test.jsx` startup issue is resolved.
* Optional DOM-level measurement coverage if the project later needs automated proof of the committed display's exact CSS dimensions instead of manual verification.

Notes

* `npm run lint` was not run because there is no `lint` script in `package.json`.
* The fixed committed-display dimensions are covered manually here because this Jest setup does not reliably assert stylesheet-driven layout measurements from imported CSS.

---

Bug-013: Committed spell slot columns spill out of the spells component container

Test Status

* passed

Manual Regression Steps

1. Start a game, complete a player's initial spell setup, and inspect the committed `Spells` display on the gameplay sidebar.
Expected result: The committed spell display still renders inside its fixed `300px` by `200px` panel without any horizontal spill.

2. Inspect all six committed spell slot columns across the display.
Expected result: All six columns fit inside the container, each `committed-spell-slot-item` uses the reduced `40px` width, and no column extends past the panel edge.

3. Save or inspect a setup with several token colours, including duplicate token types in one slot.
Expected result: Token consolidation still works, token counts remain readable, slot numbers remain inside their column borders, and the fantasy sidebar styling is still intact.

4. Open the Spells modal again after checking the sidebar.
Expected result: The Spells modal layout, drag-and-drop behavior, Save/Cancel flow, and first-turn setup behavior are unchanged.

Automated Tests Completed

* `$env:CI='true'; npm test -- --watchAll=false --runInBand` passes the existing committed-spell and surrounding regression suites:
  * `src/features/spells/CommittedSpellSlots.test.jsx`
  * `src/components/tokens/Token.test.jsx`
  * `src/features/spells/SpellsModal.test.jsx`
  * `src/pages/GameplayPage.test.jsx`

Checks Run

* `npm run build` passed.
* `$env:CI='true'; npm test -- --watchAll=false --runInBand` passed the focused committed-spell, token, gameplay, board, setup, and debug suites, but `src/App.test.jsx` still failed to start because of the existing Jest `source-map-support` error.

Automated Tests Still Needed

* A clean full Jest run after the existing `src/App.test.jsx` startup issue is resolved.
* Optional DOM/CSS measurement coverage if the project later needs automated proof of the exact `40px` committed-slot width instead of manual verification.

Notes

* `npm run lint` was not run because there is no `lint` script in `package.json`.
* This bug fix stays layout-only: committed spell data, token consolidation logic, the reusable token component behavior, and the Spells modal flow were intentionally left unchanged.
