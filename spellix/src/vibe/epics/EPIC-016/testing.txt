---

Feature-001: Add hazard debug trigger and hazard result modal

Test Status

* passed

Manual Test Steps

1. Open Settings, open Debug, and inspect the Hazards section.
   Expected: The localized heading, environment label, and Trigger Hazard button appear.
2. Inspect the environment dropdown.
   Expected: Field, Hills, Gravel, Mud, Stream, River, Woods, Forest, and Mountains appear exactly once.
3. Trigger hazards repeatedly for Field, River, and Mountains.
   Expected: Only positive-weight hazards for the selected environment appear, with selection frequency following the table weights.
4. Trigger a hazard for each environment.
   Expected: The corresponding battle background appears behind the wooden hazard modal, including the stream and river assets.
5. Inspect an English and Japanese hazard modal.
   Expected: The localized Hazard title, two yellow warning icons, localized hazard name/effect, correct font, requested centring and spacing, and no `ul` or `li` appear.
6. Trigger a health-loss hazard.
   Expected: The locked player image and health bar appear, OK is disabled for one second, health loses exactly 5 or 10 without going below zero, then OK enables.
7. Click OK after health loss.
   Expected: The board returns with the next player active and the next-turn modal shown first.
8. Trigger a skip-next-turn hazard.
   Expected: OK is enabled immediately and the affected player's one-time skip flag is set through the existing mechanism.
9. Advance play until the affected player returns.
   Expected: The existing miss-turn modal appears, clears the flag, and advances to the following player.
10. Reduce a player to zero with a hazard and cycle back to their turn.
    Expected: The player remains at zero after the hazard and uses the existing start-of-turn respawn flow.
11. Watch the displayed player while the underlying turn state changes.
    Expected: The original hazard player and post-damage health remain visible until navigation completes.
12. Watch the browser console throughout the flow.
    Expected: No runtime warnings or errors appear.

Automated Tests Completed

* `src/data/hazards.test.js` verifies all nine environment definitions, all 20 exact bilingual table rows, effect amounts, zero-weight exclusion, cumulative weighted boundaries, and totals that do not equal 100.
* `src/features/debug/DebugModal.test.jsx`, `src/i18n/translations.test.js`, and `src/features/battle/battleEnvironments.test.js` verify the localized Debug controls, exact copy, all dropdown options, and stream/river background sources.
* `src/pages/HazardPage.test.jsx` verifies English/Japanese rendering, warning icons, required CSS, no list markup, delayed lethal damage, health display locking, immediate skip marking, player locking, turn advance, and next-turn modal state.
* `src/AppMiniGames.test.jsx` verifies the complete Debug-to-Hazard-to-gameplay integration with deterministic weighted selection and delayed damage.
* The combined EPIC-016 focused run passed 17 tests across 6 suites, and the final unfiltered run passed all 67 tests in those suites.
* Existing skip-turn, lethal-health, and start-of-turn respawn regressions passed 12 focused tests across `GameplayPage.test.jsx` and `GameSetupContext.test.jsx`.
* Direct ESLint passed for every Feature-001 production and test file.
* `npm run build` passed.
* The project does not define an `npm run lint` script.

Automated Tests Still Needed

* None for Feature-001.

Manual Execution

* Not run in a browser during this automated pass.

---

Bug-002: Winner screen starry background should match gameplay page exactly

Test Status

* passed

Manual Regression Steps

1. View the Gameplay page and note the complete background behind its animated stars.
   Expected: The existing radial and linear dark sky gradients fill the viewport.
2. Win the Boss Battle and compare the Winner page directly with Gameplay.
   Expected: The Winner page uses the exact same base gradients, full-height sizing, overflow behavior, isolation, and animated star layers.
3. Inspect the Winner page content.
   Expected: `WINNER!`, the locked winning player image, and Back to Start remain unchanged and visible above the stars.
4. Click Back to Start.
   Expected: The existing End Game reset and start-page navigation still run.
5. Watch the browser console throughout the flow.
   Expected: No runtime warnings or errors appear.

Automated Tests Completed

* The focused `WinnerPage.test.jsx` regression failed before the fix because the winner wrapper did not have the shared sky-page background class.
* The regression now verifies Winner uses `.magical-night-sky-page`.
* The same suite verifies the shared class retains relative positioning, isolation, hidden overflow, `100vh` height, radial gradient, and linear gradient, and verifies Gameplay uses the same class.
* Existing winner title, locked player image, English/Japanese Back to Start copy, and End Game action delegation tests remain passing.
* The focused Winner, Gameplay encounter, and Magical Night Sky run passed all 13 tests across 3 suites.
* Direct ESLint passed for the scoped JSX production and test files.
* `npm run build` passed.

Automated Tests Still Needed

* None for Bug-002.

Manual Execution

* Not run in a browser during this automated pass.

---

Bug-003: Winner screen Back to Start button should return to homepage, not start a new game

Test Status

* passed

Manual Regression Steps

1. Win the Boss Battle and wait for the Winner screen.
   Expected: The locked winner, `WINNER!`, and Back to Start button appear.
2. Click Back to Start.
   Expected: The current game and winner state clear, and the homepage route `/` appears.
3. Inspect the destination.
   Expected: The Spellix heading/logo and normal Start button are visible; Game Setup and Gameplay are not entered automatically.
4. Click the normal Start button.
   Expected: The existing homepage flow proceeds to Game Setup only after this click.
5. Watch the browser console throughout the flow.
   Expected: No runtime warnings or errors appear.

Automated Tests Completed

* Added a `WinnerPage.test.jsx` route-level regression using `resetGame()` followed by navigation to `/`, matching App's current End Game callback.
* The regression passes under React Strict Mode and verifies the Spellix homepage heading, Start button, cleared winner state, and absence of Gameplay.
* The existing Winner callback, English/Japanese button copy, shared background, and locked winner tests remain passing.
* `StartPage.test.jsx` confirms the existing homepage behavior remains passing.
* The focused Winner and Start page run passed all 9 tests across 2 suites.
* Direct ESLint passed for the changed regression test.
* `npm run build` passed.

Automated Tests Still Needed

* None for Bug-003.

Manual Execution

* Not run in a browser during this automated pass.

---

Bug-004: Winner screen should clear game state on load and Back to Start must not redirect to gameplay

Test Status

* passed

Manual Regression Steps

1. Win the Boss Battle and wait for the Winner screen.
   Expected: The starry Winner screen, `WINNER!`, winning player image, and localized Back to Start button still render.
2. Inspect game state after the Winner screen loads.
   Expected: Board, players, turn order, active battle, encounter, and temporary game state are cleared while only the minimal winner display snapshot remains.
3. Click Back to Start.
   Expected: The winner snapshot clears and route `/` shows the Spellix homepage and normal Start button.
4. Remain on the homepage without clicking Start.
   Expected: No players or board are initialized, no next-turn modal appears, and the app never redirects to `/gameplay`.
5. Click the normal Start button.
   Expected: A fresh game initializes only now and the existing Game Setup flow opens.
6. Open `/winner` without a winner display snapshot.
   Expected: The app redirects to the homepage, not Gameplay.
7. Watch the browser console throughout the flow.
   Expected: No runtime warnings or errors appear.

Automated Tests Completed

* A focused `WinnerPage.test.jsx` regression failed before the fix because the Winner screen retained the active battle, board, players, and turn order.
* Winner regressions now verify live-state clearing with snapshot rendering, Strict Mode-safe Back to Start behavior, no automatic game initialization, normal Start-only initialization, invalid-route homepage fallback, localized copy, and the shared starry background.
* `StartPage.test.jsx` verifies the normal Start button invokes game initialization before navigating to Game Setup.
* `EliteBossGameSetupContext.test.jsx` verifies Boss victory captures the winning player's display snapshot.
* The focused Bug-004 run passed all 15 tests across 3 suites.
* Direct ESLint passed for every touched Bug-004 production and test file.
* `npm run build` passed.

Automated Tests Still Needed

* None for Bug-004.

Manual Execution

* Not run in a browser during this automated pass.

---

Bug-005: Revert Winner screen state-clearing change and only fix Back to Start navigation

Test Status

* passed

Manual Regression Steps

1. Win the Boss Battle and wait for the Winner screen.
   Expected: The screen loads without a runtime error and still shows the starry background, `WINNER!`, winning player image, and localized Back to Start button.
2. Inspect final game state while the Winner screen is displayed.
   Expected: The final battle, board, players, turn order, and Elite/Boss assignments remain intact.
3. Temporarily provide no Elite/Boss assignments to DebugModal.
   Expected: DebugModal remains usable and omits only the Elite / Boss Enemies section instead of crashing.
4. Click Back to Start.
   Expected: Route `/` shows the Spellix homepage and normal Start button without navigating to `/gameplay`.
5. Remain on the homepage without clicking Start.
   Expected: The final game state remains unchanged and no new game starts automatically.
6. Click the normal Start button.
   Expected: Fresh game state is created now and the existing Game Setup flow opens.
7. Watch the browser console throughout the flow.
   Expected: No runtime warnings or errors appear.

Automated Tests Completed

* Winner and DebugModal regressions failed before the fix by proving the final game state was cleared and reproducing the null assignment runtime error.
* `WinnerPage.test.jsx` now verifies intact final game state, navigation-only Back to Start behavior under Strict Mode, no `/gameplay` redirect, no automatic reset, normal Start-only rebuilding, winner rendering, invalid-route fallback, localized copy, and the shared starry background.
* `DebugModal.test.jsx` verifies null assignments do not crash or render the assignment section while existing assignment, hazard, battle, token, and potion controls remain passing.
* `StartPage.test.jsx` and `EliteBossGameSetupContext.test.jsx` verify normal Start initialization and the existing Boss victory winner snapshot.
* The focused Bug-005 run passed all 30 tests across 4 suites.
* Direct ESLint passed for every touched Bug-005 production and test file.
* `npm run build` passed.

Automated Tests Still Needed

* None for Bug-005.

Manual Execution

* Not run in a browser during this automated pass.

---

Feature-003: Implement Elite Tower battles, Boss Battle access, and win condition

Test Status

* passed

Manual Test Steps

1. Start a new game and open Debug several times.
   Expected: Exactly three distinct level-4 enemies remain fixed under `Elite / Boss Enemies` for Gravel, Woods, and Boss.
2. Inspect the gameplay panel for multiple players.
   Expected: Only the two localized elite enemy names appear above committed spells, the boss is omitted, progress is player-specific, the panel is no taller than `100px`, and no checkbox input is present.
3. Land on both generated elite tower locations, including a tower already completed.
   Expected: The tower image identity selects the matching fixed enemy every visit and the matching feature background appears.
4. Win one elite battle and lose the other.
   Expected: Only the winning tower is marked for the encounter player, the win enters normal rewards, and the loss enters the normal loss/respawn flow.
5. Land on Boss Battle before the same player completes both towers.
   Expected: No battle starts; the exact localized lockout copy, boss-castle background, locked player image, health bar, and disabled OK appear.
6. Wait one second on the lockout page, then click OK.
   Expected: The locked player's health becomes zero, OK enables, gameplay returns with the next player and pending next-turn modal, and no immediate respawn occurs.
7. Complete both towers for one player and land on Boss Battle.
   Expected: The assigned boss starts over the castle background at `150 / 150`, without changing its base enemy data.
8. Lose the boss battle.
   Expected: Normal battle loss runs and both elite completion values remain true.
9. Win the boss battle.
   Expected: Rewards and turn advance are skipped; the starry winner page shows the locked winner, yellow `WINNER!` title, and localized Back to Start button with the required spacing.
10. Click Back to Start.
    Expected: The same reset and start-page navigation as End Game runs.
11. Watch the browser console throughout the flow.
    Expected: No runtime warnings or errors appear.

Automated Tests Completed

* `eliteBossEncounters.test.js` verifies three distinct level-4 assignments, generated Gravel/Woods image identity resolution, boss identity, and both-tower readiness.
* `gameSetup.test.js` verifies default and preserved per-player elite progress.
* `EliteBossGameSetupContext.test.jsx` verifies player-scoped elite victory, normal elite rewards, boss `150 / 150` override, unchanged base health, reward-free boss victory, normal boss loss, progress preservation, and locked-boss state.
* `GameplayEliteBoss.test.jsx` verifies the two-enemy visual-only progress UI, `100px` maximum height, repeated completed-tower battles, assigned enemy selection, unready boss routing, and ready boss routing.
* `DebugModal.test.jsx` verifies the exact `Elite / Boss Enemies` heading and all three assigned names.
* `BossNotReadyPage.test.jsx` verifies exact English/Japanese copy, the disabled one-second timer under React Strict Mode, zero health, locked player, turn advance, and pending next-turn modal.
* `WinnerPage.test.jsx` verifies the locked winner, `WINNER!`, localized Back to Start copy, and delegation to the End Game action.
* The focused Feature-003 run passed all 38 tests across 7 suites.
* Existing `RewardPage.test.jsx` and `GameSetupContext.test.jsx` regression suites passed.
* The complete Gameplay suite passed 56 tests and retained 6 unrelated baseline failures for existing spell warning copy, dice result copy, and image styles.
* The complete Battle suite passed 49 tests and retained 3 unrelated baseline failures for existing CSS expectations and dice result copy.
* Direct ESLint passed for every touched Feature-003 production and test file.
* `npm run build` passed.

Automated Tests Still Needed

* None for Feature-003.

Manual Execution

* Not run in a browser during this automated pass.

---

Feature-002: Treat multi-square board features as one logical landing and movement square

Test Status

* passed

Manual Test Steps

1. Roll so only one cell of Field Village is reachable.
   Expected: All four Field Village cells highlight and clicking any highlighted cell lands on the same logical village.
2. Repeat with Forest Village.
   Expected: All four cells highlight and behave as one logical destination.
3. Land one player on a village, then land another player on the same village.
   Expected: Both players share the same logical feature but use different visual cells while an unoccupied cell remains.
4. Fill all four visual cells and land another player on the same village.
   Expected: The player still lands on the feature and overlap occurs only after all cells are occupied.
5. Land on Elite Tower Gravel, Elite Tower Woods, and Boss Battle.
   Expected: Each player is visually placed inside the selected fixed footprint and all cells share one logical feature identity.
6. Highlight each fixed feature from one reachable edge cell.
   Expected: Every cell in that elite or boss footprint highlights.
7. Start on a 2x2 village and move exactly one square.
   Expected: The two cells above, below, left, and right of the footprint are valid where in bounds; no internal feature step is charged.
8. Start on each corner elite/boss footprint and move one square.
   Expected: Every in-bounds external neighbor is valid and out-of-bounds neighbors are ignored.
9. Move into a multi-square feature through different footprint cells.
   Expected: Every entry stops on the same logical feature and chooses a visual cell using the occupancy priority rather than treating the clicked cell as a separate destination.
10. Move away from a multi-square feature to a normal square.
    Expected: The player returns to a normal `{ x, y }` position and normal one-square movement remains unchanged.
11. Trigger any currently connected landing behavior from a multi-square feature.
    Expected: One click produces one position update and one turn advance; no duplicate landing action occurs for the footprint cells.
12. Watch the browser console throughout the flow.
    Expected: No runtime warnings or errors appear.

Automated Tests Completed

* `movement.test.js` verifies generated feature node grouping, partial reachability, all eight one-step exits from a 2x2 footprint, both fixed elite node identities, boss identity, and in-bounds corner exits.
* `multiSquareFeatures.test.js` verifies complete footprint lookup, random unoccupied-cell selection, all-occupied fallback, normal-square preservation, and visual placement inside Field/Forest-style generated, both elite, and boss footprints.
* `BoardGrid.test.jsx` verifies every generated village cell and all fixed elite/boss cells highlight together from one logical node.
* `GameplayPage.test.jsx` verifies Gameplay stores `type`, logical `featureId`, and independently selected visual `x/y` coordinates through the existing player-position update.
* The focused Feature-002 run passed 14 tests across 4 suites.
* The complete board generation, movement, placement, and BoardGrid suites passed all 45 tests.
* The complete Gameplay suite passed 52 tests including Feature-002. Six unrelated baseline assertions remain failed for previously changed spell warning copy, dice result copy, and player image styles.
* Direct ESLint passed for all Feature-002 production files and pure movement/placement tests.
* `BoardGrid.test.jsx` retains 15 older `testing-library/no-node-access` violations, and `GameplayPage.test.jsx` retains 13 older violations; none are in the Feature-002 additions.
* `npm run build` passed.

Automated Tests Still Needed

* None for Feature-002.

Manual Execution

* Not run in a browser during this automated pass.

---

Bug-001: Health-loss hazards do not apply damage or enable OK button

Test Status

* passed

Manual Regression Steps

1. Trigger a health-loss hazard that removes 5 health in a development build using React Strict Mode.
   Expected: The player image and health bar appear, and OK starts disabled.
2. Wait one second.
   Expected: Exactly 5 health is removed once, the health bar updates, and OK enables.
3. Wait another second without clicking OK.
   Expected: No second damage application occurs.
4. Click OK.
   Expected: Gameplay returns with the next player active and the next-turn modal shown first.
5. Repeat with a hazard that removes 10 health.
   Expected: Exactly 10 health is removed after one second and OK enables.
6. Repeat with health low enough to reach zero.
   Expected: Health clamps at zero, OK enables, and respawn waits until the affected player's next turn.
7. Trigger a skip-next-turn hazard.
   Expected: OK remains immediately available and the existing one-time skip flow is unchanged.

Automated Tests Completed

* A new `HazardPage.test.jsx` Strict Mode regression failed before the fix with health remaining at 100 and OK disabled after one second.
* The same regression passes after the fix, verifies 5 health is removed, verifies OK enables, and verifies waiting another second does not apply damage twice.
* Seventeen focused behavioral tests passed across `HazardPage.test.jsx`, `AppMiniGames.test.jsx`, `GameplayPage.test.jsx`, and `GameSetupContext.test.jsx`, covering 5/10/lethal health, skip behavior, locked display, navigation, next-turn modal state, and respawn ordering.
* Direct ESLint passed for `HazardPage.jsx` and `HazardPage.test.jsx`.
* `npm run build` passed.

Remaining Test Gap

* The complete hazard-page suite currently has one unrelated Feature-001 styling failure because `.hazard-title-row` is `margin-bottom: 20px` while its existing test expects `60px`. That concurrent CSS change was not altered as part of this timer-only bug fix.

Manual Execution

* Not run in a browser during this automated pass.
