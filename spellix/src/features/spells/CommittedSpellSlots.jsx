import CommittedSpellSlotList from '../../components/spells/CommittedSpellSlotList';

function CommittedSpellSlots({ spellSlots }) {
  return <CommittedSpellSlotList spellSlots={spellSlots} />;
}

export default CommittedSpellSlots;
